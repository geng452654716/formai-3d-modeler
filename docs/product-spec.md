# 产品规格（MVP）

## 1. 产品定位

这是一个面向 3D 打印的 AI 建模桌面软件。核心用户不需要掌握传统建模软件的完整操作体系，而是通过图片、自然语言、尺寸参数和局部选择来创建、检查和修改模型。

首版重点不是人物雕刻或动画，而是：

1. 产品外观与硬表面模型；
2. 电子产品外壳、保护壳和包裹件；
3. 能以毫米为单位精确制造的 3D 打印模型；
4. 在满足精确尺寸的前提下，逐步增加游戏/道具类多边形建模能力。

## 2. 已确认需求

### 2.1 输入

- 单张参考图片；
- 正面、侧面、背面等多视图图片；
- 对电子开发板拍照，自动识别 USB、供电、按键、指示灯等需要暴露的接口位置；
- 自然语言建模与修改指令；
- 关键物理尺寸；
- 电子元件的长、宽、高、位置和安装方向；
- 后续支持导入电子元件的 STEP、STL 或 3MF 模型。

单张图片生成完整 360° 模型时，未显示区域只能由 AI 推测。精确制造至少需要用户在图片上选择两个标定点，并输入两点之间的真实尺寸，由系统计算像素距离和毫米/像素比例；电子外壳类模型优先要求多视图或元件尺寸。第一版多视角联合标定使用各视角尺度中位数和最大偏差提示，不等同于摄影测量相机位姿求解。

接口识别结果必须包含位置、估算宽高、置信度、是否需要开孔和可选二维开孔轮廓，并与精确 CAD 特征分开标记；不同视角的接口对应必须区分“已匹配”和“待确认”。待确认接口必须由用户确认、拆分或忽略后才能应用联合结果；用户可修正接口 ID、类型、所在面、尺寸、偏移、开孔要求和开孔轮廓，修正值优先于单图识别估算。单视角识别完成后，用户还可在已标定二维照片平面上直接选择、拖动和缩放接口框；系统按毫米/像素比例回算接口宽高、以图片中心估算横向偏移、以接口框底边估算底部偏移。照片框编辑不是相机位姿求解，修改后必须重新执行多视角联合标定。

### 2.2 建模能力

- 创建基础几何体和硬表面模型；
- 精确移动、旋转和缩放；
- 实体并集、差集、交集；
- 内部掏空和指定壁厚；
- 按电子元件尺寸生成包络空间；
- 为元件设置装配间隙；
- 创建开孔、线缆出口、散热孔、螺丝孔和螺丝柱；
- 倒角和圆角；
- 外壳分件、上盖/下盖以及装配结构；
- 颜色调整；
- 后续增加自由曲面和有机网格建模。

### 2.3 交互能力

- 鼠标旋转、缩放和平移视角；
- 点击选择对象或部件；
- 使用三维操控器移动、旋转和缩放；
- 框选或点击局部区域后向 AI 下达指令；参数化精确 CAD 已支持点击单个稳定面、点击单条稳定边、按顺序手工多选 2–64 条稳定边链，并框选当前视角射线可见的多个稳定面；
- 框选先执行屏幕空间三角形与矩形相交检测，再用摄像机射线第一命中过滤隐藏面，避免把被遮挡的背面一并提交；
- 截取局部视图并附加给 AI；稳定面 ID、原始毫米坐标、逆变换后的外法线、零件包围盒、摄像机矩阵和尺寸上下文与 PNG 截图一起提交；
- 手工编辑顶点、边和面；
- 撤销、重做；
- 模型版本历史；
- 两个版本的重叠、并排和差异对比；
- 导出 STL 和 3MF。

### 2.4 AI 行为

- 复用本机 Codex 的安装和现有 API Key 登录状态，不要求用户在本产品界面重复填写密钥；
- Codex 自动执行修改；
- 每次 AI 修改前必须创建版本快照；
- 执行失败时不覆盖最后一个有效版本；
- 修改完成后自动运行几何和打印检查；
- 用户可以查看 AI 当前阶段、工具调用和结果摘要；
- 用户可以从历史版本恢复或从任意版本创建分支。

当前交付状态：已支持历史列表、任意参数化版本恢复和从恢复节点继续修改形成分支，并可比较全部参数与通用开孔元数据。存在本机精确快照时，可加载历史 STL 与当前精确 CAD 做半透明重叠和按真实毫米尺寸并排视觉比较，也可按稳定零件 ID 对历史与当前 STEP 执行 OpenCascade 双向精确布尔差集，以绿色/红色高亮新增和删除区域并报告体积。参数化 CAD 清单现已记录“几何签名匹配第一版”的面描述和稳定 ID，版本实体对比可统计共享、新增与消失编号；精确 CAD 的专用选择 STL 还会把连续 `triangleIndex` 区间回映射到这些稳定面，支持点击单面和框选多个当前可见面。该能力不是 OpenCascade 原生永久拓扑命名，`triangleIndex` 只对同一次生成的选择网格有效，大幅拓扑变化、对称面或布尔重建仍可能重新编号。上传 STL 使用独立网格区域/壁厚采样协议，不伪装成参数化 CAD 稳定面选择；界面必须明确区分元数据差异、STL 视觉比较、实体级精确布尔差异和第一版稳定面选择。

稳定 CAD 面局部特征第一版已经接入：用户点击单个稳定平面并输入包含直径或半径、以及高度或深度的中文指令后，可生成圆形凸台或切除圆孔。Codex 登录时只能返回受限的 `add-cylinder | cut-cylinder` 结构化计划，不能输出或执行任意 Python、CadQuery、shell，也不能在同一计划中同时修改整模参数；计划中的 `partId + stableFaceId` 必须逐字绑定当前点击选择，最终点击中心与法线只取自前端已验证上下文。Codex 不可用时才使用确定性中文尺寸解析器。Worker 必须从当前 STEP 重新定位 OpenCascade 面并使用真实外法线执行布尔；视口点击法线只做点积一致性检查。结果必须通过有效性、封闭性、单一 Solid、体积方向、点击距离、法线方向和 STL 再导入检查，之后重新生成面签名、选择 STL、面映射和 3MF。修改后原 `triangleIndex` 立即失效。`localFeatures[]` 会在后续参数化整模重建时从基础参数化实体开始按顺序安全重放，并使用每条记录的 `targetFace` 几何签名快照辅助重新定位；稳定面、记录中心、外法线、尺寸或布尔结果任一不一致时，必须拒绝整次重建并保留最后有效模型，不能静默跳过。第一版仍不支持框选多面、曲面 UV、矩形孔/槽孔、整面拉伸、局部圆角/倒角或任意拓扑编辑。


### 2.5 拆件、连接与紧固结构

- 提供装配视图和拆分视图，装配视图默认显示最终闭合效果；
- 自动拆件支持适配打印空间、最少支撑和手工分型面三种策略；
- 连接结构首批支持圆柱定位销、D 形防转销、燕尾榫、球头和磁铁孔；
- 紧固结构支持 PLA/PETG 卡扣、M2/M2.5/M3 螺丝柱、通孔、沉孔和沉头孔；
- 螺纹提供两个独立选项：M2/M2.5/M3 打印友好圆脊近似内外螺纹，以及已经实体化的 M2/M2.5/M3 ISO 公制粗牙 60° 内外螺纹；界面必须明确区分两种牙型；
- 所有公母连接结构必须提供独立间隙参数，并按材料和喷嘴给出过紧、薄壁与断裂风险提示；
- 手办和任意网格模型需要增加曲面切割、连通区域分析、薄弱区域分析、非流形修复和打印方向评估。

当前迭代已完成制造方案规划器、通用 CAD/上传 STL 的 OpenCascade 平面拆件与封闭补面、体积守恒验证、桌面命令接入、视口装配/拆分显示和 STEP/STL 导出。圆柱定位销、D 形防转销、燕尾榫、球头/球窝、磁铁孔，PLA/PETG 可拆悬臂卡扣，M2/M2.5/M3 螺丝柱、底孔、通孔和圆柱沉孔，以及两类 M2/M2.5/M3 内外螺纹均已经由 OpenCascade 布尔写入精确实体。打印友好近似螺纹使用圆脊截面，面向 FDM 直接打印装配；ISO 螺纹按 ISO 261 粗牙系列使用 M2×0.40、M2.5×0.45、M3×0.50 毫米螺距，并沿螺旋扫掠 ISO 基本 60° 梯形截面。两类牙型在界面和协议中是独立选项，不能互相冒充。

ISO 内外螺纹使用同一螺旋起点和相位，沿 X/Y/Z 任意切割轴的局部 U/V 坐标生成；结果返回公称直径、螺距、有效长度、60° 牙型角和“ISO 公制粗牙基本牙型”标准名称。考虑 P1S、0.4 毫米喷嘴和 PLA/PETG 的实际打印偏差，配合内螺纹孔仍加入独立径向补偿和受限轴向补偿；该补偿属于 FDM 制造适配，不改变 ISO 基本牙型选项的标识。生成过程会检查实体有效性、封闭性、单一 Solid、连接结构最小设计壁厚、公母间隙和装配干涉。`minimumDesignedWallMm` 只校验新生成连接结构，不得与全模型壁厚采样混用；全模型结果由独立壁厚分析协议提供。界面必须继续明确区分“识别结果”“已规划”和“已生成精确实体”。

### 2.6 全模型壁厚分析

- 对任意封闭 CAD STEP/STP 零件和用户上传 STL 执行全局表面采样，不绑定固定模型类别。
- 默认按 P1S、0.4 毫米喷嘴和 PLA/PETG 设计检查分为四级：危险 `< 0.80` 毫米、偏薄 `0.80–1.20` 毫米、建议 `1.20–2.00` 毫米、充足 `>= 2.00` 毫米。
- 界面必须显示最薄值、5% 分位、有效样本数、覆盖率、四级样本计数和三维红/橙/黄/绿热力图。
- 结果名称必须明确为“表面法向射线采样估算”；不得宣传为有限元、受力、疲劳寿命或连续精确壁厚场。
- 覆盖率过低、复杂非流形、自相交或无法形成有效封闭实体时必须中文拒绝，不得输出伪造的安全结论。
- 模型几何、版本、来源或拆件状态改变后，旧壁厚结果必须失效；CAD 与上传 STL 之间不得交叉复用热力图。
- 最近样本颜色只用于显示离散风险，不代表每个顶点都完成了独立精确射线求交。
- 热力图开启后，用户可点击模型表面选择最近的离散采样区域，或一键定位最薄处；界面显示三维标记、原始毫米坐标、估算壁厚和风险等级。
- CAD 局部选择会作为明确几何上下文附加给 Codex；参数化修改完成后，系统对同一零件重新执行壁厚采样，并按原坐标寻找最近采样点，报告修改前后的估算壁厚变化。
- 上传封闭单 Solid STL 已建立第一版受限局部实体重建：输入来自壁厚离散样本的原始毫米坐标和单位内法向，支持沿外法向增加圆形凸台，以及沿内法向切除圆孔。
- 上传 STL 局部修改后必须基于当前工作文件重新执行“表面法向射线采样估算”，并保留原始上传 STL；该能力不等同于参数化特征树、任意网格编辑、自由曲面重建或多 Solid 局部编辑。

### 2.7 照片精确开孔与制造校验

- 经过人工复核且 `requiresOpening=true` 的接口，在能够确定正交接口面后进入通用精确开孔协议；支持正面、背面、左侧、右侧、顶部和底部。
- 二维开孔轮廓支持圆孔、矩形孔、圆角矩形孔和槽孔；用户可人工修改接口类型、所在面、宽高、位置、是否开孔和轮廓。
- 开孔坐标来自已标定的二维照片平面。单张透视图不能单独确定精确接口面，也不能提供相机位姿、摄影测量或任意物体三维重建结果。
- OpenCascade 对顶部开孔切削上盖，对其余五面开孔切削主体；每次布尔运算后必须检查实际相交、实体有效性、封闭性和单一 Solid。
- 生成前检查保守矩形包围盒的边缘余量和同面孔间距；这些检查不是圆角边界精确应力分析、受力分析、疲劳分析或有限元分析。
- 新照片开孔保存相对接口面水平中心和底边的毫米定位锚点；之后改变外壳长、宽、高时，系统按当前接口面尺寸重新计算中心坐标，但开孔宽高、水平偏移和底部偏移不会按比例缩放。
- 老项目没有照片定位锚点元数据时继续使用原固定毫米坐标，并在界面提示重新复核；自动重算不能替代卡尺确认。
- 照片与 AI 尺寸只能作为建模输入，制造前必须使用卡尺复核关键孔位、孔径、板件包络和装配间隙。

## 3. 精度与单位

- 项目内部统一使用毫米；
- 视口、参数输入、导入和导出都必须明确单位；
- STL 本身不保存单位，因此导出元数据和导入流程必须固定按毫米解释；
- 每个项目保存打印机、喷嘴、材料、默认间隙和最小壁厚配置；
- 模型导出前检查包围盒是否超出打印机成型尺寸。

首版默认打印配置：

- 打印机：Bambu Lab P1S；
- 喷嘴：0.4 mm；
- 材料：PLA、PETG；
- 默认外壳壁厚：2.0 mm（可修改）；
- 默认 XY 装配间隙：0.30 mm（可修改）；
- 默认 Z 装配间隙：0.50 mm（可修改）。

## 4. 电子元件包裹工作流

1. 用户创建或导入电子元件；
2. 为每个元件记录精确尺寸、禁入空间和接口位置；
3. 用户输入外壳风格、壁厚、间隙和开合方式；
4. AI 生成元件包络体；
5. AI 在包络体外生成外壳实体；
6. AI 创建内部空腔、安装柱、定位槽和接口开孔；
7. 软件检查元件碰撞、最小间隙、最小壁厚和封闭性；
8. 用户通过局部选择继续修改；
9. 导出 STL 或 3MF，并交给拓竹切片软件。

## 5. 参数化与网格编辑的边界

精确 CAD 实体与自由网格编辑是两种不同的数据模型：

- 参数化实体适合尺寸、壁厚、圆角、孔位、外壳和装配；
- 网格编辑适合直接修改顶点、边、面和有机外形。

首版将参数化实体作为模型源数据。用户进入顶点/边/面编辑时，系统创建一个“网格分支”，保留原参数化版本。网格分支可以继续打印和局部编辑，但不能保证所有 CAD 特征仍可无损参数化修改。

## 6. 导出范围

### STL

- 输出毫米尺度的三角网格；
- 支持单零件或按部件分别导出；
- 导出前检查封闭、流形、自交、法线和退化三角形。

### 3MF

目标为拓竹工程 3MF，除几何、单位、对象层级、变换和基础颜色外，还需要保存 P1S 打印机配置、热床摆盘、材料、层高、填充和支撑等切片工程信息。实施时先完成几何 3MF，再逐步补齐并验证 Bambu Studio 工程兼容性。

## 7. MVP 验收案例

首个端到端案例使用 ESP32-S3 N16R8 42 针开发板外壳：

1. 拍摄开发板正反面及接口侧照片，自动识别 PCB、排针、USB 接口、按钮和指示灯；
2. 用户确认识别结果，并提供至少一个真实尺寸用于照片尺度校准；
3. 生成带指定壁厚和装配间隙的上下壳；
4. 自动创建内部空腔、PCB 定位结构和接口开孔；
5. 用户说“顶盖增加散热孔，四角更圆，接口孔右移 1.5 mm”；
6. Codex 自动修改并保存新版本；
7. 视口显示前后版本差异；
8. 验证模型尺寸和封闭性；
9. 导出 STL 和适用于 P1S 的拓竹工程 3MF。


### 37. 手工多选边链圆角与倒角第一版（已实现）

- 用户可切换“多选边链”，在任意参数化 CAD 零件上按顺序选择 2–64 条稳定边；视口以中文显示已选边数、零件数和精确解析状态。
- 每条边必须完成当前修订的 OpenCascade 精确解析，逐边保存所属稳定面、稳定边、真实毫米点击点、外法线和 UV。不同所属稳定面可以加入同一边链，但不同零件不能混选。
- 支持无分叉开放链与闭合链；拒绝重复物理边、不连续边、分叉拓扑、退化边、少于 2 条和超过 64 条的选择。系统不会自动添加用户未选择的边。
- 中文指令可对整条手工边链执行统一半径圆角或统一距离倒角。Codex 只能返回受限结构化计划，逐边列表必须与用户选择完全同序，不能增删、换序、替换或退化为单边操作。
- 成功后清除已失效的边链选择并报告实际处理边数；Worker 或重放失败时保留最后有效模型，当前执行失败时还要保留全部手工选择以便用户调整后重试。
- 版本记录保存逐边稳定 ID、所属面和几何签名快照；整模重建时逐边重新定位和刷新快照，任一目标失配都必须原子拒绝，不能只重放部分边。
- 第一版不支持可变半径、连续性等级控制、分叉边网、多个独立边链或永久拓扑命名。

2026-07-21 第 37 阶段完整回归通过：前端 18 个测试文件 141/141，Rust 33/33，稳定 CAD 边特征 22/22，参数化局部特征安全重放 17/17，曲面命中 7/7；TypeScript/Vite 生产构建、Cargo 检查与测试、Python 语法、Rust 格式和差异格式检查均通过。仅保留既有构建分块与 CadQuery/Pyparsing 弃用警告。


### 38. 对象变换、颜色和所见即所得导出第一版（已实现）

- 用户可以选择任意普通 CAD 零件、上传 STL、制造拆件或参考元件，通过鼠标三维操控器或精确数值移动、旋转和均匀缩放，并调整零件基础颜色。
- 位置以毫米显示，旋转以度显示；第一版不允许三轴非均匀缩放，避免误改适配电子元件和打印机的真实尺寸。
- 装配基础位置、拆分视图临时展开距离和用户真实变换必须分层。拆分只用于观察，不得污染最终制造坐标。
- 位置、旋转、缩放和颜色进入版本历史、本机会话持久化、撤销与重做。一次连续三维操控器拖动只产生一个版本；历史必须区分几何修改与纯显示修改，纯对象变换不要求重新构造 CAD 实体，跨过几何修改时不得错误保留当前实体。
- 变换后的单零件 STL 必须与视口所见位置、角度和尺寸一致；完整装配 3MF 必须保留多个对象、毫米单位、对象名称、颜色和装配状态。
- STEP 继续代表参数化 OpenCascade 原始坐标，界面必须明确提示其不应用视口变换。几何 3MF 可由 Bambu Studio 打开，但在真正写入并验证拓竹专有切片字段前不得称为完整拓竹工程文件。
- 导出只能读取当前模型 artifacts 清单中的普通 STL 文件名，不接受任意路径；桌面端和 Worker 都必须执行范围校验，并在文件完整写入和复核后才复制到下载目录。CAD 制造拆件与上传 STL 拆件必须按各自在视口中的稳定对象 ID 读取变换，不得丢失已完成的移动、旋转、缩放或颜色调整。

2026-07-21 第 38 阶段回归结果：前端 156/156、Rust 35/35、Python 变换导出 3/3；生产构建、类型、格式和浏览器交互验收通过。

### 39. 上传 STL 网格元素手工编辑第一版（已实现）

- 用户上传的任意封闭单 Solid STL 可以在原始模型视图中选择单个顶点、单条三角边或单个三角面，并使用源模型 XYZ 毫米坐标进行精确位移；第 41 阶段已在同一协议上补齐点击单选与框选多选。
- 选择和高亮必须与当前模型修订绑定；顶点、边和面分别使用黄色球体、粗线和半透明三角面显示。拆件结果显示时不得误选拆件网格，必须先返回原始上传模型。
- STL 中同坐标的重复顶点副本必须同步移动。修改后必须重新执行退化面、封闭性、实体有效性、Solid 数量和体积检查；需要自动修洞或清理才能通过的结果视为失败。
- 失败不会覆盖最后有效模型；成功后旧拆件和壁厚分析立即失效，并生成中文结果与版本记录。
- 第 39 阶段不是自由雕刻：当时不支持拓扑增删、挤出、焊接、分裂、框选多元素或多 Solid 局部编辑；第 41 阶段仅补齐受限的同类元素框选批量位移。参数化 CAD 继续走稳定面/稳定边局部特征协议。
- 修改前后快照绑定对应上传模型修订号；第 40 阶段已经补齐工作 STL、STEP 和清单的受管恢复，因此网格修改现已具备真实文件级撤销、重做和版本历史恢复。

2026-07-21 第 39 阶段回归结果：前端 162/162、Rust 35/35、Python 4/4；构建、格式和浏览器真实交互验收通过。

### 40. 上传模型精确快照恢复与完整撤销/重做（已实现）

- 首次导入任意 STL 必须同时生成原始 STL、当前工作 STL、当前工作 STEP 和模型清单，后续上传模型修改不得丢失原始上传文件。
- 上传模型快照和参数化 CAD 快照必须明确区分。上传模型快照记录 `modelSource=uploaded-stl` 和不可变修订号，版本记录、快照清单和恢复请求三者修订号必须完全一致。
- 恢复只接受当前项目 `artifacts/versions` 下的受管直接子目录和固定工作文件清单；不得接受任意外部快照路径、路径穿越、符号链接逃逸或缺失工作 STEP 的不完整快照。
- 恢复前必须使用 OpenCascade 对工作 STL 与 STEP 分别复核有效性、封闭性和 Solid 数量，并检查两者体积以及模型清单声明的一致性。恢复写回必须使用临时文件、批量原子替换和失败回滚。
- `撤销 / 重做 / 恢复此版本` 只有在 Worker 完成真实模型恢复后才能移动历史索引；失败时当前索引、当前上传模型和最后有效文件均保持不变。
- 恢复成功后，旧拆件、壁厚热力图、网格元素选择、局部修改状态和版本几何对比必须失效，并切回当前原始上传模型视图。
- 参数化 CAD 继续按参数和精确 CAD 快照恢复；上传 STL 精确快照不得伪装成参数化 CAD，也不得进入 STEP 双向布尔差集。Web 模式不提供本机精确上传快照恢复。
- 当前不支持跨项目导入版本快照、从任意外部目录恢复或云端版本同步。

2026-07-21 第 40 阶段回归结果：前端 165/165、Rust 37/37、上传快照恢复 3/3、拆件与补面 27/27、网格元素编辑 4/4；生产构建、类型、格式和差异检查通过。

### 41. 任意上传 STL 框选多顶点、多边和多面批量位移第一版（已实现）

- 原始上传模型视图中的全中文“网格元素编辑”面板支持“点击单选 / 框选多选”，顶点、边、面三种模式互斥。改变元素类型、模型修订、视图来源、制造拆件、壁厚分析、CAD 重建、上传模型恢复或局部修改时，旧框选请求和选择集合必须同步清除。
- 框选按当前摄像机执行屏幕投影：顶点使用源顶点位置，边使用三维端点中点，面使用三维三角面重心。候选投影深度必须位于 `[-1, 1]`；第一版不做遮挡过滤，可能选择矩形内被前景遮挡的元素，界面必须明确说明这是穿透框选。
- 顶点使用六位小数源毫米坐标去重；边使用排序后的两个源端点形成无向键去重；面使用当前修订的 `triangleIndex` 去重。单次最多选择 512 个同类元素，第 513 个不同候选出现时必须按网格遍历顺序截断并显示全中文安全上限提示。
- 选择集合中的每个元素都必须携带当前 `selectionRevision`、`triangleIndex`、`elementIndex` 和三个源坐标组成的 `triangleMm`。模型修订变化后禁止沿用旧集合；Python Worker 必须把 `triangleMm` 与当前工作 STL 对应三角面逐点复核。
- 统一 XYZ 位移必须同时作用于所有选中元素涉及的唯一源坐标，并同步修改 STL 中全部同坐标副本。修改后继续拒绝退化三角面、开放模型、无效 Shape、Solid 数量变化、异常体积以及依赖自动修洞或清理才能成立的结果。
- 视口使用合并的黄色点、粗线和半透明面几何高亮整个集合；用户可随时清除选择。成功后更新模型修订、使旧选择失效、清除旧拆件与壁厚结果，并生成绑定修改前后上传模型精确快照的中文版本记录，从而继续支持真实文件级撤销、重做和版本恢复。
- 第一版仅支持同类元素的统一毫米位移，不承诺自由雕刻、拓扑增删、挤出、焊接、分裂、多 Solid 局部编辑或任意自由曲面重建。

验收结果：浏览器上传 20.00 × 16.00 × 10.00 毫米封闭测试 STL 后，分别框选到 8 个顶点、18 条无向边和 12 个三角面；将 8 个顶点沿 Z 轴统一移动 0.1 毫米后，修订和版本由 2/2 更新为 3/3，OpenCascade 复核体积保持 3200.00 立方毫米，页面重载后控制台无错误。自动化回归覆盖 512 截断与顺序、去重、过期修订、三层协议校验、批量写回、快照恢复和失败回滚。

### 42. 任意上传 STL 选择集合统一旋转与均匀缩放第一版（已实现）

- 用户在原始上传模型视图中选择一个或最多 512 个同类顶点、边或面后，可以在“位移 / 旋转 / 缩放”之间切换，不需要重新建立另一套选择协议。
- 旋转和缩放必须以选择集合涉及的唯一源毫米坐标几何中心为枢轴。旋转只能选择源模型 X、Y、Z 单轴，允许 `-180° ～ 180°` 的非零角度；缩放只能均匀缩放，允许 `0.25 ～ 4 倍` 且不能等于 1。
- 面板必须全中文显示当前元素类型、选择方式、选择数量、轴、角度、比例、枢轴说明、安全范围和失败原因。模型修订改变后，旧选择立即失效，不能静默套用到新网格。
- TypeScript、开发后端、Tauri/Rust 和 Python Worker 都必须拒绝缺失轴、未知轴、越界角度、越界比例、无变化值、未知操作和非有限数值；单顶点绕自身旋转或缩放也必须说明没有实际变化。
- 变换后必须重新验证退化三角面、封闭性、有效 Shape、Solid 数量和体积，并原子更新工作 STL、STEP、清单和结果；失败时不得覆盖最后有效模型。
- 成功版本继续使用上传模型精确快照，桌面端可以真实撤销、重做和恢复；Web 模式没有本机快照时必须明确拒绝，不得仅移动历史索引。
- 第一版不支持非均匀缩放、任意旋转轴、拓扑增删、挤出、焊接、分裂、多 Solid 局部编辑或未受约束自由雕刻。

验收结果：浏览器对任意命名的 20 × 16 × 10 毫米封闭 STL 框选 8 个顶点，绕 Z 轴旋转 10° 后体积保持 3200.00 立方毫米；重新框选并按 0.9 倍均匀缩放后体积为 2332.80 立方毫米。两个操作都生成中文结果、刷新版本并清除旧选择。自动化回归覆盖操作联合类型、三层参数拒绝、几何中心、旋转矩阵、缩放、重复源坐标同步、实体安全检查、精确快照恢复和失败回滚。

### 43. 参数化 CAD 零件转受管网格分支第一版（已实现）

- 用户在精确 CAD 视图中可以选择当前修订清单里的任意一个零件创建网格分支；入口、确认、进度、成功和失败信息全部使用中文，不得把能力限制为主体、上盖、电子外壳或 ESP32 Demo。
- 创建前必须明确告知：原参数化 CAD 分支会保留；新分支可以编辑顶点、边和面；网格修改后不再保证参数化特征可编辑。用户未确认时不得写入受管工作文件。
- 后端必须绑定当前 CAD 修订和零件 ID，只接受生成清单声明的普通 STL 文件名，并拒绝旧修订、未知零件、路径穿越、空文件和超过 50 MB 的模型。
- 创建成功后切换到独立受管网格工作集，复用点击或框选顶点、边、面以及位移、单轴旋转和均匀缩放；原 CAD 版本继续保留并可从版本历史恢复。
- CAD 派生网格必须保存来源修订、零件 ID、零件名称和源 STL 文件。网格元素变换、局部加料/切除、创建精确快照和恢复快照后，来源信息都不能丢失。
- 版本历史必须把参数化 CAD、普通上传模型和 CAD 派生网格分开说明。桌面端只有在真实恢复工作 STL、STEP 和模型清单后才能移动历史索引；Web 模式缺少本机快照时必须明确拒绝恢复。
- 第一版不合并多个 CAD 零件，不声称编辑后的网格仍是可参数化 B-Rep，不支持拓扑增删、面挤出、焊接、分裂、多 Solid 网格编辑或未受约束自由雕刻。

验收结果：浏览器从当前 CAD 修订中列出“主体”和“上盖”，主动选择“主体”后显示参数化能力边界确认，成功创建 62.60 × 32.60 × 12.60 毫米的真实网格分支并进入顶点、边、面编辑面板；版本增加为中文 CAD 网格分支记录，控制台无警告或错误。自动化覆盖任意零件、旧修订和危险文件拒绝、来源持久化、快照恢复以及创建后的分析状态清理。

## 受管网格单三角面法向编辑

第 44 阶段为普通上传 STL 和 CAD 派生网格提供第一版拓扑增删能力：

- **入口与选择**：用户在“网格元素编辑”中选择“法向编辑”后，系统自动切换为面元素和点击单选；请求必须绑定当前上传模型修订，并且只包含一个三角面。
- **操作与尺寸**：支持“向外加料”和“向内压入”，距离为 0.20～100.00 毫米。界面、校验、状态、结果和错误全部使用中文。
- **方向可信度**：系统从源 `triangleMm` 重新计算三角面几何法线，再通过 OpenCascade 对实体内外点进行分类，得到真实外法线；STL 文件自带法线不能作为建模依据。
- **真实几何结果**：以选中三角面构造封闭三角柱工具体；向外执行布尔并集，向内执行布尔切除。结果必须返回真实外法线、工具体积、修改前后体积和体积差。
- **安全门与恢复**：结果必须保持有效、封闭、单一 Solid，并通过体积方向、STEP/STL 导出和 STL 再导入检查。失败时不得覆盖当前工作文件；桌面模式继续创建修改前后精确快照，并保留 CAD 派生来源。
- **明确拒绝**：触碰锐边、开孔边界或复杂分面边界后，如果导出网格形成非流形边，系统必须说明是“法向编辑导出结果”未通过检查，而不能误导用户认为原始 STL 损坏。
- **第一版边界**：不支持框选多面、相邻面区域自动扩展、侧壁斜度、倒角联动、拓扑焊接、任意方向或自由雕刻。

浏览器验收使用非 Demo 绑定的通用长方体 STL：向外加料 2 毫米使体积从 8640.00 增至 9360.00 立方毫米；重新导入后向内压入 2 毫米使体积从 8640.00 减至 7920.00 立方毫米。

## 连续共面区域法向编辑

第 45 阶段把单三角面法向编辑扩展为由一个种子三角面驱动的连续共面区域编辑：

- **种子与连续性**：用户只能点击一个当前修订中的种子三角面。系统沿共享无向边扩展邻接三角面，不能把空间上接近但不共享边的区域合并，也不能跨越锐边、曲面或断开的同平面补片。
- **共面判定**：法线夹角固定公差为 0.5°；全部候选顶点到种子平面的距离必须不超过 `max(0.00001, min(0.02, 模型对角线 × 0.000001))` 毫米。STL 自带法线不参与可信判定。
- **资源上限**：单次最多扩展 20,000 个三角面、区域面积最多 200,000 平方毫米。种子面自身也必须先通过数量和面积上限，超限、非流形共享边或边界异常时必须中文拒绝。
- **带孔区域**：系统必须提取共面区域的所有闭合边界环，区分外环和孔洞环，使用一个带孔平面 Face 构造单一棱柱工具体；不能逐三角面生成重叠工具体，也不能把原有贯穿孔封死。
- **结果与安全门**：成功结果显示受影响三角面数量、区域面积、边界环数量、法线夹角公差、平面距离公差、工具体积和修改前后体积。结果继续要求有效、封闭、单一 Solid、体积方向正确，并通过 STEP/STL 导出和 STL 再导入检查。
- **版本与恢复**：工作文件仍使用临时文件、批量原子替换和失败回滚。普通上传 STL 与 CAD 派生网格都保留 `branchSource`、桌面精确快照、撤销、重做和版本恢复语义。
- **第一版边界**：不扩展曲面连续区域，不穿越锐边，不支持多个种子、多区域框选、侧壁斜度、倒角联动、拓扑焊接、任意方向或自由雕刻。

浏览器验收使用通用 30 × 24 × 12 毫米长方体 STL：点击顶面一个种子三角面后自动扩展 2 个连续共面三角面，区域面积为 720.00 平方毫米；向外加料 2 毫米使体积从 8640.00 增至 10080.00 立方毫米，重新导入原模型后向内压入 2 毫米使体积从 8640.00 减至 7200.00 立方毫米。带孔自动化用例识别 2 个边界环并保持中心贯穿孔。

## 连续共面区域执行前预览

第 46 阶段在正式加料或压入前提供只读预览：视口高亮由单个种子三角面扩展得到的完整连续共面区域，面板显示预计三角面数量、平方毫米面积、边界环数量和两项公差。预览规则、资源上限和非流形拒绝条件与 Worker 对齐，但不替代后端复核；正式请求仍只携带一个种子面，避免把前端推导结果当成可信建模输入。模型修订、元素类型、选择方式、变换操作或选择变化时旧预览立即失效。第一版不预览曲面连续区域、不穿越锐边、不支持多种子和多区域框选。


## 连续共面区域边界线框与重复选面性能

第 47 阶段把执行前预览从“只看黄色面片”扩展为“黄色区域面 + 青绿色闭合边界环”。用户可以直接观察区域外边界和现有孔洞，确认连续共面扩展是否越过了预期范围；所有线框点都以源毫米坐标保存，并在渲染时才转换到当前装配或拆分视图。第一版对外环和孔洞环使用同一种颜色与说明，不承诺数组顺序、环方向或语义名称。

同一模型修订第一次点击种子面时构建全网格三角面索引和共享边拓扑，之后切换种子面复用缓存，不再反复扫描整张大网格。模型修订、重新上传、版本恢复、几何或视图来源变化后缓存必须失效。该优化只改善前端交互延迟，不放宽 20,000 个三角面、200,000 平方毫米等安全上限，也不替代桌面 Worker 对当前工作 STL 的独立复核。

下一阶段将基于种子平面的二维投影和环包含关系区分外环与孔洞，并显示外环/孔洞数量、各环周长与包围尺寸；不得仅依赖 STL 三角面绕序或有符号面积方向判断语义。第一版不扩展到曲面区域、嵌套岛、多区域框选或自由编辑。

## 连续共面区域外环/孔洞语义与尺寸测量

第 48 阶段把边界线框升级为可读的环语义和尺寸反馈。系统将全部闭合环投影到种子平面二维坐标，使用环包含关系计算嵌套深度：未被其他环包含的是外环，被一个外环包含的是孔洞。判断不得依赖 STL 三角面绕序、边界环数组顺序或单纯的有符号面积方向。

预览卡必须分别显示外环和孔洞数量，并为每个环显示真实三维周长以及种子平面中的二维包围宽度和高度。视口继续以黄色显示完整区域，以青绿色显示外环、以珊瑚色显示孔洞环，并提供全中文图例。模型修订、导入、恢复、选择或操作模式变化后，旧分类和测量必须与区域预览一起失效。

前端 `boundaryLoops` 只属于当前修订的执行前反馈，保存源毫米坐标、`outer|hole` 语义、周长、包围尺寸和嵌套深度；兼容的 `boundaryLoopsMm` 继续保留。桌面 Worker 不接收、不读取也不信任前端分类，正式执行时仍从当前工作 STL 独立扩展区域并由 OpenCascade 判断建模 Wire 语义。第一版检测到嵌套岛时安全拒绝，不扩展到曲面区域、多区域框选或自由编辑。

下一阶段将实现边界环点击聚焦与视口尺寸标注第一版：用户可从测量列表选择某个外环或孔洞，视口只强化当前环并显示周长、包围宽度和高度标注，便于在多个孔洞间定位。聚焦状态只用于查看，不改变 Worker 请求、区域扩展或建模语义；第一版不提供拖动尺寸、直接编辑边界或曲面标注。

## 连续共面区域边界环聚焦与视口尺寸标注

第 49 阶段允许用户从测量列表点击任意外环或孔洞。列表必须使用可访问按钮表达聚焦状态，显示“点击定位”或“已聚焦”；再次点击当前环取消聚焦，点击其他环直接切换。视口聚焦时应降低其他环亮度、加粗当前环，并在模型附近显示环名称、周长、平面包围宽度和高度。

聚焦索引只对当前 `MeshPlanarRegionPreview` 有效。新预览、模型修订、导入、恢复、元素种类、选择方式、选择集合、变换操作或视图来源变化后不得复用旧聚焦。无效索引必须安全回落为未聚焦状态。聚焦只用于查看，不进入桌面 Worker 请求、不改变连续共面扩展、Wire 分类或正式布尔结果。

下一阶段将实现边界环视口直接拾取与顺序导航第一版：用户可直接点击视口中的外环或孔洞线框进行聚焦，并可使用“上一个环/下一个环”在多个孔洞间循环定位。第一版只改变查看状态，不支持拖动边界、尺寸驱动修改、曲面环或自由编辑。

### 50. 连续共面区域边界环视口直接拾取与顺序导航第一版（已实现）

- 当当前修订的共面区域包含多个边界环时，面板显示“上一个环”“下一个环”和当前中文状态；导航按预览数组顺序首尾循环，未聚焦时可从首环或末环进入。
- 用户可直接点击视口中的外环或孔洞线框进行聚焦、切换和取消。透明宽线只扩大鼠标命中范围，不改变可见线框颜色、测量结果或打印尺寸。
- 只有真实命中边界环时才阻止事件继续传给模型选面；点击环外区域仍会选择新的种子三角面并刷新连续共面区域预览。
- 列表直接选择、顺序导航和视口拾取共享同一个修订绑定聚焦索引；当前修订、预览、选择或操作失效时聚焦同步清除。
- 聚焦属于只读查看状态，不保存到项目版本、不传给 Codex/Worker、不参与 OpenCascade 建模，也不支持拖动环或尺寸驱动修改。

2026-07-22 验收：带孔区域的 70.40 毫米外环和 34.00 毫米孔洞环可通过列表、顺序按钮和视口线框互相切换；非线框区域选面未被阻断，页面没有 Console 错误。

**下一阶段产品范围：**为当前聚焦环显示宽度、高度尺寸辅助线、端点短线和轮廓外中文数值，优先解决现有中心卡片遮挡孔洞的问题；保持只读，不提供拖拽修改或尺寸约束。

### 51. 连续共面区域尺寸辅助线与标注避让第一版（已实现）

- 当用户聚焦当前连续共面区域中的外环或孔洞时，视口必须显示该环宽度、高度方向的主尺寸线、延伸线和端点短线，并分别提供“宽度 … 毫米”“高度 … 毫米”的全中文只读标签。
- 宽度标签、高度标签和环摘要必须位于三个不同的轮廓外方向；摘要只显示环名称和周长，避免重复尺寸信息并减少对孔洞中心和模型表面的遮挡。
- 尺寸线偏移按环平面包围尺寸自适应，小型孔洞仍保留至少 1.5 毫米间距；辅助线与标签只能引用当前模型修订的预览测量基底。
- 切换聚焦环时必须替换整组尺寸标注，再次点击当前环、清除选择、切换操作或模型修订变化时必须全部清除；不能影响边界线框直接拾取和非线框区域选面。
- 本阶段不允许尺寸驱动编辑、拖动标签、拖动辅助线、曲面测量、嵌套岛或自由雕刻。

2026-07-22 验收：25 个前端测试文件 190/190，生产构建和差异检查通过。浏览器验证外环 24.60 × 10.60 毫米、孔洞 11.00 × 6.00 毫米的独立中文标签与轮廓外摘要均随聚焦正确更新；取消孔洞聚焦后标签全部消失，非线框底面仍生成 138 个三角面的新预览，Console 无错误。

**下一阶段：**连续共面区域尺寸标注视口自适应翻转第一版。根据相机投影和视口安全区在候选外侧方向间选择标签位置，优先避免顶部工具栏、右侧编辑面板与视口边缘；保持只读语义，不进入 Worker 或版本数据。


### 52. 连续共面区域尺寸标注视口自适应翻转第一版（已实现）

- 宽度、高度和摘要标签必须从稳定源毫米测量基底生成有限候选，不得因相机角度改变真实毫米数值、环语义或边界顺序。
- 系统必须识别顶部工具栏、右侧网格编辑面板和画布边缘安全区；候选选择优先避免裁切，其次减少三个标签的屏幕矩形重叠，同分时保持原候选以降低抖动。
- 当所有三维候选都靠近遮挡区时，最终中文标签必须在屏幕空间夹紧到安全区内；相机旋转、缩放、平移、视口大小或聚焦环改变时必须自动重新计算。
- 再次点击当前环、清除选择或预览失效后，尺寸线和所有标签必须立即清除；该状态不得写入 Worker 请求、项目版本或精确快照。
- 本阶段不允许手工拖动标签、尺寸驱动修改、曲面测量、嵌套岛或自由雕刻。

2026-07-22 验收：前端 25 个测试文件 192/192，生产构建和差异检查通过。浏览器验证布局候选会随相机与外环/孔洞切换，标签保持在右侧面板 606 像素起点之外的安全区内；外环、孔洞的宽高和周长数据不变，取消聚焦后全部标签消失，Console 无错误。

**下一阶段：**连续共面区域法向加料/压入工具体视口预演第一版。执行前应以当前毫米距离、操作方向和外环/孔洞语义显示半透明工具体，使用户确认实际作用范围；只做预演，不替代桌面 Worker 的独立扩展和实体校验。

### 53. 连续共面区域法向加料/压入工具体视口预演第一版（已实现）

- 用户选中当前修订的连续共面区域后，只要作用距离位于 0.20 至 100.00 毫米，视口必须实时显示带孔半透明棱柱工具体、方向线和“向外加料预演 / 向内压入预演”中文毫米标签。
- 工具体轮廓必须按外环和孔洞语义读取，不依赖边界环数组顺序；原有贯穿孔在预演中必须保持为空，不能被半透明实体封死。
- 操作方向和距离是非持久瞬时输入。方向、距离、种子面、区域预览、模型修订或视图来源变化时预演同步更新；距离无效、选择清除或上下文不再属于当前上传模型时立即隐藏。
- 前端可根据闭合网格有符号体积修正绕序，用于判断预演方向，但不得把该推断描述为 Worker 的真实实体分类结果；正式执行仍由 OpenCascade 独立确认外法线并检查封闭性、有效性、单 Solid、体积方向与失败回滚。
- 工具体、方向线和标签不得接收鼠标事件，不得影响边界环拾取、模型选面和尺寸标签；预演不得写入项目版本、精确快照或 Worker 请求。

2026-07-22 产品验收：前端 193/193、生产构建和差异检查通过。浏览器验证默认向外 2.00 毫米、切换向内 6.00 毫米、0.1 毫米无效清除、带孔轮廓及孔洞聚焦/尺寸标注共存，页面无 Console 错误。

### 54. 连续共面区域工具体预演轮廓强化与标签视口避让第一版（已实现）

- 用户必须能同时看清工具体起始端和末端的外环及全部孔洞轮廓；末端轮廓应比起始端更醒目，孔洞应使用同一加料/压入语义色的较浅层级。
- 法向方向线末端必须显示不可拖动的十字标记，帮助用户确认作用方向和终点；标记大小必须有毫米下限和上限，避免在大模型上过小或在小模型上遮挡轮廓。
- 所有新增线条和标签必须不接收鼠标事件，不得阻止边界环拾取、模型选面、尺寸标注或相机操作；中文预演标签必须随当前相机投影保持在顶部工具栏、右侧编辑面板和画布边缘安全区之外。
- 无效距离、退化或少于三个点的环、非有限坐标和零长度基底必须安全清除预演，不得生成错误显示或写入模型。
- 本阶段仍只显示工具体，不提供近似布尔最终实体、工具体拖动、侧壁斜度、倒角联动、曲面区域、多种子区域或自由雕刻。

2026-07-22 第 54 阶段验收：前端 25 个测试文件 193/193、生产构建和 `git diff --check` 通过。浏览器带孔端壁为 8 个三角面、194.76 平方毫米、外环 1 个、孔洞 1 个；向外加料 2.00 毫米和向内压入 6.00 毫米均能查看起止端与孔洞轮廓、方向末端十字，多角度旋转时标签未进入固定工具栏、右侧面板或裁切区，Console 无错误。仅保留既有 Vite 大分块警告。

### 55. 连续共面区域工具体起止端侧边连接线与遮挡层级强化第一版（已实现）

- 工具体的每个外环和任意数量孔洞必须按对应唯一顶点显示起止端侧边连接线，闭合环的重复首尾点不得生成重复侧边。
- 外环连接线必须使用主要加料/压入语义层级，孔洞连接线必须使用更浅颜色与透明度，帮助用户区分实体外侧壁和贯穿孔侧壁。
- 连接线必须在半透明工具体、起止端轮廓和方向十字同时显示时保持可辨识，并在相机旋转、缩放或操作方向变化后实时更新。
- 所有连接线必须不可拾取且不得参与模型选择、边界环聚焦、尺寸标注、Worker 请求、布尔建模、版本记录或快照恢复。

2026-07-22 第 55 阶段验收：针对性测试 20/20、前端 193/193、生产构建和差异检查通过。浏览器以 8 个三角面、194.76 平方毫米、外环 1 个、孔洞 1 个的带孔端壁验证向外 2.00 毫米及向内 6.00 毫米；外环与孔洞侧边层级清晰，多角度旋转后预演仍存在、标签在安全区，Console 无错误。仅保留既有 Vite 大分块警告。

### 56. 连续共面区域工具体面积与体积只读摘要第一版（已实现）

- 用户选择连续共面区域并输入有效距离后，面板显示外环面积、孔洞总面积、净作用面积和“工具体估算”；视口标签同步显示操作方向、距离、净作用面积和估算体积。
- 净作用面积固定为唯一外环面积减去任意数量孔洞面积；顺时针或逆时针绕序不得改变结果。理论体积固定为净作用面积乘正作用距离，加料与压入只改变方向语义，不改变同一面积和距离下的绝对估算值。
- 非有限点、少点环、零面积环、孔洞面积不小于外环或无效距离必须清除摘要，不保留过期数值。模型修订、区域、方向、距离和视图来源变化时同步重算或清除。
- 文案必须使用“工具体估算”，不得把它称为模型净体积变化、打印耗材、切片时间或支撑用量；最终几何与体积仍以 OpenCascade Worker 执行结果为准。

2026-07-22 第 56 阶段验收：针对性测试 20/20、前端 193/193、生产构建和差异检查通过。自动测试覆盖单孔、多孔、反向绕序与全部退化输入；浏览器验证 50.00 平方毫米区域在 2.00 毫米时估算 100.00 立方毫米、6.00 毫米时估算 300.00 立方毫米，方向切换和距离输入实时同步，0.10 毫米立即清除摘要并显示中文错误，安全区正常且 Console 无错误。

### 57. 连续共面区域执行结果体积对照卡第一版（已实现）

- 成功执行连续共面加料或压入后，网格编辑面板必须显示 OpenCascade 实际工具体积、模型体积变化绝对值和相对工具体的实际作用比例，统一使用立方毫米和百分比。
- 对照卡只属于当前上传模型修订；非 `extrude-face` 结果、过期修订、缺失加料/压入模式、缺失结果或非法数值必须隐藏旧卡片。
- 工具体积必须为正有限数，模型体积变化必须有限。百分比只允许在几何上合理的 0% 至 100% 范围内显示，轻微浮点超出可夹紧为 100%，明显异常时不得继续展示误导结果。
- 向外加料必须说明工具体可能与已有实体重叠，向内压入必须说明工具体可能超出实体厚度并被裁剪；不得使用“材料利用率”“打印耗材”或“精确材料用量”等表述。
- 第一版只查看单次最新成功结果，不提供累计历史、切片时间、支撑分析或耗材预测，也不新增 Worker 字段、持久状态、项目版本或快照迁移。

2026-07-22 第 57 阶段验收：针对性测试 24/24、前端 197/197、生产构建和差异检查通过。浏览器在通用 50.00 平方毫米共面区域执行向外 2.00 毫米加料，实际工具体积为 103.58 立方毫米，模型体积变化为 100.00 立方毫米，实际作用比例为 96.54%；卡片保留全中文解释，Console 无错误。仅保留既有 Vite 大分块警告。

### 58. 连续共面区域平面估算与工具体构造偏差对照第一版（已实现）

- 当前修订的成功共面区域结果必须显示执行前平面估算、OpenCascade 实际工具体积、模型体积变化和实际作用比例，帮助用户理解从二维区域到真实布尔结果的三段几何链路。
- 工具体构造偏差必须显示“与平面估算一致”“高于”或“低于”、绝对立方毫米差值和带正负号百分比；数值统一保留两位小数。
- 微小浮点差异按一致处理；绝对偏差超过 50%、面积或距离缺失、非正、非有限、结果过期时不得显示偏差解释，但不得连带隐藏仍然合法的执行结果体积卡。
- 中文说明必须使用审慎的几何语义，明确差异可能来自 Wire 重建和几何容差；不得宣称材料损耗、耗材误差、打印收缩、支撑量或切片补偿。
- 第一版不新增 Worker、Store、项目版本或快照字段，不提供 Wire 差集可视化、截面误差定位或材料预测。

2026-07-22 第 58 阶段验收：针对性测试 27/27、前端 25 个测试文件 200/200、生产构建和差异检查通过。浏览器在通用 655.20 平方毫米共面区域执行向外 2.00 毫米加料，显示平面估算 1310.40 立方毫米、实际工具体积 1357.38 立方毫米、高于 46.98 立方毫米（+3.58%）、模型体积变化 1310.40 立方毫米和实际作用比例 96.54%；两列卡片无拥挤，Console 无错误。仅保留既有 Vite 大分块警告。

### 59. 连续共面区域三段体积链路方向一致性与异常提示第一版（已实现）

- 成功结果必须按“平面轮廓 / 工具体 / 布尔作用”顺序显示三段只读状态；前两段复用既有平面估算和工具体偏差，第三段复核加料增量或压入减量是否与操作模式一致。
- 加料却减少体积、压入却增加体积时必须显示红色“几何方向安全警告”，写明声明操作、实际增减量和“请勿将其视为正常结果”；不得静默显示为方向一致。
- 工具体积百万分之一以内的有符号变化显示为“体积近似未变化”，不误判为加料或压入反向；无效或过期结果不显示状态。
- 状态只帮助识别单次布尔结果，不自动撤销或修复模型，不新增 Worker、Store、版本或快照字段，不扩展为材料、耗材、收缩或切片质量判断。

2026-07-22 第 59 阶段验收：针对性测试 31/31、前端 25 个测试文件 204/204、生产构建和差异检查通过。浏览器真实加料结果显示“平面估算已计算 / 高于平面估算 / 加料增量一致”，原 1310.40 → 1357.38 → 1310.40 立方毫米链路、+3.58% 偏差和 96.54% 作用比例保持完整，Console 无错误。仅保留既有 Vite 大分块警告。

### 60. 连续共面区域执行结果中文诊断摘要与复制第一版（已实现）

- 当前修订的成功共面加料或压入结果必须提供全中文多行诊断摘要，至少包含操作模式、区域面积、作用距离、平面估算、实际工具体积、工具体构造偏差、有符号模型体积变化、实际作用比例和方向状态。
- 面积、距离或偏差不可用时必须显示“暂不可用”；过期修订、无效模式、非有限结果或明显异常作用比例不得生成摘要，也不得包含绝对路径、账号、密钥或源文件信息。
- “复制几何诊断”只在用户主动点击后访问系统剪贴板。成功和失败都必须提供全中文反馈；复制文本不得自动进入 Store、版本、快照、Worker、Codex 或网络。
- 第一版只处理当前单次执行结果，不保存诊断历史，不判断材料、耗材、支撑、收缩或切片质量。

2026-07-22 第 60 阶段验收：针对性测试 36/36、前端 25 个测试文件 209/209、生产构建和差异检查通过。浏览器复制结果包含 655.20 平方毫米、2.00 毫米、1310.40 / 1357.38 / +1310.40 立方毫米、+46.98 立方毫米（+3.58%）、96.54% 和“加料增量一致”，并显示“已复制到系统剪贴板，不会自动发送或保存”；全新验收页 Console 无错误。仅保留既有 Vite 大分块警告。

### 61. 连续共面区域诊断摘要填入 Codex 指令草稿第一版（已实现）

- 当前修订存在有效共面区域诊断时，结果卡必须显示“交给 Codex 分析”；入口失效时必须禁用，不得复用过期结果。
- 用户点击后，系统只在当前页面的本地指令草稿中追加稳定中文请求，内容包含完整诊断和“请分析几何链路是否合理并提出下一步修改建议”。
- 草稿中的既有用户文字必须原样保留；同一完整诊断请求重复点击不得重复堆叠。指令框必须支持多行查看、补充和修改。
- 点击入口不得自动提交表单、执行建模、调用 Codex/API、上传网络或写入 Store、项目版本、快照及 Worker。用户仍需自行检查并点击“执行建模指令”。

2026-07-22 第 61 阶段验收：针对性测试 40/40、前端 25 个测试文件 213/213、生产构建和差异检查通过。浏览器真实加料结果在保留用户“确认这个区域不会碰到内部元件”要求后，追加完整中文诊断和分析要求；重复点击后诊断标题仍为 1 处，消息数量不变且未自动执行，Console 无错误。仅保留既有 Vite 大分块警告。

### 62. 诊断草稿块识别与安全移除（已实现）

- 系统只把当前页面中由“交给 Codex 分析”实际注入过的完整请求登记为可管理诊断块；用户自行粘贴的相似文本不自动获得系统来源身份。
- 唯一完整块必须显示“已识别 1 个完整的本页诊断块”和“移除诊断块”按钮。移除后保留块前后所有用户文字、归一化相邻空行，并明确提示剩余指令尚未执行。
- 用户修改了块内任意文字、只留下部分结构标记、粘贴出重复块或同时存在冲突块时，系统必须进入保护状态，显示中文原因并禁用移除，不能按标题、前缀或近似内容猜测删除范围。
- 识别与移除仅修改当前页面草稿，不调用 Codex/API、Worker 或网络，不写入 Store、项目版本与快照，也不自动执行剩余文字。
- 网格编辑面板与指令面板同时出现时，两者必须保持按钮可见可点击，不允许编辑面板遮挡诊断草稿操作。

2026-07-22 第 62 阶段验收：针对性测试 45/45、前端 25 个测试文件 218/218、生产构建和差异检查通过。浏览器真实加料诊断可被识别并单独移除，前后用户要求完整保留，消息数量不变；再次注入后修改“实际作用比例”字段，界面显示保护提示且移除按钮禁用。面板避让后真实鼠标可点击，干净页面 Console 无错误。仅保留既有 Vite 大分块警告。

### 63. 诊断草稿块只读摘要与精确定位（已实现）

- 唯一完整诊断块状态卡显示总行数、操作模式和方向状态；缺少可识别业务字段时使用“未识别”，不得补造数值或语义。
- “定位诊断块”只在唯一完整本页块存在时可用。点击后文本框必须获得焦点，完整块必须被精确选中并滚动到可见位置，前后用户文字不能进入选择范围。
- 编辑、残缺、重复、冲突或未登记文本不提供定位范围，定位和移除按钮同时禁用。
- 定位不得修改草稿、提交表单、调用 Codex/API、Worker 或网络，也不得写入 Store、项目版本与快照。

2026-07-22 第 63 阶段验收：针对性测试 49/49、前端 25 个测试文件 222/222、生产构建和差异检查通过。浏览器在 12 行用户文字之后定位真实 13 行诊断块，精确选择字符 148 至 374，文本框滚动值为 174 且保持 3 条消息；手工修改方向字段后定位与移除同时禁用。Console 无错误，仅保留既有 Vite 大分块警告。

### 64. 唯一旧诊断块安全替换为最新诊断（已实现）

- 当前最新诊断与草稿中唯一完整本页块不同时，结果卡必须显示“替换为最新诊断”；两者完全相同时显示“当前诊断已在草稿”，不得重复追加。
- 替换必须原样保留块前后所有用户文字、空格和换行，只更新诊断请求块本身，并同步把页面级来源登记从旧块切换为最新块。
- 旧块被编辑、残缺、重复、冲突或不属于本页登记时，入口必须显示“诊断草稿需手工处理”并禁用；系统不得猜测范围、覆盖用户修改或追加第二个块。
- 替换不提交表单、不执行建模、不调用 Codex/API、Worker 或网络，不写入 Store、项目版本和快照；用户仍需检查草稿并主动执行。

2026-07-22 第 64 阶段验收：针对性测试 53/53、前端 25 个测试文件 226/226、生产构建和差异检查通过。浏览器把真实 2.00 毫米加料诊断替换为 3.00 毫米最新诊断，前后用户要求和单一标题均保留，消息数量不变；手工修改方向字段后再产生 1.00 毫米最新结果，界面拒绝覆盖且按钮禁用。Console 无错误，仅保留既有 Vite 大分块警告。

### 65. 旧诊断与最新诊断字段差异摘要（已实现）

- 仅当当前草稿包含唯一完整本页旧诊断、最新诊断不同且安全动作允许替换时，结果区域显示“最新诊断将更新 N 项”。
- 差异字段固定为操作模式、区域面积、作用距离、执行前平面估算、实际工具体积、工具体构造偏差、模型体积变化、实际作用比例和方向状态，并按此业务顺序显示“旧值 → 新值”。
- 两侧相同的字段自动过滤；任一侧缺失时不得补造。相同请求不显示差异，编辑、残缺、重复、冲突、未登记或空最新摘要必须隐藏卡片并继续沿用既有安全拒绝。
- 差异卡只读且即时派生，替换前不修改草稿或系统块登记，不保存第二份完整正文，不写入 Store、项目版本与快照，不调用 Codex/API、Worker 或网络，也不自动执行。

2026-07-22 第 65 阶段验收：针对性测试 56/56、前端 25 个测试文件 229/229、生产构建和差异检查通过。浏览器真实比较 2.00→3.00 毫米加料诊断，显示固定顺序的 6 个变化字段；替换前草稿与前后用户文字不变、消息数量保持 3 条，替换后差异卡消失。手工修改方向字段后进入安全拒绝且不显示差异。Console 无错误，仅保留既有 Vite 大分块警告。

### 66. 诊断字段差异按需复制（已实现）

- “复制差异摘要”仅在差异卡可见且当前诊断仍满足安全替换条件时提供；差异消失、重新计算或草稿变为编辑、残缺、重复、冲突状态时，入口和旧成功反馈必须立即失效。
- 复制文本固定包含“【共面区域诊断字段差异】”、变化数量和按现有业务顺序排列的逐项“字段：旧值 → 新值”；不得带入完整旧诊断、最新诊断、分析请求正文或用户草稿内容。
- 成功后显示“差异摘要已复制，不会自动发送或执行”，失败显示“复制差异摘要失败，请检查剪贴板权限”；失败不得中断当前建模页面。
- 复制不得修改草稿、系统块登记、Store、项目版本或快照，不调用 Codex/API、Worker 或网络，不自动替换、发送或执行。

2026-07-22 第 66 阶段验收：针对性测试 59/59、前端 25 个测试文件 232/232、生产构建和差异检查通过。浏览器真实比较 2.00→3.00 毫米加料诊断并复制固定顺序的 6 个变化字段；捕获到的剪贴板内容不含完整请求标题或分析提示。复制前后旧草稿、前后用户文字和 3 条消息均不变，替换后差异卡与复制反馈消失。干净验收页 Console 无错误，仅保留既有 Vite 大分块警告。

**下一阶段产品范围：**允许用户在复制前按需展开只读内容预览；预览必须与实际复制文本完全同源，不保存差异历史、不修改草稿，也不自动发送或执行。

### 67. 诊断差异复制内容预览（已实现）

- 仅在当前诊断仍满足安全替换且存在有限差异摘要时提供“预览复制内容 / 收起复制内容”；入口默认收起，不增加确认弹窗。
- 展开正文必须与“复制差异摘要”实际写入的字符串严格同源，保留标题、变化数量和逐项换行，不允许界面另行拼接或缓存第二份正文。
- 预览为只读、可选择并可键盘聚焦的文本区域，显示“以下内容仅供检查，不会自动发送或执行。”；不得修改草稿、系统块登记、Store、版本或快照。
- 替换诊断、产生不同摘要或进入编辑、残缺、重复、冲突等不安全状态后，预览和复制反馈必须清除；不同摘要重新出现时默认收起。

2026-07-22 第 67 阶段验收：针对性测试 62/62、前端 25 个测试文件 235/235、生产构建和差异检查通过。浏览器真实生成 6 项诊断差异，确认预览默认收起；展开后正文与剪贴板捕获文本严格相同，保留换行、可选择并可键盘聚焦。收起、替换和不同摘要重新出现均按预期清理或重置局部状态，草稿前后用户文字未被修改；干净验收页 Console 无错误，仅保留既有 Vite 大分块警告。

### 68. 诊断差异预览一键全选（已实现）

- “全选预览内容”仅在差异复制预览展开时出现，点击后选中当前预览正文的完整多行文本，不调用 Clipboard API，也不自动复制。
- 成功后显示“已全选当前预览内容，不会自动复制或执行。”；Selection/Range 不可用、空引用或异常时显示“无法全选预览内容，请手动拖动选择。”，页面继续可用。
- 收起预览会隐藏入口并清除选择反馈；摘要身份变化、替换或安全状态失效后，旧入口和反馈随 keyed 组件卸载或重挂载。
- 该交互只改变浏览器临时 Selection 范围，不修改草稿、系统块登记、Store、项目版本、快照、模型或执行结果。

2026-07-22 第 68 阶段验收：针对性测试 65/65、前端 25 个测试文件 238/238、生产构建和差异检查通过。真实浏览器一键选中的 6 项差异文本与预览正文严格相同，剪贴板调用为 0、草稿不变；失败、收起、重新展开和替换清理均符合预期，干净页面 Console 无错误。

### 69. 诊断差异预览内容统计（已实现）

- 仅在差异复制预览展开时显示“共 N 行 · N 个字符”，统计与当前只读正文同时出现和消失。
- 行数兼容 CRLF、LF 和 CR；字符数按 Unicode 码点计算，换行仍计入字符总数，不按 UTF-16 代码单元重复计算 Emoji。
- 统计直接消费当前 `diagnosticDifferenceSummary` 对应的预览正文，不截断、不重写、不保存第二份正文，也不影响浏览器 Selection。
- 摘要变化、替换或不安全状态会卸载旧统计；重新展开只显示当前摘要的统计，不修改草稿、系统块登记、Store、版本、快照或模型。

2026-07-22 第 69 阶段验收：针对性测试 67/67、前端 25 个测试文件 240/240、生产构建和差异检查通过。真实浏览器中 6 项差异正文独立计算为 8 行、230 个 Unicode 字符，界面严格显示“共 8 行 · 230 个字符”；全文选择、收起、重新展开、替换清理和用户文字保留均符合预期，干净页面 Console 无错误。

### 70. 任意封闭模型六向打印方向评估（已实现）

- 用户可在当前精确 CAD 零件或任意上传 STL 的参数面板中主动执行六向打印方向分析，不需要模型具有固定业务类型或名称。
- 每个方向展示旋转后毫米尺寸、是否适配 P1S 256 × 256 × 256 毫米成型空间、近似底面接触面积、45° 悬垂支撑面积与比例，并以低、中、高或不可用标识风险。
- 系统只在可放入成型空间的候选中，按固定权重综合悬垂比例、打印高度和底面接触面积给出可解释推荐；若六向均不可用，明确建议拆件或缩放，不给出伪推荐。
- 分析结果不自动旋转模型、不改变装配、不写入版本或快照；来源、零件或修订变化后旧结果立即消失，快速参数预览不能冒充精确网格参与分析。
- 页面明确说明结果是轴向几何估算，不等同于 Bambu Studio 或其他切片器的真实支撑树、耗材与时间结果。

2026-07-22 第 70 阶段验收：针对性测试 6/6、前端 26 个测试文件 246/246、生产构建和差异检查通过。真实浏览器完成 CAD 主体、上盖、快速预览安全降级和任意上传 STL 路径验收；上传模型以原文件名进入分析，展示六个方向及 1,608 个有效三角面，推荐 Z 正方向朝上。来源和零件切换会清除旧结果，验收页 Console 无错误或警告。

### 71. 六向打印方向建议一键应用（已实现）

- 用户完成六向分析后，可在推荐仍属于当前对象和当前修订时主动打开中文确认卡；确认卡展示对象名称、目标方向和应用后尺寸，不使用原生系统确认框。
- 确认后只把当前 CAD 零件或上传 STL 对象旋转到推荐的绝对轴向角度，不累计旋转，不改变位置、缩放、颜色、其他零件和几何文件。
- 应用会生成中文对象展示版本，并支持完整撤销和重做；取消不产生版本，当前旋转已与推荐等价时只显示完成状态，不允许重复应用。
- 应用成功后旧分析立即清除并提示重新分析；来源、对象、修订或均匀缩放变化时旧结果和确认入口同样失效。
- 分析尺寸与风险已按当前均匀缩放计算；非有限、零或负缩放会以中文错误拒绝分析，不能用未缩放尺寸误判 P1S 成型空间。

2026-07-22 第 71 阶段验收：针对性测试 18/18、前端 26 个测试文件 253/253、生产构建和差异检查通过。真实浏览器验证 CAD 取消、确认、撤销、重做和重复应用保护，1.5 倍缩放后的尺寸与面积换算，以及任意上传 STL 的真实对象写入；验收页 Console 为 0 个错误、0 个警告。

### 72. 推荐打印方向应用后的自动落床预览与确认（已实现）

- 推荐方向应用并重新分析后，系统显示“自动落床预览”，明确对象名称、当前最低点、平台高度 0 毫米、目标 Y 位置和移动方向；分析本身保持只读。
- CAD 零件与任意上传 STL 都按各自真实视口层级计算最低点：CAD 使用对象内归一化，上传 STL 使用对象外世界归一化；两者共同遵循原始 STL Z-up 到视口 Y-up 的坐标映射。
- 只有用户点击“将对象落到平台”并再次确认后，才更新当前对象的垂直位置；水平位置、旋转、缩放、颜色、其他对象和几何文件保持不变。
- 落床生成全中文对象展示版本，可撤销和重做；取消不创建版本，当前对象已落床时显示完成状态并隐藏重复应用入口。
- 预览绑定当前对象、来源修订、位置、旋转、缩放、基础位置与归一化空间。上下文失效、目标位置越界或 Store 状态与分析来源不一致时安全拒绝；成功提示会在来源身份刷新后继续保留。

2026-07-22 第 72 阶段验收：针对性测试 24/24、前端 26 个测试文件 259/259、生产构建和差异检查通过。真实浏览器覆盖 CAD Y=10 毫米与任意上传 STL Y=7.5 毫米的取消、确认、撤销、重做和重复保护；两类模型确认后最低点均为 0.00 毫米，Console 为 0 个错误、0 个警告。

**下一阶段产品范围：**增加已落床对象的平台边界适配与只读居中预览。对照 P1S 256 × 256 毫米平台显示对象在视口 X/Z 平面的四边余量、越界方向与居中所需水平位移；第一版只读，不自动移动、不排列多对象、不创建版本，也不修改装配关系或几何文件。

# FormAI 实施计划

更新日期：2026-07-23

## 状态定义

- **已实现**：已经存在可运行代码，并通过自动化或人工检查。
- **本阶段已完成**：本轮要求已写入真实数据流或 OpenCascade 实体，并完成验证。
- **已规划**：界面和参数方案已经存在，但尚未写入精确实体。
- **未实现**：尚无可用代码，不能在界面中宣称已经完成。

## 已实现

- macOS Tauri 桌面应用、全中文菜单和界面。
- React Three Fiber 三维视口、装配视图、拆分视图、对象选择和基础参数面板。
- CadQuery/OpenCascade 通用模型 Worker、动态零件清单，以及当前电子元件保护壳模板的内部掏空、圆角、倒角、接口开孔和独立上盖。
- 本机 Codex 登录复用、自然语言参数修改、修改前快照、失败回滚和版本历史。
- PNG/JPEG/WebP 文件输入与拍照入口，并通过 `codex exec --image` 发送真实图片。
- STL、STEP、标准几何 3MF 导出，以及 P1S 成型尺寸检查。
- 拆件策略、连接结构、卡扣、螺丝和螺纹的制造方案规划器。
- 通用协议使用 `model` + `parts[]` + `assemblyFile`；拆件来源使用 `sourceKind` + `sourcePartId`，支持当前 CAD 零件和用户上传 STL，不依赖任何具体开发板或模型类别名称。
- 通用封闭 STEP/STL 全局壁厚采样、风险分级和三维热力图；结果协议明确标记为“表面法向射线采样估算”。
- 参数化精确 CAD 的稳定面选择网格、点击单面、框选当前视角可见面、局部截图和 Codex 结构化上下文。
- 点击单个稳定 CAD 平面后的局部轮廓、整面拉伸/偏移，以及点击任意稳定面所属种子稳定 CAD 边后的单边圆角/倒角和唯一平面边界整圈圆角/倒角；包含桌面/浏览器本机 API、Codex 受限计划、真实 UV 与外法线复核、清单修订校验、参数化安全重放、结果原子替换和选择网格刷新。

ESP32-S3 N16R8 只作为 `modeling/examples/esp32-s3-n16r8.json` 示例项目保留。

## 本阶段已完成

### 1. 照片输入与双点尺寸标定

- 在原始照片上选择两个标定点并显示标定线。
- 读取图片原始像素尺寸，计算标定线像素长度和毫米/像素比例。
- 未选满两个点时禁止执行“精确标定识别”。
- 将标定点、像素长度、真实长度和毫米/像素比例传给 Codex。

验收标准：识别请求中包含完整标定数据，界面明确显示像素距离和比例，而不是只有一个无对应位置的尺寸输入框。

### 2. 元件接口位置自动识别

- 识别 USB-C、按钮、LED、排针、电源接口及未知接口。
- 返回接口中心位置、估算宽高、置信度和是否需要外壳开孔。
- 在照片上叠加中文接口标记。
- 将参考照片元数据和识别接口持久化到项目 Store。
- 把经过人工复核、需要开孔且能够确定正交接口面的识别结果转换为通用精确开孔记录，不绑定 USB-C 或固定模型类别。
- 视觉结果包含可选二维开孔轮廓；不确定时使用圆角矩形并给出中文复核警告。

验收标准：应用识别结果后，接口类型、所在面、轮廓、宽高和二维标定位置进入通用开孔协议，并触发精确 CAD 重建；透视图不能单独决定精确接口面。

### 3. OpenCascade 平面拆件与自动补面

- 对封闭 CAD 实体使用切割平面两侧的大包围盒执行 Boolean Common。
- OpenCascade 布尔结果必须形成两个封闭 Solid，切割面由内核自动补面。
- 检查两部分有效性、封闭性、正体积和切割前后体积守恒。
- 提供 STEP/STL 验证输出和自动化测试脚本。
- 已接入 Tauri/Rust 命令与中文“拆件与连接”界面，可从当前模型清单动态选择任意可拆零件，并设置 X/Y/Z 切割轴和毫米坐标。
- 精确拆件结果会替换视口中的原零件；“装配视图”合并显示，“拆分视图”沿切割方向展开。
- 带连接结构的两侧 STL/STEP 已加入导出菜单；桌面发布资源包含拆件 Worker 和精确连接特征模块。

验收标准：示例实体切割后两部分均为有效封闭实体，体积和误差在容差内；界面返回补面数、封闭性和体积守恒结果，并可查看和导出真实拆件。

### 4. 用户自定义 STL 通用拆件第一阶段

- 支持上传任意文件名、任意模型类别的二进制或 ASCII STL，不绑定电子外壳、开发板、主体、上盖或手办等固定语义。
- Web 与 Tauri 使用统一上传接口，限制 STL 格式、空文件和 50 MB 大小，并以安全内部文件名保存；界面和元数据保留原始文件名。
- 使用 OpenCascade 导入三角面、缝合 Shell，并将封闭流形网格转换为分面 Solid；开放网格或无法形成有效实体时返回中文错误，不输出伪造的可打印结果。
- 支持沿任意 X/Y/Z 轴和毫米坐标拆分；由 OpenCascade Boolean 自动生成两侧切割补面。
- 自动检查实体数、封闭性、正体积、补面数量和拆件前后体积守恒。
- 输出拆件两侧的 STEP/STL；上传原模型和拆件结果均可在视口中查看，并支持装配视图与拆分视图。
- 场景树、项目标题、状态栏和导出菜单根据用户上传模型动态生成，不出现固定模型部件命名。
- 上传模型激活时，右侧参数区切换为通用 STL 的文件名、尺寸、三角面、实体数、体积、封闭性和拆件校验信息，不再显示示例电子外壳的固定参数。
- 上传模型的候选连接位按真实包围盒和当前切割平面坐标轴计算，不使用默认开发板或壳体尺寸；生成时由精确内核使用真实实体相交体积重新选择能够同时附着到两侧拆件的位置。

验收标准：至少覆盖一个非项目内置的封闭 STL、一个二进制或 ASCII STL 以及一个开放 STL；有效模型可完成上传、预览、拆件、自动补面和导出，开放模型必须被明确拒绝。

### 5. 高级 STL/手办网格修复与自动拆件

**第一批已完成：**

- 通用读取二进制和 ASCII STL，按毫米公差焊接重复顶点并建立三角形边拓扑。
- 检测并清理退化三角形和重复三角形；统计边界边、非流形边和连通网格数量。
- 收集不分叉的开放边界闭环，校验法线方向、共面性、投影自交和嵌套轮廓。
- 对独立、简单、近似共面的孔洞执行二维投影和耳切三角化，生成修复后 STL 工作副本。
- 修复后再次检查开放边和非流形边，再交给 OpenCascade 缝合为封闭 Solid；原始 STL 始终保留。
- 上传清单和中文界面展示输入/输出三角面、清理数量、修洞数量、补面三角形及修复前后开放边。
- 修复后的工作 STL 用于视口和后续拆件，并可与原始上传 STL 分别导出。

**后续仍需实现：**

- 修复非流形边、自相交、复杂非共面破面、分叉/嵌套边界和细小孤岛。
- 对超大三角网格进行分块、简化、缓存和后台任务优化。
- 自动评估打印方向、薄弱区域、支撑成本与候选拆件位置。
- 根据模型局部形状自动建议拆件面和连接结构位置，而不是依赖固定模板。

### 6. 连接与紧固结构写入精确实体

**本批已完成：**

- 圆柱定位销、D 形防转销、燕尾榫、球头/球窝和双侧磁铁安装孔已作为 OpenCascade 加料/减料特征写入最终拆件。
- M2、M2.5、M3 螺丝柱、底孔、通孔和圆柱沉孔已写入最终拆件，并输出带结构的 STEP/STL。
- PLA/PETG 可拆悬臂卡扣已写入精确实体：负方向拆件生成 6.0 毫米宽、1.6 毫米臂厚的悬臂和单侧导入斜面扣钩，正方向拆件生成带间隙的臂槽、扣合窗口及反向弯曲释放空间。
- 卡扣和配合槽使用切割轴局部 U/V 坐标生成，已覆盖 X/Y/Z 三个切割轴，不绑定电子外壳、手办或其他固定模型类别。
- M2、M2.5、M3 打印友好圆脊近似内外螺纹已写入精确实体。内螺纹方案在负方向拆件生成带螺旋槽的螺丝柱，并在正方向拆件生成通孔和圆柱沉孔；外螺纹方案在负方向拆件生成一体式芯柱和圆脊螺旋，在正方向拆件生成带径向打印补偿的配合内螺纹孔。
- M2、M2.5、M3 ISO 公制粗牙 60° 内外螺纹已写入精确实体；按 ISO 261 粗牙系列分别使用 0.40、0.45、0.50 毫米螺距，并与打印友好圆脊近似牙型保持两个独立选项。
- ISO 牙型沿螺旋扫掠 60° 梯形截面，基本三角形高度为 `H = √3 / 2 × P`，工作牙高为 `5H/8`，牙顶截平宽度为 `P/8`，牙根宽度为 `3P/4`；内外螺纹使用同一螺旋起点和相位。
- 近似螺纹和 ISO 螺纹均使用切割轴局部 U/V 坐标覆盖 X/Y/Z 三轴，并返回公称直径、螺距和有效长度；ISO 结果额外返回 60° 牙型角和标准名称。配合孔按 FDM 打印加入独立径向补偿和受限轴向补偿，避免细螺距牙槽重叠。
- 根据切割轴建立局部平面坐标，使用真实支撑柱探针与两侧拆件的相交体积筛选可附着位置；默认目标为 2 个连接器和 2 个紧固结构，窄截面允许降为 2 个连接器和 1 个螺丝柱、卡扣、近似螺纹或 ISO 螺纹。
- 生成后检查 OpenCascade 实体有效性、封闭性、额外悬浮 Solid、连接结构最小设计壁厚、公母间隙和装配干涉。
- 公母间隙限制为 0.10–1.00 毫米；连接结构最小设计壁厚限制为 1.20 毫米，适配 P1S、0.4 毫米喷嘴及 PLA/PETG 的当前默认制造配置。
- 中文界面显示实际连接位置、连接器及螺丝柱/卡扣/近似螺纹/ISO 螺纹数量、卡扣长宽高、螺纹直径、螺距、有效长度、牙型角和标准名称、最小设计壁厚、装配干涉、补面数量和拆件体积误差；装配视图可查看闭合结果，拆分视图可查看两侧结构。

**下一批仍需实现：**

- 曲率、受力、疲劳寿命和打印方向分析；当前 `minimumDesignedWallMm` 仍只校验新生成连接结构自身的参数化设计壁厚，与下面的全模型表面采样结果是两个独立指标。

### 7. 全局壁厚采样与三维热力图

- 对任意封闭 STEP/STP 或上传 STL 复用通用实体导入、简单孔洞修复和封闭性校验，不绑定电子外壳、手办或固定零件类别。
- 对 OpenCascade 三角化表面按面积确定性采样，使用实体内部分类确定内法向，再沿内法向寻找最近下一处实体边界。
- 默认风险阈值为：危险 `< 0.80` 毫米、偏薄 `0.80–1.20` 毫米、建议 `1.20–2.00` 毫米、充足 `>= 2.00` 毫米。
- 返回最薄值、5% 分位、中位数、最大值、四级计数、有效采样数、表面积、三角面数和覆盖率；覆盖率低于 35% 时中文拒绝，避免把异常网格报告为安全。
- 三维视口使用原始 STL 坐标匹配离散采样点，并以红、橙、黄、绿顶点色显示最近样本风险；可关闭或重新显示热力图。
- 热力图支持点击局部区域和一键定位最薄处，三维视口显示发光标记及中文壁厚标签；Codex 面板同步显示来源零件、原始毫米坐标、风险等级和估算壁厚。
- CAD 参数化修改成功后，自动对同一零件重新执行壁厚采样，并按原始坐标恢复最近采样点，报告修改前后的局部估算壁厚变化。上传封闭单 Solid STL 已支持第一版受限局部实体重建：沿壁厚采样点的表面法向增加圆形凸台，或沿内法向切除圆孔；完成后读取当前工作文件重新采样。
- Web、Tauri/Rust 和 Zustand 数据流均已接入；参数修改、撤销/重做、新建项目、上传/移除 STL、拆件和 CAD 重建会清除旧分析，避免跨版本或跨来源复用结果。
- 该能力只能称为“表面法向射线采样估算”，不是有限元、受力或疲劳仿真；离散采样也不能保证覆盖未采样的极细局部。

验收标准：当前 CAD 零件和上传封闭 STL 均可完成分析；热力图、中文图例和统计与结果协议一致；可定位和清除局部采样点；CAD 参数化修改后能自动复查同一局部；上传封闭单 Solid STL 可连续执行圆形凸台加厚和圆孔切除并自动复查；切换来源或改变几何后不会继续显示旧结果。

### 8. 多视角联合标定（第一版已实现）

- 同一项目支持正面、背面、左侧、右侧、顶部、底部和透视照片；可一次多选，也可在向导中继续添加或移除照片。
- 每个视角独立保存文件名、视角、双点标定、导入时间、Codex 单图识别结果和接口观测；元数据及联合结果保存到本机应用存储，图片二进制暂不保存。
- 至少两个已完成标定和识别的视角才能执行联合标定；采用各视角毫米/像素比例的中位数作为联合尺度，并计算最大相对偏差。
- 最大尺度偏差超过 10% 时显示中文警告，要求检查共面关系并用卡尺复核，不自动声称制造级一致。
- 接口优先按跨视角稳定 ID 合并为“已匹配”；不同 ID 但类型和尺寸接近时只形成“待确认”候选；单视角接口继续保留。
- 多视角对同一 CAD 参数的估算值使用中位数融合；经过复核、需要开孔且可确定正交接口面的接口进入通用精确开孔协议，其他接口继续作为识别和避让信息保存。
- 当前实现复用桌面后端逐张调用 Codex 视觉分析，再在前端执行确定性的尺度融合与候选匹配；没有实现相机内外参求解、SfM、稠密摄影测量或任意物体三维网格重建。

验收标准：可管理至少两个不同视角并分别标定、识别；联合结果显示融合尺度、最大偏差、已匹配和待确认接口；尺度冲突有中文警告；应用后尺寸融合和通用正交面开孔规则可验证；所有能力边界在界面中可见。


### 9. 多视角接口人工确认与编辑闭环（第一版已实现）

- 联合标定结果中的待确认接口不能再直接应用；必须逐组执行“确认为同一接口”“拆为独立接口”或“忽略误识别”。
- 单视角接口可人工确认保留；尺寸相似但 ID 不同的跨视角候选可拆回各自独立接口，拆分后仍需逐一确认。
- 支持编辑接口唯一 ID、类型、所在面、宽高、照片横纵位置、物理横向偏移、底部偏移、是否需要开孔和开孔轮廓。
- 人工编辑值保存在联合结果的复核字段中，不覆盖原始 Codex 观测；项目接口列表和通用精确开孔协议优先使用复核值。
- 误识别接口可忽略并恢复；忽略项不会进入项目接口列表，也不会参与自动开孔。
- 联合结果和人工复核状态继续通过项目 Store 与本机存储保存；所有按钮、校验错误和状态提示均为中文。

验收标准：待确认项存在时“应用联合结果并生成精确模型”保持禁用；确认、拆分、忽略和编辑均有纯函数测试；全部待确认项处理完成后才能应用，且人工修正值进入接口输出和通用精确开孔映射。

### 10. 照片接口区域直接编辑（第一版已实现）

- 照片预览新增“尺寸标定 / 编辑接口”中文模式切换；识别前继续使用双点标定，识别后可进入接口直接编辑。
- 已识别接口按标定比例显示为真实宽高对应的矩形框；可点击选中、拖动修改中心位置，并通过右下角手柄调整宽度和高度。
- 界面区分原始识别、人工修改和当前选中状态，并显示接口类型、毫米尺寸、开孔要求、照片中心位置、横向偏移和底部偏移。
- 接口框中心和尺寸被限制在照片边界内，最小可操作宽高为 8 像素；正面接口横向偏移以照片中心为零，底部偏移按接口框底边到照片底边的已标定距离估算。
- 人工拖动或调整尺寸会写回当前单视角识别记录，并清空旧联合标定和跨视角匹配；界面用中文要求重新执行联合标定。
- 支持“恢复本次识别值”，恢复当前 Codex 识别批次的原始接口位置和尺寸。
- 当前能力只是在已标定二维照片平面上做确定性换算，不包含相机位姿优化、SfM、摄影测量或三维网格重建。

验收标准：毫米尺寸与照片百分比框双向换算正确；拖动和缩放不会越过照片边界；偏移同步更新；无效标定安全拒绝；接口编辑后旧联合结果不可继续应用。

### 11. 通用六面精确开孔（已实现）

- `InterfaceOpeningSpec`、Vite 开发后端、Tauri/Rust 后端和 Python Worker 已形成完整协议；前后端执行相同的数量、枚举、尺寸、置信度和字符串长度校验，并转换为 Python snake_case 字段。
- 支持正面、背面、左侧、右侧、顶部和底部六个正交接口面，不限制接口类型或模型类别。
- 支持圆孔、矩形孔、圆角矩形孔和槽孔四种二维轮廓；人工复核界面可修改类型、所在面、宽高、位置、是否开孔和轮廓。
- OpenCascade 对顶部开孔切上盖，对其余五面开孔切主体；缺少 `interface_openings` 时保留模板 USB 开孔，显式空数组时禁用所有接口开孔，非空数组时照片/人工复核开孔覆盖模板 USB。
- 切削前检查保守矩形包围盒的边缘余量和同面孔间距；切削体必须与目标实体实际相交；每次 Boolean 后必须保持有效、封闭且为单一 Solid。
- CAD 结果清单返回开孔模式、实际开孔列表、主体/上盖计数、最小边缘余量和最小孔间距；参数面板使用中文显示上述结果，并提示照片开孔已覆盖模板参数。
- 桌面版本快照和 AI 参数化修改前快照同时保存通用开孔状态。
- 新照片开孔同时保存解析后的中心坐标与底边定位锚点；老项目没有锚点时继续使用固定毫米坐标。边缘与间距校验不能替代受力、疲劳或有限元分析。

验收标准：Python 回归覆盖缺少字段、显式空数组、六个接口面、四种轮廓、主体/上盖目标、边缘余量拒绝、孔间距拒绝以及有效封闭单 Solid；前端协议、Rust 参数转换、TypeScript/Vite 构建和中文参数面板同步通过。

### 12. 照片开孔定位锚点与参数重算（已实现）

- 新照片开孔记录保存 `face-center-bottom` 定位来源、相对接口面水平中心的毫米偏移和相对接口面底边的毫米偏移，同时保留当前解析后的中心坐标供显示、版本记录和兼容读取。
- 手工修改参数或 Codex 修改参数时，Store 会立即按新外壳尺寸重新计算开孔中心；CAD 生成前再次解析，AI 修改失败回滚时同时恢复参数和开孔坐标。
- Vite、Tauri/Rust 和 Python Worker 校验同一套可选锚点协议；只要出现任一锚点字段，就必须完整提供定位来源、水平偏移和底部偏移。
- Python Worker 不信任前端旧中心坐标，会根据当前外壳高度或宽度再次解析；结果清单将锚点字段恢复为 camelCase，确保重启、版本快照和清单恢复后仍可继续重算。

### 13. 模型版本历史与参数/开孔对比（已实现）

- 三维视口左下角的版本状态已改为可点击入口，打开全中文“版本历史与对比”面板。
- 历史列表显示版本编号、名称、创建时间、当前版本状态，以及是否存在本机精确生成快照。
- 可选择任意历史版本作为基准，与当前参数化模型比较；全部外壳、参考元件、间隙、圆角、倒角和模板开孔参数均显示基准值、当前值与毫米增减量。
- 通用开孔比较区分模板参数开孔与自定义通用开孔模式，并按稳定开孔编号识别新增、删除和修改；修改项显示名称、接口类型、所在面、轮廓、宽高、中心坐标、圆角和照片定位锚点变化。
- `null` 模板开孔与 `[]` 明确无开孔不会被错误视为相同；老版本缺失开孔字段时按模板模式兼容读取；数值比较忽略 `1e-6` 毫米以内的浮点噪声。
- 支持从任意历史版本恢复参数和通用开孔状态，自动切回 CAD 视图、清除跨版本拆件和壁厚分析结果，并通过既有失效状态触发 OpenCascade 重建；恢复后继续修改会从该节点创建新分支并截断旧重做分支。
- 当前对比明确标注为“参数与通用开孔元数据差异”，不伪装成两个 OpenCascade 实体的精确布尔几何差异。

验收结果：前端新增 6 个纯函数测试，覆盖无变化、参数增减、新增/删除开孔、开孔字段与锚点修改、模板/自定义模式切换和浮点容差；浏览器人工验收已确认 2.0 → 2.4 毫米壁厚差异显示、任意版本恢复、自动重建入口和中文界面正常，控制台无错误或警告。

### 14. 版本快照旧实体视觉对比（第一版已实现）

- Tauri 新增受限的版本快照清单和文件读取命令；前端不能传入任意文件路径。快照目录必须是 `artifacts/versions` 的直接子目录，并在规范化路径后再次检查边界。
- 旧实体文件必须是 `generation-result.json` 中 `outputs`、`parts[].stlFile`、`parts[].stepFile` 或 `assemblyFile` 已声明的普通文件名；拒绝 `../`、快照根目录外绝对路径、未声明文件、目录外符号链接、缺失清单和缺失零件 STL。
- “版本历史与对比”面板为存在本机精确快照的历史版本提供“半透明重叠”和“并排对比”两个中文入口；当前 CAD 未重建完成、正在查看上传 STL、选择当前版本或历史版本没有快照时会明确禁用并显示原因。
- 三维视口以蓝色显示基准版本、橙色显示当前版本；重叠模式保持两个 STL 的原始毫米装配坐标，并排模式根据两个清单的实际最大零件宽度计算对称 X 轴偏移和 18 毫米实体间距，不绑定具体电子开发板尺寸。
- 对比期间历史实体不可选中，不会覆盖当前对象选择或壁厚局部定位；视口提供模式切换、中文图例和“退出对比”。参数改变、版本恢复、模型来源切换、项目重置或 CAD 重建会自动关闭旧对比，避免显示过期实体。
- 界面明确声明当前结果是两个已保存 STL 的视觉叠加，不等于稳定面级映射或 OpenCascade 精确布尔差集。

验收结果：新增 4 个前端布局测试，覆盖不同宽度不重叠、左右对称、毫米间距、重叠坐标保持和无效尺寸回退；Rust 增至 8 个测试，覆盖合法快照、清单读取、已声明 STL、路径穿越、快照根目录外访问、未声明文件和缺失清单中文错误。2026-07-20 完整回归通过：前端 10 个测试文件 57/57、Rust 8/8、通用精确开孔 10/10、拆件与补面 27/27、壁厚 6/6，TypeScript/Vite 构建、`cargo check` 和 Python 语法编译均通过；仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。Web 降级验收确认无本机快照的旧版本会显示中文原因，两个实体对比按钮保持禁用，参数差异仍正常显示且控制台无错误或警告；真实历史 STL 的重叠和并排显示仍需在 macOS Tauri 桌面端完成一次人工视觉验收。

**后续阶段：**把视口 STL 命中三角面精确回映射到“几何签名匹配第一版”的 CAD 面稳定 ID，并把面 ID、局部法线、截图和尺寸上下文交给 Codex；不能宣称任意拓扑修改下的面 ID 已完全稳定。
- 接口宽高、水平偏移和底部偏移始终保持毫米值，不随外壳按比例缩放；老项目没有锚点元数据时保持固定坐标并提示重新复核。
- 参数面板使用中文显示“底边锚定，随尺寸重算”或“固定坐标，建议重新复核”。

验收标准：前端覆盖侧面、顶部/底部和老格式兼容；Python 覆盖 Worker 重算、不完整协议拒绝和清单往返；Rust 覆盖 snake_case 转换与不完整协议拒绝；完整构建和既有拆件回归通过。

## 不能混淆的能力边界

- **识别结果**：Codex 从图片返回的结构化估计，不等于已经生成 CAD 特征。
- **制造方案**：拆件、连接和紧固结构的参数规划，不等于已经写入实体。
- **精确实体**：只有经过 OpenCascade 重建、有效性检查和尺寸检查的结果才能标记为已生成。
- **上传网格修洞**：只对独立、简单、近似共面的开放边界做三角化补面；非流形、自相交、分叉、嵌套和明显非共面破面仍会中文拒绝。
- **拆件切割补面**：模型成为封闭 Solid 后，由 OpenCascade Boolean 为拆件两侧生成切割面；它和上传阶段的网格修洞是两套不同校验过程。
- **全模型壁厚采样**：对原始完整实体执行离散法向射线估算；它不等于连接结构的 `minimumDesignedWallMm`，也不等于受力、疲劳或有限元仿真。

## 2026-07-17 验证记录

- 前端最新回归为 9 个测试文件、53/53 通过，覆盖多视角联合标定、接口人工复核、照片接口二维编辑、通用精确开孔协议、定位锚点参数重算和模型版本差异计算。
- 通用精确开孔已通过 TypeScript/Vite 构建、Rust `cargo check`、Rust 4/4 测试和 Python 10/10 OpenCascade 回归；Python 覆盖六个接口面、四种轮廓、模板/空数组语义、定位锚点重算与清单往返、边缘余量、孔间距、目标实体及有效封闭单 Solid。
- 当前内置浏览器不支持向文件输入控件注入本地照片，本轮未完成接口框拖动与缩放的浏览器点击式验收；不得据此声称真实 Codex 照片识别质量或该交互链已经用实拍样本验收。
- 浏览器界面验收上传两张不同尺寸的 PNG，分别显示 640 × 420 和 600 × 400 像素；两个视角的双点标定状态可独立保存和切换。
- 浏览器通过受控 Tauri `invoke` 模拟结构化视觉返回，仅用于验证前端数据链：两个视角均识别后，联合尺度为 0.15608 毫米/像素，同一稳定 ID 的 USB-C 显示为“已匹配”，待人工确认 0 组。
- 浏览器新会话复查图片预览的对象 URL 生命周期，控制台为 0 个错误、0 个警告；真实 Codex 视觉质量仍需在 macOS 桌面端使用实拍样本验收。

- `pnpm run model:generate`：通用 Worker 生成 `model-body`、`model-cover` 的 STEP/STL 和 `model-assembly.3mf` 成功。
- `pnpm run model:wall-test`：6/6 通过，覆盖四级阈值、序列化边界一致性、通用 STEP、薄壁 STL、复杂非流形中文拒绝和参数校验。
- `pnpm run model:split-test`：27/27 通过；除原有 STEP/任意 STL、网格修复、拆件补面和体积守恒覆盖外，覆盖五类连接器、M2/M2.5/M3 螺丝柱、PLA/PETG 可拆卡扣、M2/M2.5/M3 打印友好圆脊近似内外螺纹，以及 M2/M2.5/M3 ISO 公制粗牙 60° 内外螺纹；同时覆盖 X/Y/Z 局部坐标、标准螺距、牙型角元数据、常用材料间隙、浅深度/窄截面中文拒绝、单一封闭 Solid、近零公母干涉和精确 STEP/STL 写出。
- `pnpm run model:opening-test`：10/10 通过，覆盖模板兼容、显式空数组、六面四轮廓、照片定位锚点重算、不完整协议拒绝、清单往返、主体/上盖切削和制造约束拒绝。
- `pnpm run test`：9 个测试文件、53/53 通过，包含通用开孔协议、定位锚点重算、多视角、照片二维编辑、版本差异计算、壁厚局部上下文和既有前端逻辑。
- `pnpm run build`：TypeScript 与 Vite 生产构建通过。
- `pnpm run desktop:check`：Rust `cargo check` 通过。
- `pnpm run desktop:test`：Rust 4/4 通过，包含通用开孔 snake_case 参数转换和图片识别 Schema。
- 本地网页端到端验收：默认主体沿 X 轴中心拆件，D 形防转销、M3 螺丝柱和 0.25 毫米间隙成功生成 2 个连接器和 1 个螺丝柱；最小设计壁厚 1.20 毫米、装配干涉 0.000000 立方毫米、补面 2 个、体积误差 0.000000 立方毫米。
- PLA/PETG 卡扣网页端到端验收：同一主体沿 X 轴中心拆件，D 形防转销和 0.25 毫米间隙成功生成 2 个连接器和 1 个 6.00 × 2.60 × 9.50 毫米可拆卡扣；最小设计壁厚 1.20 毫米、装配干涉 0.000000 立方毫米、补面 2 个、体积误差 0.000000 立方毫米。
- M3 打印友好近似内螺纹网页端到端验收：同一主体沿 X 轴中心拆件，D 形防转销和 0.25 毫米间隙成功生成 2 个连接器和 1 个打印内螺纹；直径 3.00 毫米、螺距 1.20 毫米、有效长度 7.50 毫米，最小设计壁厚 1.20 毫米、装配干涉 0.000000 立方毫米、补面 2 个、体积误差 0.000000 立方毫米。
- M3 打印友好近似外螺纹网页端到端验收：同样配置成功生成 2 个连接器和 1 个打印外螺纹；直径 3.00 毫米、螺距 1.20 毫米、有效长度 7.50 毫米，最小设计壁厚 1.20 毫米、装配干涉 0.000000 立方毫米、补面 2 个、体积误差 0.000000 立方毫米。
- M3 ISO 公制粗牙 60° 内螺纹网页端到端验收：主体沿 X 轴中心拆件，D 形防转销和 0.25 毫米间隙成功生成 2 个连接器和 1 个 ISO 内螺纹；直径 3.00 毫米、螺距 0.50 毫米、有效长度 7.00 毫米、牙型角 60°，标准为“ISO 公制粗牙基本牙型”，最小设计壁厚 1.20 毫米、装配干涉 0.000000 立方毫米。
- M3 ISO 公制粗牙 60° 外螺纹网页端到端验收：同样配置成功生成 2 个连接器和 1 个 ISO 外螺纹；直径 3.00 毫米、螺距 0.50 毫米、有效长度 7.00 毫米、牙型角 60°，标准为“ISO 公制粗牙基本牙型”，补面 2 个、体积误差 0.000000 立方毫米。
- 已人工检查装配视图切换、精确实体状态栏、实际连接位置和“带连接结构的拆件”导出菜单；浏览器控制台无警告或错误。
- Chrome 浏览器新增开放网格人工验证：上传非项目内置的 `开放网格验收模型.stl`，输入 10 个三角面和 4 条开放边；自动识别 1 个简单平面孔洞，新增 2 个补面三角形，修复后形成 12 个三角面、1 个封闭实体和 34 × 22 × 16 毫米包围盒。
- 修复后的模型沿 X 轴中心平面生成两个封闭实体，拆件切割补面 2 个，体积误差 0.000000 立方毫米；导出菜单同时显示原始上传 STL、修复后工作 STL及两侧 STL/STEP。
- 已验证中文聊天消息、右侧网格修复指标、上传摘要、能力边界和视口状态；重新加载后的应用控制台无警告或错误。
- 移除三维视口对远程 HDR 环境贴图的依赖，断网时不再因资源加载失败导致画布崩溃。
- 当前 CAD 主体壁厚实测：1,200 个请求样本获得 1,155 个有效样本，覆盖率 96.25%，耗时约 3.24 秒，结果 JSON 约 152 KB；最薄 1.449725 毫米，5% 分位和中位数均为 2.0 毫米，危险 0、偏薄 0、建议 15、充足 1,140。
- 上传 STL 本地接口验收：任意文件名模型经简单平面孔洞修复后形成 12 个三角面、1 个封闭实体；壁厚分析获得 12/12 有效样本和 100% 覆盖率。
- 浏览器人工验收：CAD 主体分析完成后可在装配/拆分视图显示中文统计和红/橙/黄/绿三维热力图，并可关闭热力图；修复了材质从普通模式切换到顶点色模式时未触发着色器重建的问题，控制台无错误。
- 壁厚局部修改闭环第一版：热力图开启后可点击模型表面选择最近采样点或一键定位最薄处，三维视口显示中文风险标记；下一条 Codex 指令自动携带零件、原始毫米坐标、估算壁厚和风险等级，CAD 重建后自动重新采样并报告同一局部修改前后的估算壁厚变化。
- 浏览器人工验收：点击“定位最薄处”后，三维标记和 Codex 局部上下文卡片同步出现；点击“清除局部定位”后两者同步消失。应用页面没有错误或警告，控制台中仅存在浏览器扩展脚本自身的警告。
- 上传 STL 受限局部实体重建已接通 Web/Tauri/Python：壁厚采样点提供原始毫米坐标和单位内法向，当前仅执行局部圆形凸台加厚与圆孔切除；任意自由曲面、网格雕刻和多 Solid 局部编辑仍会中文拒绝。
- 连续局部修改会读取 `imported-model-result.json.sourceFile` 指向的 `imported-model-working.stl`，原始 `imported-model.stl` 始终保留；临时 STEP/STL 通过重新导入验证后，工作文件与 JSON 清单采用带备份回滚的批量替换。
- 本地 API 连续编辑验收：20 × 16 × 10 毫米封闭长方体先增加直径 6 毫米、高 2 毫米凸台，体积由 3200.000000 增至 3256.525235 立方毫米；再从新顶面切除直径 2 毫米、深 4 毫米圆孔，修改前体积正确继承为 3256.525235，修改后为 3243.964071 立方毫米。两次结果均有效、封闭且为单一 Solid。
- 修改后壁厚复查读取 `imported-model-working.stl`，按“表面法向射线采样估算”返回 120 个有效样本，最薄估算 1.999378 毫米。
- 2026-07-20 最新回归通过：前端 10 个测试文件 57/57、Rust 8/8、通用精确开孔 Python 10/10、`model:split-test` 27/27、壁厚 Python 6/6、TypeScript/Vite 构建、`cargo check` 和 Python 语法编译均通过；仅保留既有 Vite 大分块警告与 CadQuery 依赖的 Pyparsing 弃用警告。
- 浏览器端到端验收：通过“拆件与连接”上传任意文件名 STL，页面显示真实文件名、20 × 16 × 10 毫米包围盒、12 个三角面、单一封闭实体和 3200.00 立方毫米体积；壁厚热力图完成 12/12 样本和 100% 覆盖率。
- 浏览器连续局部修改验收：定位壁厚样本后输入中文自然语言，先完成直径 6 毫米、高 2 毫米圆形凸台，再完成直径 2 毫米、深 4 毫米圆孔；界面分别显示 3200.00 → 3256.53 和 3256.53 → 3243.96 立方毫米，并在每次修改后自动复查同一局部。浏览器控制台无错误或警告。
- 本轮未执行 macOS DMG 打包；发布包仍需补充签名、公证和 Python/CadQuery 运行环境内置方案。

## 本轮仍需真实样本确认

- 照片上传、双点标定和 Codex 结构化接口识别的数据链已实现，但还需要使用真实元件接口面照片完成一次桌面端视觉识别端到端验收；ESP32 照片可作为首个 Demo 样本。
- 当前已支持封闭 STL 和带独立简单平面孔洞的开放 STL；非流形/自相交修复、分叉或嵌套边界、复杂非共面破面重建和 AI 自动选择拆件位置仍在后续阶段。

### 15. OpenCascade 精确版本布尔差异（第一版已实现）

- 新增通用 `modeling/version_geometry_difference.py` Worker，按 `generation-result.json` 中稳定的 `parts[].id` 匹配历史与当前零件，不绑定电子外壳、ESP32、手办或固定零件数量。
- 对匹配零件执行 `current.cut(base)` 与 `base.cut(current)`，分别得到当前版本新增区域和相对当前版本已删除区域；新增零件和删除零件按完整实体处理，零体积结果不导出无效 STL。
- Worker 只接受清单 `outputs` 已声明的直接 STEP/STP 文件，拒绝目录穿越、重复零件 ID、缺失文件、非毫米制清单、无效/开放实体和无体积模型；输出记录布尔容差、Solid 数与新增/删除体积。
- Tauri 新增 `run_version_geometry_difference` 受限命令：历史目录必须是 `artifacts/versions` 的直接快照子目录，当前目录和 Worker 路径由后端内部构造，并复用 CAD 生成锁；前端不能提交任意 STEP、Worker 或输出路径。
- Rust 会再次校验历史/当前 STEP 清单声明、规范化文件边界，以及 `version-difference-*.stl` 输出名称和实际位置；前端只能通过既有生成文件协议读取差异结果清单声明的 STL。
- “版本历史与对比”面板新增“计算精确差异”；三维视口以半透明灰色显示当前完整模型，以绿色显示新增区域、红色显示删除区域，并显示 `OpenCascade 精确布尔差集`、新增/删除体积和无变化提示。差异实体不可选择，不污染当前对象状态。
- Web 模式不会伪造精确结果；没有本机快照、当前 CAD 尚未完成重建、正在查看上传 STL 或选择当前版本时，按钮保持禁用并显示中文原因。

验收结果：Python 新增 6 个测试，覆盖相同实体、仅新增体积、移动开孔产生双向差异、多零件按稳定 ID 匹配、任意新增/删除零件以及目录穿越/未声明 STEP 拒绝；Rust 当时增至 10 个测试，新增历史 STEP 和差异 STL 输出边界校验。实体级差异不依赖面 ID；后续“几何签名匹配第一版”见第 16 节。

### 16. 跨重建面几何签名匹配（第一版已实现）

- 新增通用 `modeling/face_geometry_signatures.py`，对任意 CadQuery/OpenCascade 实体逐面提取曲面类型、面积、绝对中心、法向、绝对包围盒、归一化中心、归一化包围盒、面积比例、边数量和边曲线类型摘要；使用规范化 JSON 的 SHA-256 生成几何指纹。
- 当前生成 Worker 在覆盖输出前读取上一版 `generation-result.json`，只在相同稳定 `parts[].id` 内执行一对一近似匹配；优先继承完全相同指纹，再按归一化位置、包围盒、面积比例、平面法向和边拓扑加权匹配。匹配成功的当前面继承历史 `stableId`，未匹配面分配新 ID。
- 每个零件清单新增 `faces[]` 和 `faceMatching`，顶层清单新增聚合 `faceMatching`；记录历史/当前面数、继承、新增、消失数量、平均继承置信度、匹配阈值和中文能力边界。
- 前端新增通用面描述类型和“零件 ID + 面稳定 ID”跨版本统计；进入实体对比后显示共享稳定面、新增编号和消失编号。旧快照没有面描述时显示中文兼容提示，不伪造匹配结果。
- 明确能力边界：这是“几何签名匹配第一版”，不是 OpenCascade 原生永久拓扑命名；大幅拓扑变化、对称面或布尔重建可能重新编号。视口命中三角面到稳定面 ID 的第一版回映射见第 17 节。
- 修正版本快照复制范围：只保存 `generation-result.json` 及其 `outputs`、零件 STL/STEP 和装配文件声明；不再把旧壁厚、拆件、局部编辑或版本差异临时结果复制进快照。复制复用生成锁，缺失或目录穿越声明会中文拒绝，失败时删除不完整目录。
- 桌面发布资源补充 `face_geometry_signatures.py` 和上一阶段的 `version_geometry_difference.py`，避免开发环境可用但发布包缺少 Worker。

验收结果：面几何签名 Python 7/7，覆盖相同实体、整体尺寸变化、增加孔洞、真实保护壳参数变化、唯一 ID/指纹、曲面类型拒绝和重复历史 ID；前端新增 2 个稳定面统计测试，完整前端为 11 个测试文件 60/60；Rust 增至 12/12，新增快照只复制当前 CAD 声明文件、拒绝目录穿越和缺失文件。2026-07-20 完整回归包括 TypeScript/Vite 构建、`cargo check`、版本差异 6/6、通用开孔 10/10、拆件 27/27、壁厚 6/6和 Python 语法编译；仅保留既有 Vite 大分块警告和 CadQuery/Pyparsing 弃用警告。

### 17. STL 三角面到 CAD 稳定面的可验证回映射与局部编辑（第一版已实现）

- 新增通用 `modeling/face_tessellation_mapping.py`：对任意参数化 CadQuery/OpenCascade 零件逐面独立三角化，按稳定面顺序连接为专用选择 STL，并生成一对一 face-map JSON；实现不依赖 ESP32、电子外壳、手办或固定零件数量。
- 每个映射项记录稳定面 ID、曲面类型、面积、面中心和连续 `[triangleStart, triangleStart + triangleCount)` 区间；选择 STL 与用户导出的 STL 分离，避免普通导出优化改变交互映射。
- Three.js 视口加载当前清单声明的选择 STL 和 face-map；点击使用 `event.faceIndex` 回查稳定面，框选支持屏幕空间三角形/矩形真实相交，不再只检查三角形中心。
- 框选为候选面收集代表采样点，并通过当前摄像机 `Raycaster` 第一命中验证可见性，过滤被前方几何遮挡的背面；候选过多时最多保留 100 个稳定面并显示中文提示。
- 点击和框选都会生成局部截图；WebGL 画布启用可读取缓冲，截图 Data URL 在桌面端转换为 PNG 字节附件，不把大段图片数据写入文本提示。
- 点击命中先通过 `worldToLocal` 去除装配/拆分视图偏移，再应用选择网格坐标逆变换，恢复到零件原始毫米坐标；法线使用对应的逆变换法线矩阵恢复。真实拆分视图验证中，上移后的上盖命中仍报告原始 CAD 的 Z = 2.00 毫米。
- 发送给 Codex 的中文结构化上下文包含零件 ID/名称、稳定面 ID、曲面类型、面积、面中心、原始毫米命中坐标、外法线、本次三角面索引、零件包围盒、摄像机矩阵、选择方式、尺寸信息和截图状态。
- Rust 生成文件协议从结果清单 `outputs` 动态允许选择 STL 和 face-map JSON，不把 `model-body-*`、`model-cover-*` 写死为唯一名称；目录穿越继续拒绝。新增测试以 `custom-shell-selection.stl` 和 `custom-shell-face-map.json` 验证通用命名。
- Zustand 状态测试覆盖 CAD 就绪、上传 STL、制造拆件替代几何、版本实体对比、参数变化清理和主动清理等边界，避免在不匹配的几何上保留旧面选择。

验收结果：主体生成 39 个稳定面、1608 个三角面，上盖生成 58 个稳定面、3688 个三角面，映射区间连续且总数一致；完整前端测试为 13 个测试文件 71/71，Rust 为 14/14。2026-07-20 完整回归还通过 TypeScript/Vite 构建、`cargo check`、面签名 7/7、面三角映射 4/4、版本差异 6/6、通用开孔 10/10、拆件 27/27、壁厚 6/6 和 Python 语法编译；仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。浏览器实际验证点击选择、拆分视图逆变换、局部截图与框选高亮均可用；同一顶部框选区域从旧中心判断的 82 个面降为经遮挡过滤后的 22 个当前可见面，控制台无错误或警告。

能力边界：`triangleIndex` 只对同一次生成的选择网格有效；稳定面 ID 仍是“几何签名匹配第一版”，不是 OpenCascade 原生永久拓扑命名；上传 STL 使用独立网格区域/壁厚采样协议；截至本阶段尚没有曲面 UV（已在第 23 阶段实现），也尚未支持任意拓扑的通用局部特征修改。

第 17 阶段提出的下一步已经进入第 18 阶段并完成第一版，具体实现与能力边界如下。

### 18. 稳定 CAD 平面局部特征写入 OpenCascade（第一版已实现）

- 新增通用 `modeling/local_cad_feature.py`，不绑定 ESP32、电子外壳或固定零件数量；从当前 `generation-result.json` 定位目标零件 STEP，并以 `partId + stableFaceId` 重新查找 OpenCascade 面。
- 初始版本只接受点击选择的单个平面，支持沿该面真实外法线增加圆形凸台，以及沿内法线切除圆孔；第 20 阶段已扩展旋转矩形凸台、矩形孔和槽孔，第 21 阶段已扩展整面向外拉伸与向内偏移，第 22 阶段已扩展平面所属单条稳定边的圆角与等距倒角。前端点击法线不直接决定布尔方向，只用于与 OpenCascade 面外法线做一致性校验。
- 请求必须携带本次选择清单修订号；修订号不一致时拒绝执行并明确提示旧 `triangleIndex` 已失效。点击点还必须通过 OpenCascade 面距离校验。
- 圆形半径限制为 0.50–100.00 毫米，矩形与槽孔轮廓尺寸按判别操作分别校验，局部深度/高度及整面移动距离限制为 0.20–200.00 毫米，平面内旋转角限制为 -180°–180°；操作、互斥尺寸字段、标识长度、有限数值和中文指令长度在 Tauri/Rust 与 Vite 本机 API 两条链路中执行一致校验。
- 布尔结果必须有效、封闭且只有一个 Solid；所有凸台与整面向外拉伸操作体积必须增加，所有孔、槽与整面向内偏移操作体积必须减少。STEP/STL 重新导出后再次检查，STL 体积误差超过容差则整次修改失败。
- 成功后重新生成目标零件面描述、几何签名匹配摘要、专用选择 STL 和面映射，并同步重建保留 8 毫米打印间距的通用多零件 3MF；`generation-result.json` 更新修订号、文件清单和 `localFeatures[]`。
- 文件写入使用临时文件、备份和批量替换；失败时尽力恢复旧 STEP/STL/3MF/清单，前端保留原 `cadResult` 和原稳定 CAD 局部选择。
- Zustand 命令链在修改前后尝试保存本机快照；成功后清空旧制造拆件、壁厚、版本差异和稳定 CAD 局部选择，提交版本记录，并用中文报告体积变化和校验结果。
- 第 18–22 阶段当时的命令面板提供可直接执行的中文示例，并提示平面轮廓操作只支持点击单平面、边特征只支持点击平面所属单边；框选多面和曲面当时只作为 AI 上下文。第 34 阶段已把单边圆角/倒角扩展到非平面所属边。修改完成后仍必须重新选择，因为原 `triangleIndex`、曲面 UV 和稳定边选择已失效。

第 18–22 阶段当时的能力边界：稳定面以及所属稳定面内的稳定边仍使用“几何签名匹配第一版”，不是 OpenCascade 原生永久拓扑命名。当时支持点击单平面的圆形凸台、圆孔、旋转矩形凸台、旋转矩形孔、槽孔、整面向外拉伸或向内偏移，以及点击平面所属单条稳定边的圆角或等距倒角；当时尚不支持曲面所属边、多边链、整圈自动传播、可变半径圆角、曲面 UV、框选多面布尔或任意拓扑编辑。曲面 UV 已在第 23 阶段实现，曲面所属单边圆角/倒角已在第 34 阶段实现，唯一平面边界整圈传播第一版已在第 35 阶段实现。

自动化覆盖：Python 测试验证凸台体积增加、圆孔体积减少、封闭单 Solid、清单/选择网格/面映射/3MF 刷新、过期修订拒绝和法线不一致拒绝；前端协议测试验证中文尺寸解析以及多面/曲面拒绝；Store 测试验证成功刷新和失败保留原模型；Rust `cargo check` 验证命令注册和类型链路。第 19 阶段会在此基础上增加 Codex 结构化计划与重放回归。

### 19. Codex 稳定面局部特征结构化计划与参数化重建安全重放（已实现）

- Codex 输出协议新增 `localFeature`，当前只允许 `add-cylinder`、`cut-cylinder`、`add-rectangle`、`cut-rectangle`、`cut-slot`、`offset-face-outward`、`offset-face-inward`、`fillet-edge` 和 `chamfer-edge`；整模参数变化与局部特征不能同时出现，Codex 不能输出或执行任意 Python、CadQuery、Shell。
- Rust 后端与 TypeScript 前端执行双重校验：平面操作必须点击选择单个 `PLANE`，边操作必须点击选择 `PLANE` 所属单条稳定边；`faces[0]`、`hit` 与 `edge` 必须一致，Codex 返回的 `partId + stableFaceId + stableEdgeId` 必须逐字绑定当前选择。最终中心和法线始终取自已验证的选择上下文。
- Codex 返回空计划、未知操作、越界尺寸、其他零件、其他稳定面或其他稳定边时不调用 Worker，保留当前 CAD 和选择并显示中文原因。Codex 未登录时才使用本地确定性中文尺寸解析。
- 新增 `modeling/local_cad_feature_core.py`，让直接局部修改和整模重建重放复用同一套 OpenCascade 重新定位、点距、法线、布尔、单 Solid 和体积方向校验。
- `generate_model.py` 每次从 `build_body()` / `build_cover()` 的基础参数化实体开始，按 `localFeatures[]` 记录顺序重放；不会在上一次最终实体上重复叠加。每条记录保存修改前 `targetFace` 几何签名快照，用于直接稳定 ID 不可用时的辅助定位。
- 重放最多接受 200 条记录；未知零件、稳定面消失、记录中心偏离、法线反向、尺寸非法或布尔失败都会拒绝整次重建。失败发生在 STEP/STL/3MF 和清单导出之前，因此保留最后有效模型，不静默跳过。
- 成功清单新增 `localFeatureReplay`，记录请求数、成功重放数和目标修订号；每条记录更新 `replayStatus` 与 `replayedRevision`。最终选择 STL、面映射、STEP、STL 和 3MF 均基于重放后的实体重新生成。
- 自动化新增参数化生成后重放圆形/矩形凸台、圆孔/矩形孔/槽孔和整面拉伸/偏移、多特征顺序、重复重建不重复叠加、中心/法线/未知零件失败保护、`targetFace` 回退和全部导出资产更新；Rust 覆盖计划目标、选择类型、判别尺寸字段和尺寸边界；Store 覆盖 Codex 成功、越权目标、空计划和调用失败。

2026-07-20 第 19 阶段回归通过：前端、Rust、OpenCascade Worker、TypeScript/Vite 构建、`cargo check` 和全部 Modeling Python 语法编译均通过。第 20 阶段新增操作后的最新完整回归记录见下节。

能力边界：`targetFace` 回退仍属于“几何签名匹配第一版”，不能宣称永久拓扑命名；`triangleIndex` 只对同一次生成的选择网格有效。截至本阶段不支持曲面 UV（已在第 23 阶段实现）、框选多面局部布尔或任意拓扑编辑。

### 20. 稳定 CAD 平面矩形与槽孔局部特征（已实现）

- 通用平面轮廓协议扩展为五种受限操作：圆形凸台、圆孔、矩形凸台、矩形孔和槽孔；不绑定 ESP32、电子外壳、手办或固定模型类别。
- `modeling/local_cad_feature_core.py` 使用确定性的局部平面 X/Y 轴构造轮廓，矩形和槽孔支持 `rotationDeg` 平面内旋转；槽孔使用总长与宽度定义，且长度不得小于宽度。
- TypeScript、Vite 本机 API、Tauri/Rust 与 Python Worker 采用一致的判别字段协议：圆形只能携带 `radiusMm`，矩形只能携带 `widthMm + heightMm`，槽孔只能携带 `widthMm + lengthMm`，无关字段必须为空。
- Codex 仍只能返回受限 JSON 计划，目标必须逐字绑定当前 `partId + stableFaceId`；点击中心和法线继续取自前端已验证选择，不能由 Codex 伪造或切换。
- 直接局部修改和参数化整模重建共用同一几何核心；`localFeatures[]` 保存可空轮廓尺寸、深度和旋转角，重建时严格按记录顺序重放，任一非法或失效记录都会拒绝整次重建并保留最后有效模型。
- 自动化覆盖旋转矩形凸台、旋转矩形孔、旋转槽孔、体积方向、封闭单 Solid、错误判别字段、槽孔尺寸关系、旋转角边界，以及三种新特征的参数化重放一致性。

2026-07-20 完整回归通过：前端 15 个测试文件 84/84，Rust 20/20；面签名 7/7、面三角映射 4/4、通用开孔 10/10、拆件与连接结构 27/27、壁厚 6/6、版本差异 6/6、上传 STL 局部编辑 4/4、稳定 CAD 面局部特征 4/4、通用平面矩形/槽孔 4/4、参数化局部特征安全重放 8/8；TypeScript/Vite 构建、`cargo check` 和全部 Modeling Python 语法编译均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

能力边界：上传 STL 仍使用独立的网格区域与壁厚采样协议，目前只支持圆形凸台加厚和圆孔切除；不能把本阶段的参数化稳定面矩形/槽孔能力宣称为上传 STL 已支持。

### 21. 稳定 CAD 平面整面拉伸与偏移（已实现）

- 受限稳定平面协议扩展为七种操作，新增 `offset-face-outward` 与 `offset-face-inward`；两种整面操作只接受 `depthMm`，`radiusMm`、`widthMm`、`heightMm`、`lengthMm` 必须全部为空，`rotationDeg` 必须为 0。
- OpenCascade 几何核心使用 `Face.thicken()` 从目标面的完整外环与内环生成封闭工具实体，不自行重建轮廓；测试覆盖带圆孔内环的平面，确认整面向外拉伸不会填死原孔。
- 正厚度的实际方向不依赖前端或 Codex 猜测：先比较加厚工具质心相对真实面中心在已验证外法线上的投影，再选择正负厚度；投影不足以可靠判断时用中文拒绝执行。
- 整面向外拉伸使用布尔并集，向内偏移使用布尔切除；结果继续要求有效、封闭、单一 Solid，并分别验证体积增加或减少。点击位置、命中法线和 `partId + stableFaceId` 继续由已验证选择上下文绑定。
- TypeScript 本地中文解析支持“整面向外拉伸 2 毫米”和“将这个面向内偏移 1.5 毫米”；Vite 本机 API、Tauri/Rust、Codex JSON Schema、中文提示与 Python Worker 使用相同判别字段和范围校验。
- `localFeatures[]` 可记录两种整面操作，参数化整模重建从基础实体开始按顺序调用同一几何核心安全重放；任何稳定面、中心、法线、尺寸或布尔失败仍会拒绝整次重建，不覆盖最后有效模型。
- 命令面板新增全中文整面操作建议，执行中状态、失败说明和成功体积摘要保持中文；修改后仍必须重新选择，因为原选择网格的 `triangleIndex` 已失效。

2026-07-20 完整回归通过：前端 15 个测试文件 86/86，Rust 20/20；面签名 7/7、面三角映射 4/4、通用开孔 10/10、拆件与连接结构 27/27、壁厚 6/6、版本差异 6/6、上传 STL 局部编辑 4/4、稳定 CAD 面局部特征 5/5、通用平面与整面几何核心 7/7、参数化局部特征安全重放 9/9；TypeScript/Vite 构建、`cargo check` 和全部 Modeling Python 语法编译均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

能力边界：整面拉伸/偏移目前只适用于参数化 CAD 中点击选择的单个稳定平面；上传 STL 仍只支持圆形凸台和圆孔，不能宣称支持整面偏移。稳定面定位仍是“几何签名匹配第一版”，`triangleIndex` 仍只对同一次生成的选择网格有效；截至本阶段暂不支持曲面 UV（已在第 23 阶段实现）、框选多面操作或任意拓扑编辑。

### 22. 稳定 CAD 单边圆角与倒角（已实现）

- 三维视口新增“点击选边”模式：先通过当前选择网格的 `triangleIndex` 定位稳定面，再以面内边采样折线找出离点击位置最近的候选边；Python Worker 随后使用 OpenCascade 真实拓扑重新定位并精确复核点击距离。
- 圆角和倒角目标必须由 `partId + stableFaceId + stableEdgeId` 共同限定。Codex 只能返回受限 JSON 计划，不得输出或执行 Python、CadQuery、Shell，不得切换当前已验证零件、稳定面或稳定边。
- `stableEdgeId` 是所属稳定面内的几何签名匹配第一版，包含边类型、长度、中心、端点和采样点等签名；它不是 OpenCascade 永久拓扑命名，也不保证在大幅拓扑变化、对称结构或复杂布尔后保持编号。
- 第一版只支持 `PLANE` 所属的单条稳定边，操作为 `fillet-edge` 或 `chamfer-edge`；圆角半径或倒角距离使用 `depthMm`，范围为 0.20–50.00 毫米，四个平面轮廓尺寸必须为空且 `rotationDeg` 必须为 0。
- 第 22 阶段本地中文解析已支持“将这条边做 2 毫米圆角”和“将这条边做 1 毫米倒角”；命令面板、执行状态、错误和成功摘要全部使用中文。当时曲面所属边会在前端、Rust、TypeScript 和 Python 各层明确拒绝；该限制已在第 34 阶段解除。
- OpenCascade 结果继续要求有效、封闭且为单一 Solid；Worker 导出更新后的 STEP、STL、选择 STL、面映射和通用多零件 3MF，并返回体积变化、稳定面/稳定边继承状态和点击点到真实目标边的距离。
- `localFeatures[]` 保存 `stableEdgeId` 与修改前 `targetEdge` 几何签名快照。参数化整模重建从基础实体开始按记录顺序安全重放；任何稳定边、点击位置、尺寸或布尔失败都会拒绝整次重建并保留最后有效模型。
- 每次修改都会重新生成选择网格，因此旧 `triangleIndex` 仅对修改前的当前选择网格有效，原稳定边选择也同时失效；继续修改前必须重新选择。
- 第 22 阶段自动化覆盖中文圆角/倒角解析、Codex 稳定边越权拒绝、当时的曲面边拒绝、尺寸与互斥字段校验、圆角/倒角有效封闭单 Solid、边描述与点击精确复核、文件导出、参数缩放后的边 ID 继承、安全重放和失败不覆盖，以及 Store 成功清理选择和 Worker 失败保留当前模型。第 34 阶段新增曲面所属边的接受、安全执行和拒绝过期 UV 回归。

2026-07-20 完整回归通过：前端 15 个测试文件 93/93，Rust 25/25；面签名 7/7、面三角映射 4/4、通用开孔 10/10、拆件与连接结构 27/27、壁厚 6/6、版本差异 6/6、上传 STL 局部编辑 4/4、稳定 CAD 局部特征 5/5、通用平面与整面几何核心 7/7、参数化局部特征安全重放 11/11、稳定 CAD 单边特征 9/9；TypeScript/Vite 构建、`cargo check` 和指定 Modeling Python 文件语法编译均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

第 22 阶段当时的能力边界：不支持曲面所属边、多边链、整圈自动传播、可变半径圆角、圆角连续性控制或手工拓扑编辑；稳定边只能称为“所属稳定面内的几何签名匹配第一版”。其中曲面所属单边圆角/倒角已在第 34 阶段实现，唯一平面边界整圈传播第一版已在第 35 阶段实现，唯一切线连续边链第一版已在第 36 阶段实现；当前仍不支持手工多选边链、分叉链、多个 Wire、可变半径、连续性等级控制和手工拓扑编辑。

### 23. 曲面点击真实法线与 UV（已实现）

- 新增独立的 OpenCascade 曲面命中解析链路：前端按当前 `revision + partId + stableFaceId + triangleIndex` 提交选择网格命中点和法线，Python Worker 从对应 STEP 重新定位稳定面，并用 `GeomAPI_ProjectPointOnSurf` 将网格点投影到真实裁剪曲面。
- 投影结果必须位于真实裁剪面内，并同时返回真实三维点、曲面参数 `u/v`、UV 边界、点到曲面的投影距离和网格法线与真实外法线的点积；真实外法线由 `BRepGProp_Face.Normal` 计算，并考虑 OpenCascade 面方向。
- TypeScript 协议和 Store 只接受与当前修订、零件、稳定面及三角形索引完全一致的结果。用户在解析期间重新点击后，旧异步响应会被丢弃，不能覆盖新的选择上下文。
- 解析成功时，选择上下文标记为 `precision: 'opencascade'` 和 `resolutionStatus: 'resolved'`，并保留原网格点与网格法线用于诊断；解析失败时保留网格预览，记录中文错误，但不生成或伪造 UV。
- Vite 开发 API 与 Tauri/Rust 命令均直接解析 Worker stdout JSON，不读取共享结果文件，并复用生成互斥锁，避免曲面解析期间模型修订变化；两层入口都会校验 ID 长度、非负整数 `triangleIndex` 和有限坐标。
- Codex 局部特征计划和本地精确 CAD 操作必须等待当前点击达到 `resolved + opencascade` 且具有有限 UV；解析中或失败时明确拒绝执行，不能把选择网格坐标或法线冒充 OpenCascade 精确结果。
- 命令面板已提供全中文的“解析中 / 精确命中 / 解析失败”状态，成功时显示 UV、投影距离等诊断信息，失败时说明仅保留选择网格预览。
- 第 23 阶段完成时，曲面真实点、外法线和 UV 只进入选择与 AI 上下文；第 24 阶段已在此基础上实现受限圆形凸台和圆孔，其他曲面操作仍未开放。

2026-07-20 完整回归通过：前端 17 个测试文件 104/104，Rust 28/28；曲面命中 7/7、面签名 7/7、面三角映射 4/4、通用开孔 10/10、拆件与连接结构 27/27、壁厚 6/6、版本差异 6/6、上传 STL 局部编辑 4/4、稳定 CAD 局部特征 5/5、参数化局部特征安全重放 11/11、通用平面与整面几何核心 7/7、稳定 CAD 单边特征 9/9；TypeScript/Vite 构建、`cargo check`、Rust 测试和指定 Modeling Python 文件语法编译均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

第 23 阶段当时的能力边界：稳定面仍是“几何签名匹配第一版”，不是 OpenCascade 永久拓扑命名；`triangleIndex` 只对同一次生成的选择网格有效。当时尚未实现的受限曲面圆形特征已在第 24 阶段完成，曲面所属单边圆角/倒角已在第 34 阶段完成，唯一平面边界整圈传播第一版已在第 35 阶段完成；曲面参数编辑、任意多边链、切线链、多个 Wire、可变半径圆角、框选多面布尔和任意拓扑编辑仍未实现。

**第 23 阶段后续：**受限曲面局部圆孔/凸台已在第 24 阶段实现。

### 24. 基于真实 UV 的受限曲面圆形凸台与圆孔（已实现）

- 曲面点击继续绑定当前 `revision + partId + stableFaceId + triangleIndex`，只接受 `resolved + opencascade` 的真实命中结果。Worker 会根据稳定面协议重新定位 STEP 目标面，并在请求 UV 上重新计算真实三维点和考虑面方向的外法线；请求曲面类型、记录中心、记录法线或 UV 不一致时拒绝修改。
- 第 24 阶段第一版非平面曲面只开放圆形凸台和圆孔：凸台沿真实外法线生成，圆孔沿真实内法线切除。当时曲面矩形凸台、矩形孔、槽孔、整面偏移、曲面边圆角/倒角和曲面参数直接编辑仍在前端、Vite、Rust、Python 与 Codex 结构化计划各层明确拒绝；后续受限矩形/槽孔和曲面所属单边圆角/倒角已分别在后续阶段实现。
- 使用 `GeomLProp_SLProps` 计算点击位置最大绝对曲率，并要求 `圆形半径 × 最大绝对曲率 <= 0.5`；返回最大绝对曲率、最小曲率半径和曲率比，避免第一版平面圆轮廓在高曲率区域产生明显畸变或不稳定布尔。
- 对切平面圆周采样并投影回底层曲面，再使用真实裁剪面分类器检查作用区域没有越过裁剪边界。该算法是第一版局部安全约束，不等同于曲面测地圆或通用曲面偏置。
- 沿真实内法线使用 OpenCascade 直线/曲面求交估算局部壁厚：曲面凸台所在处壁厚小于 0.80 毫米时拒绝；盲孔要求至少保留 1.20 毫米；深度达到当前法线方向最近下一处边界时标记为通孔。复杂凹面、多层实体或同一射线上多次交点时，该值仍只是局部几何估算，不是有限元分析。
- 修改结果继续要求有效、封闭且为单一 Solid，并校验体积增减方向；STEP、STL、选择网格、面映射和通用 3MF 使用临时文件与批量回滚写回。考虑 STL 对曲面的三角网格近似，导出体积允许千分之一误差，同时仍要求 STL 再导入后有效、封闭且单一 Solid。
- `localFeatures[]` 已保存 `surfaceGeometryType` 和 `surfaceUv`。参数化整模重建会按记录顺序复核稳定面、真实 UV、记录中心、外法线、曲率和壁厚后安全重放；曲面记录缺少 UV 或任一检查失败时拒绝整次重建并保留最后有效模型。
- Codex 仍只能返回受限 JSON 计划，不允许输出或执行 Python、CadQuery 或 Shell；曲面计划只能选择当前点击目标的 `add-cylinder` 或 `cut-cylinder`，中心、外法线和 UV 必须使用前端验证值，不能由 Codex 改写或伪造。
- 命令面板、执行状态、成功摘要和错误全部使用中文。曲面成功摘要会显示曲率比、局部壁厚、剩余壁厚或通孔状态；Worker 失败时保留当前 CAD、曲面选择和真实 UV，便于用户调整尺寸后重试。

2026-07-20 本阶段新增自动化覆盖：曲面几何核心 13/13，包含凸台、盲孔、通孔、曲率、裁剪边界、薄壁、错误类型、缺失 UV，以及曲面参数化重放成功和缺失 UV 的整体拒绝；稳定 CAD Worker 6/6，包含曲面圆孔的 STEP/STL/选择网格/面映射/3MF 写回、曲率与壁厚诊断、曲面字段持久化；前端 Store 覆盖曲面请求携带真实 UV、成功清理选择与显示诊断，以及失败保留模型和曲面选择。

2026-07-20 完整回归通过：前端 17 个测试文件 107/107，Rust 28/28；曲面圆形核心与重放 13/13、通用平面与整面几何核心 7/7、稳定 CAD 单边特征 9/9、稳定 CAD Worker 6/6、参数化局部特征安全重放 11/11、曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试和本阶段 Python 语法编译均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

能力边界：当前曲面作用区域仍是切平面圆形轮廓的安全近似，不是任意曲面贴合轮廓；局部壁厚是点击位置沿真实内法线的最近边界估算；稳定面仍是“几何签名匹配第一版”，不是 OpenCascade 永久拓扑命名；UV 必须来自当前修订的 OpenCascade 精确解析，任何局部修改后旧 `triangleIndex` 和旧 UV 上下文都必须重新选择。

### 25. 曲面圆形特征执行前的三维预览（已实现）

- 第一版只为非平面稳定面上的 `add-cylinder` 和 `cut-cylinder` 创建确定性客户端预览；平面圆形特征、矩形、槽孔、整面偏移、圆角和倒角继续按原流程执行，不混入本阶段预览协议。
- 预览直接保留通过前端协议校验的完整局部特征请求，并绑定当前 `selectionRevision + partId + stableFaceId + surfaceUv`、OpenCascade 真实投影点、真实外法线、半径和深度；不会使用选择网格近似点替代精确曲面命中。
- 预览绘制在目标 `LoadedCadMesh` 的同一零件组中，点击点和法线复用与 CAD STL 相同的 OpenCascade 到 Three.js 坐标变换，因此装配视图和拆分视图中的位置、旋转与零件偏移保持一致。
- Three.js 半透明圆柱沿真实法线定向：圆形凸台沿外法线使用绿色显示，圆孔沿内法线使用红色显示；切除体关闭深度测试和深度写入，确保位于实体内部时仍可辨认。仅用于避免表面闪烁的显示偏移不会写入 Worker 请求。
- 生命周期为 `ready / executing / failed`，界面对应全中文“准备自动执行 / 正在自动执行 / 执行失败，预览已保留”。预览生成后只等待一个浏览器任务周期便自动保存修改前快照并调用 Worker，不新增人工确认或阻塞步骤。
- Worker 成功后清除预览并刷新 CAD 修订；Worker 失败时保留原模型、当前曲面选择、真实 UV 和失败预览，记录中文错误并允许用户调整尺寸后重试。
- 重新选择或关闭选择模式、修改参数、恢复历史版本、切换模型来源、新建或重新生成模型、导入/清除 STL、拆件导致选择失效，以及任何成功生成的新修订都会清除旧预览，避免把过期预览附着到错误零件或稳定面。
- 当前预览是基于已验证结构化请求生成的确定性客户端几何指示，不是 OpenCascade 最终布尔结果，也不证明作用区域一定能通过曲率、裁剪边界、壁厚、相邻面干涉或实体有效性校验；最终结果仍以 Worker 校验后的 STEP/STL/3MF 为准。

2026-07-20 本阶段新增自动化覆盖：只为曲面圆形特征创建预览、中文尺寸描述、修订/零件/稳定面/真实点/法线/UV 绑定、执行期间状态、成功后清除、失败后保留、重新选择/关闭选择模式/参数修改时失效。完整指定回归通过：前端 17 个测试文件 110/110，Rust 28/28，曲面圆形核心 13/13，稳定 CAD Worker 6/6，参数化局部特征安全重放 11/11，曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check` 和 Modeling Python 语法编译均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

第 25 阶段当时的能力边界：本阶段没有实现 OpenCascade 最终布尔形状的执行前网格预演，也没有新增曲面矩形、槽孔、任意曲面贴合轮廓、曲面所属边特征或自由曲面直接编辑；其中受限曲面槽孔已在第 28 阶段实现，曲面所属单边圆角/倒角已在第 34 阶段实现。稳定面仍是“几何签名匹配第一版”，`triangleIndex` 和曲面 UV 上下文仍只对当前修订有效。

### 26. 曲面圆形作用区域的自交和相邻面干涉检查（已实现）

- OpenCascade Worker 在执行曲面 `add-cylinder` 或 `cut-cylinder` 的布尔运算和任何 STEP/STL/3MF 文件写回前，逐面计算圆形工具体与修改前源实体稳定面的真实交集，不再只依赖布尔后的有效性和体积结果发现风险。
- 工具体在根部允许范围之外再次接触目标稳定面时，判定为目标曲面自交或意外回撞；工具体在有效深度范围内接触非目标稳定面时，判定为相邻面干涉，并在中文错误中报告最近接触距离、干涉面数量和稳定面 ID 摘要。
- 曲面通孔根据点击位置真实内法线得到的局部壁厚放行预计出口附近的正常接触；曲面盲孔不会使用这一出口豁免，因此圆形轮廓边缘提前撞到内部孔、筋位或其他面时仍会被拒绝。
- 成功结果新增 `interferenceCheckPassed`、自交/相邻面标记、干涉面数量与 ID、最近干涉距离，以及接触面和接触采样数量。平面特征返回“不适用”诊断，不改变既有平面执行行为。
- 干涉检查失败会抛出包含“曲面作用区域干涉检查未通过”的中文错误。Store 保留修改前 CAD、当前稳定面选择、真实曲面 UV 和三维工具预览，并把预览生命周期标记为 `blocked`；视口使用橙色工具体和“检测到曲面干涉，已阻止执行”风险卡片，用户可以清除预览或调整半径、深度和点击位置后重试。
- 普通 Worker 错误仍使用 `failed` 红色状态，避免把裁剪边界、曲率、壁厚、文件或环境错误错误归类为已经确认的几何干涉。
- 成功中文摘要显示检查的接触面数和接触采样数，并明确说明未发现目标曲面自交或非目标面干涉。

2026-07-21 本阶段新增自动化覆盖：曲面核心 16/16，新增中空圆柱内壁长凸台回撞、外部悬臂相邻面干涉、中心壁厚射线无法发现但圆形工具边缘会撞到的内部偏置孔，以及普通通孔出口不误判；稳定 CAD Worker 6/6，验证新诊断字段透传和持久化；前端 17 个测试文件 111/111，覆盖成功中文摘要、`blocked` 风险预览，以及普通错误继续保持 `failed`。

2026-07-21 完整回归通过：曲面圆形核心 16/16、稳定 CAD Worker 6/6、参数化局部特征安全重放 11/11、前端 17 个测试文件 111/111、Rust 28/28；TypeScript/Vite 生产构建、`cargo check`、Rust 测试和全部 Modeling Python 语法编译均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

能力边界：本阶段是工具体与源实体边界的几何接触检查，不是有限元、受力、疲劳或打印方向分析，也不能证明任意自由曲面操作都安全；根部和通孔出口使用基于曲率、半径及局部壁厚的保守允许区。稳定面仍是“几何签名匹配第一版”，不是永久拓扑命名，`triangleIndex` 和曲面 UV 仍只对当前修订有效。

### 27. 曲面局部特征参数历史对比（已实现）

- OpenCascade Worker 只为非平面稳定面上的圆形凸台和圆孔保存 `curvedDiagnostics`，内容包括最大绝对曲率、最小曲率半径、曲率比、局部壁厚、剩余壁厚、通孔状态、自交与相邻面干涉、干涉稳定面 ID、最近干涉距离，以及接触面和接触采样数量；平面或非圆形特征不会误写曲面诊断。
- 参数化整模重建不直接复用历史诊断，而是在每次安全重放后根据当前 OpenCascade 几何重新计算；旧记录没有 `curvedDiagnostics` 时会自动补齐，旧记录包含过期或错误诊断时会被当前结果替换。
- 每次提交模型版本时深拷贝当前曲面特征诊断，并使用“创建修订 + 零件 ID + 操作类型”形成跨参数化重放的特征标识，避免后续 CAD 结果原地变化污染历史快照。
- 版本历史的中文差异界面新增“曲面局部特征变化”，支持识别新增、删除和修改，并逐项显示工具半径/直径、作用深度、中文曲面类型、曲率、壁厚、通孔、干涉稳定面和接触采样的前后值。
- 老版本没有曲面特征快照时按空列表兼容，不影响原有参数、开孔模式和通用开孔差异对比。

2026-07-21 本阶段新增自动化覆盖：Worker 诊断持久化到 `generation-result.json`、错误历史诊断重算、旧记录自动补齐、Store 版本快照深拷贝，以及曲面特征相同、新增、删除、尺寸变化、曲率/壁厚变化、盲孔转通孔、干涉诊断变化和旧版本兼容。

2026-07-21 完整回归通过：曲面圆形核心 17/17、稳定 CAD Worker 6/6、参数化局部特征安全重放 11/11、前端 17 个测试文件 115/115、Rust 28/28；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、全部 Modeling Python 语法编译和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

能力边界：本阶段比较的是已保存的非平面圆形局部特征记录和诊断快照，不是两个任意网格或 B-Rep 的通用曲面差分；稳定面仍是“几何签名匹配第一版”，不是永久拓扑命名，`triangleIndex` 和曲面 UV 仍只对当前修订有效。

### 28. 非平面稳定面上的曲面槽孔第一版（已实现）

- 通用稳定面协议新增非平面 `cut-slot`，适用于各种参数化 CAD 模型，不绑定 ESP32 或任何固定 Demo。槽孔使用宽度、总长度、切入深度和旋转角，圆形半径与矩形高度字段必须为空。
- OpenCascade 在当前修订解析出的真实 `surfaceUv` 点击位置建立切平面，使用长圆形 `slot2D` 轮廓并沿真实内法线切削；旋转角围绕真实法线生效，结果继续要求有效、封闭、单一 Solid 且体积减少。
- 槽孔总长度的一半作为保守包络半径，用于曲率比、真实裁剪面边界、根部允许区、局部壁厚、剩余壁厚、通孔出口、自交和相邻稳定面干涉检查；不会只按槽宽低估曲面上的作用范围。
- TypeScript、Vite、Tauri/Rust、Python Worker 与 Codex 受限 JSON 使用一致白名单。Codex 不得输出或执行 Python、CadQuery、Shell，也不得切换当前 `partId + stableFaceId` 或伪造中心、法线和曲面 UV。
- Three.js 在目标零件上生成宽度、长度、深度和旋转角一致的半透明长圆形三维预览，沿真实内法线显示；正常切削为红色，已确认干涉风险为橙色，普通失败保留红色预览，成功后清除。
- `localFeatures[]` 持久化槽孔宽度、长度、深度、旋转角和 `curvedDiagnostics`。参数化整模重建使用同一几何核心按当前 OpenCascade 几何重新计算安全诊断，旧记录中的缺失或错误诊断不会直接复用。
- 版本历史深拷贝曲面槽孔快照，并在全中文差异界面显示“曲面槽孔”、槽孔宽度、槽孔长度、旋转角、作用深度以及曲率、壁厚、通孔和干涉诊断变化。
- 自动化覆盖曲面槽孔成功切削、体积方向、封闭单 Solid、长度一半包络的曲率拒绝、壁厚和干涉诊断、Worker 持久化、参数化安全重放、客户端中文预览、结构化请求、Store 版本快照深拷贝及版本差异。

2026-07-21 完整回归通过：前端 17 个测试文件 118/118、Rust 28/28、稳定 CAD Worker 8/8、曲面局部特征 20/20、参数化局部特征安全重放 11/11、曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、全部 Modeling Python 语法编译和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

能力边界：曲面槽孔第一版是在真实 UV 点击位置的切平面安全近似，不是沿任意曲面贴合或测地线槽孔，也不代表支持任意自由曲面轮廓。稳定面仍是“几何签名匹配第一版”，不是永久拓扑命名；`triangleIndex` 和曲面 UV 只对当前修订有效，任何模型重建或局部修改后都必须重新选择。

**下一阶段：**实现曲面轮廓方向与 UV 切向控制的安全扩展，并评估非平面矩形局部特征；继续使用受限轮廓、保守包络和 OpenCascade 实体校验，不直接宣称支持任意自由曲面贴合。

### 29. 曲面槽孔真实 UV 切向方向控制（已实现）

- OpenCascade 曲面精确命中结果新增当前真实 `surfaceUv` 位置的单位 U 切向；U 切向不可计算、非有限或退化时使用中文错误拒绝，不使用选择网格或固定世界轴伪造方向。
- 非平面槽孔的 `rotationDeg=0` 明确定义为沿真实 U 切向，正旋转角围绕真实外法线。Python Worker 会在当前 STEP 和 UV 上重新计算真实 U 切向，并校验请求切向与当前切向点积不低于安全阈值，避免过期、反向或错误修订的方向进入布尔运算。
- TypeScript、Vite 本机路由、Tauri/Rust 和 Python CLI 使用一致的三分量协议：三个切向分量必须全部存在或全部为空；新执行的曲面槽孔必须携带有效切向，Codex 受限 JSON 不新增可编辑切向字段，也不得改写 OpenCascade 提供的方向。
- Three.js 曲面槽孔预览把真实 U 切向投影到点击位置切平面，使用与 OpenCascade 一致的局部坐标基准显示宽度、总长度、深度和旋转角；切削方向仍沿真实内法线。
- `localFeatures[]` 持久化 `surfaceTangentU`。参数化重放会按当前几何重新计算并写回切向；升级前没有该字段的旧记录可安全重算，携带过期或反向切向的记录会被拒绝。
- 版本快照深拷贝曲面 U 切向，全中文版本差异新增“曲面 U 切向”，使用数值容差识别方向变化，不把对象引用变化误报为模型变化。
- 自动化覆盖真实命中切向的单位长度与法线正交、Worker 持久化、反向或过期切向拒绝、旧记录重放补写、结构化请求、客户端预览和版本差异。

2026-07-21 完整回归通过：前端 17 个测试文件 118/118、Rust 28/28、稳定 CAD Worker 8/8、曲面局部特征 21/21、参数化局部特征安全重放 11/11、曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、全部 Modeling Python 语法编译和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

能力边界：U 切向、UV 和 `triangleIndex` 都只对当前修订有效；该槽孔仍是在真实 UV 点击位置建立的切平面安全近似，不是沿任意曲面贴合的轮廓，也不是测地线方向。稳定面仍只能称为“几何签名匹配第一版”，不能宣称永久拓扑命名。

**下一阶段：**实现非平面矩形凸台和矩形孔的安全包络与真实 U 切向方向语义；继续使用点击位置切平面近似、半对角线保守包络和 OpenCascade 实体校验。

### 30. 非平面矩形凸台与矩形孔安全执行（已实现）

- 非平面稳定面白名单新增 `add-rectangle` 和 `cut-rectangle`；矩形位于当前修订真实 UV 点击位置的切平面，`rotationDeg=0` 沿真实 U 切向，正角度围绕真实外法线。
- OpenCascade Worker 在当前 STEP 中重新定位稳定面，根据 UV 重新计算真实 U 切向，并校验前端上下文中的切向未过期、反向或退化；Codex 受限 JSON 不新增切向字段，也不得生成、伪造或改写该方向。
- 矩形使用半对角线 `sqrt(width² + height²) / 2` 作为曲率、裁剪边界、局部壁厚、剩余壁厚、通孔出口、自交和相邻稳定面干涉检查的保守包络半径。
- Three.js 使用与 OpenCascade 相同的真实 U 切向、外法线和旋转角显示矩形凸台或矩形孔预览；TypeScript、Vite 本机路由、Tauri/Rust 和 Python Worker 使用一致的操作白名单、尺寸字段与切向上下文校验。
- `localFeatures[]` 持久化矩形宽度、高度、深度、旋转角、真实 U 切向和 `curvedDiagnostics`；参数化重放按当前几何重新计算 U 切向和诊断，兼容旧记录缺少切向，拒绝不一致或反向切向。
- 全中文版本历史新增“曲面矩形凸台”和“曲面矩形孔”，并比较轮廓宽度、矩形高度、旋转角、曲面 U 切向、作用深度及全部曲面安全诊断。
- 自动化覆盖矩形凸台和矩形孔体积方向、半对角线包络、曲率拒绝、Worker 持久化、反向切向拒绝、旧记录重放补写、客户端中文解析与预览、Codex 协议校验和版本差异。

能力边界：曲面矩形是在真实 UV 点击位置建立的切平面安全近似，不是沿任意曲面贴合的矩形，也不是测地线轮廓。稳定面仍是“几何签名匹配第一版”，不能宣称永久拓扑命名；`triangleIndex`、曲面 UV 和真实 U 切向只对当前修订有效，任何模型重建或局部修改后都必须重新选择。

2026-07-21 完整回归通过：前端 17 个测试文件 119/119、Rust 28/28、稳定 CAD Worker 9/9、曲面局部特征 23/23、参数化局部特征安全重放 11/11、曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、全部 Modeling Python 语法编译和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

**下一阶段：**评估非平面局部特征的执行前精确工具体预演和更细粒度干涉反馈；继续保持受限 JSON、保守包络与 OpenCascade 单 Solid 校验，不扩展为任意自由曲面贴合建模。

### 31. OpenCascade 精确工具体预演与结构化曲面干涉反馈（已实现）

- 非平面 `add-cylinder`、`cut-cylinder`、`add-rectangle`、`cut-rectangle` 和 `cut-slot` 在正式修改前新增独立预检阶段。Python Worker 使用与正式布尔相同的当前 STEP、稳定面定位、真实 UV、外法线、真实 U 切向、曲率、壁厚、裁剪边界和干涉算法创建工具体，不接受客户端 Three.js 参数近似替代精确几何。
- 预检导出 `local-cad-feature-tool-preview.stl` 和 `local-cad-feature-preflight-result.json`。视口显示的是最终布尔实际使用的 OpenCascade 工具体 STL；它只表示加料或切削的作用体，不是修改后的最终模型，也不能替代正式布尔结果校验。
- 自动执行顺序固定为“生成精确工具体并预检 → 显示一个视图帧 → 保存修改前快照 → 正式执行 Worker”。只有 `status=ok` 才允许进入快照和写入阶段；`status=blocked` 或预检异常均立即停止，不创建版本、不调用正式 Worker、不覆盖当前 STEP/STL/3MF/清单。
- `blocked` 仍保留精确工具体用于查看，并返回结构化诊断：是否再次接触目标曲面、是否检测到目标曲面自交、是否碰到非目标稳定面、干涉稳定面 ID、最近干涉距离、接触面数、接触采样数，以及工具体有效性、封闭性、Solid 数、体积和毫米包围盒。前端使用全中文状态和错误说明模型未写入。
- 精确工具体与预检结果严格绑定当前 `revision + partId + stableFaceId + surfaceUv + surfaceTangentU`。模型修订、零件、选择面、UV 或 U 切向任一变化后，旧预演不能继续显示或执行。
- 平面局部特征和单边圆角/倒角不进入本阶段预检入口；第一版精确工具体预检只面向非平面圆形、矩形和槽孔。Codex 仍只能返回受限 JSON，不允许输出或执行 Python、CadQuery 或 Shell。
- 自动化覆盖预检成功时工作模型和修订不变、真实工具体有效且封闭、曲面干涉 `blocked` 时仍导出工具体但不修改清单、平面预检明确拒绝、前端工具体摘要、阻止正式 Worker、预检异常停止自动执行，以及预检早于快照和正式 Worker 的调用顺序。

能力边界：曲面矩形和槽孔仍是在真实 UV 点击位置建立的切平面安全近似，不是沿任意曲面贴合或测地线生成的轮廓；工具体预演不等于最终布尔结果。稳定面仍是“几何签名匹配第一版”，不是 OpenCascade 永久拓扑命名；`triangleIndex`、曲面 UV 和真实 U 切向都只对当前修订有效。

2026-07-21 完整回归通过：前端 17 个测试文件 120/120、Rust 28/28、稳定 CAD Worker 12/12、曲面局部特征 23/23、参数化局部特征安全重放 11/11、曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、全部 Modeling Python 语法编译和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

**下一阶段：**在不改变受限 JSON 和自动安全门的前提下，增加精确工具体的局部测量、干涉面定位与风险参数调整入口，使用户能从 `blocked` 结果直接定位风险面并修改尺寸后重新预检；继续保持 OpenCascade 单 Solid 校验，不扩展为任意自由曲面贴合建模。

### 32. 精确工具体局部测量、干涉面定位与风险参数调整（已实现）

- 曲面精确预检返回 `blocked` 后，风险卡片直接显示 OpenCascade 工具体体积、毫米包围盒、最近干涉距离、接触面数量和接触采样数量，不再要求用户从聊天文本中手工整理测量结果。
- 视口根据当前修订选择网格中的稳定面三角范围高亮全部干涉面：红色表示预检返回的全部非目标干涉面，亮红色和中文标记表示当前定位面。定位动作只更新风险预览状态，不会覆盖原目标面选择、真实曲面 UV、法线、U 切向或当前 CAD。
- 干涉面映射必须同时匹配当前 `revision`、预检 `revision`、请求 `partId`、预检 `partId` 和选择网格 `partId`；任一上下文过期时不显示风险面，避免把旧预检结果画到新实体上。
- 风险参数面板按特征类型开放直径、宽度、高度、长度、切入深度/凸出高度和旋转角。参数仍使用与 Codex 受限 JSON 相同的范围：圆形半径 0.50–100.00 毫米、矩形/槽孔宽度 0.50–200.00 毫米、槽孔长度 1.00–200.00 毫米且不能小于宽度、矩形高度 0.50–200.00 毫米、深度 0.20–200.00 毫米、旋转角 -180–180 度。
- “重新预检并自动执行”不会绕过既有链路，而是生成受限中文局部特征指令，再次经过确定性解析、当前稳定面和真实 UV 绑定、OpenCascade 单 Solid 工具体校验与精确干涉预检；只有新参数预检通过后才保存修改前快照并执行正式 Worker。
- 风险卡片支持恢复原参数和清除预览；自动执行期间所有风险操作禁用。曲面命令建议和说明已补齐矩形凸台、矩形孔，并修正“曲面矩形仍未实现”的旧提示。

能力边界：工具体测量和干涉面高亮描述的是执行前工具体与当前源实体的关系，不是最终布尔结果，也不是有限元、打印方向或装配公差分析。曲面矩形和槽孔仍是真实 UV 点击位置上的切平面安全近似，不是任意曲面贴合或测地线轮廓；本阶段没有开放自由曲面贴合建模。稳定面仍只能称为“几何签名匹配第一版”，`triangleIndex`、曲面 UV 和 U 切向只对当前修订有效。

2026-07-21 本阶段新增前端纯函数与 Store 自动化覆盖：圆形、矩形、槽孔默认参数恢复和中文指令安全往返；非有限值、尺寸范围、槽长小于宽度和旋转角越界拒绝；`blocked` 后默认定位首个干涉面；非法面 ID 不能改变定位；定位不会改变 CAD、目标面选择或真实 UV。

下一阶段计划：精确预检结果版本化留档、预检前后对比和风险参数自动收敛建议。自动建议必须继续输出受限参数，不得执行 Python、CadQuery、Shell，也不能跳过 OpenCascade 精确预检安全门。

2026-07-21 第 32 阶段完整回归通过：前端 17 个测试文件 125/125、Rust 28/28、稳定 CAD Worker 12/12、曲面局部特征 23/23、参数化局部特征安全重放 11/11、曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、全部 Modeling Python 语法编译和 `git diff --check` 均通过。界面在 1200×818 和 900×650 视口完成风险面板检查，小视口使用面板内滚动；浏览器控制台无错误。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

### 33. 精确预检结果版本化留档、前后对比与风险参数自动收敛建议（已实现）

- 新增独立于 `ModelVersion` 的 `LocalCadFeaturePreflightRecord`。每次曲面精确工具体预检无论通过还是被阻断，都会深拷贝保存源 CAD 修订、受限请求、OpenCascade 结构化结果、时间和执行关联；被阻断的尝试不会伪装成已经写入模型的版本。
- 预检历史只保留最近 50 条，避免桌面会话长期运行时无限增长。正式 Worker 成功后只把对应通过记录关联到新 CAD 修订；Worker 失败时保留“预检通过但未写入模型”的真实状态。
- 恢复模型版本、切换视图或清除风险预览不会删除独立预检档案，也不会把历史中的稳定面、曲面 UV 或请求恢复为当前可执行选择；新建模型画布会清空本项目的预检档案。
- 新增同源修订、同零件、同稳定面和同操作的预检前后比较，覆盖直径、宽高、槽孔长度、深度、旋转角、工具体积、毫米包围盒、最近干涉距离、干涉面、接触面/采样、曲率比、局部/剩余壁厚、通孔、自交和非目标面干涉，并用全中文显示“修改前 → 修改后”。
- 风险面板新增“安全候选参数”：圆形按 10%/20%/30% 缩小直径，矩形同比缩小宽高，槽孔优先缩短长度并保证 `length >= width`，诊断需要时再提供降低深度或 `±5°` 小范围旋转候选。所有候选先通过既有 `validateLocalCadFeatureAdjustment()`，最多显示 5 条。
- 用户点击“应用并重新精确预检”后，候选只会生成受限中文命令并重新进入原有安全链，不会直接修改 CAD，也不会绕过 Codex JSON 校验、稳定面/真实 UV 绑定、参数范围、OpenCascade 精确工具体预检、单一实体检查和正式 Worker。
- 版本历史对话框新增独立的“精确预检记录”折叠区，显示通过/阻断、目标零件与稳定面、参数、工具体积、干涉稳定面、最近干涉距离和正式执行修订；清空预检记录不会删除模型版本或当前 CAD。
- 界面明确提示“该建议只缩小风险参数，不保证最终布尔成功”。曲面矩形和槽孔仍是用户真实 UV 点击位置切平面上的安全近似，不是任意曲面贴合或测地线轮廓；稳定面仍是“几何签名匹配第一版”。

2026-07-21 本阶段新增前端纯函数与 Store 自动化覆盖：预检留档深拷贝、历史裁剪、执行修订关联、同目标查找、阻断到通过比较、参数与工具体测量差异、干涉面增减、圆形/矩形/槽孔候选、安全范围、无法收敛的中文说明、阻断不建模型版本/不建修改前快照/不调用正式 Worker、参数重试生成第二条记录、成功记录关联正式修订、恢复版本不复活旧预检上下文，以及新建画布清空档案。

2026-07-21 完整回归通过：前端 18 个测试文件 134/134，Rust 28/28，曲面命中 7/7，稳定 CAD Worker 12/12，曲面局部特征 23/23，参数化局部特征安全重放 11/11；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、全部 Modeling Python 语法编译和 `git diff --check` 均通过。浏览器实测版本历史对话框可打开“精确预检记录”中文折叠区，控制台无错误。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

### 34. 非平面稳定面所属单边圆角与倒角第一版（已实现）

- “点击选边”不再把所属稳定面限制为 `PLANE`。前端会继续以 `partId + stableFaceId + stableEdgeId` 绑定单条稳定 CAD 边，并显示所属平面、圆柱面等曲面类型；Codex 只允许返回受限的 `fillet-edge` 或 `chamfer-edge` JSON 计划，不得切换目标、执行 Python、CadQuery、Shell 或生成任意代码。
- 非平面所属边必须先完成当前修订的 OpenCascade 精确命中，保留真实三维点击点、真实外法线和有限 `surfaceUv`。Vite 本机 API、Tauri/Rust 后端、TypeScript 请求层和 Python Worker 都继续校验修订、零件、稳定面、稳定边、操作与互斥参数。
- Worker 会从 STEP 重新定位稳定面和稳定边，并在记录 UV 上重新计算真实曲面点与考虑面方向的外法线；点击点到目标边距离、点击点到真实 UV 点距离、点击法线与真实外法线点积任一超出安全阈值都会拒绝修改。
- OpenCascade 只对所选单边执行固定半径圆角或等距倒角。结果必须有效、封闭、仅含一个 Solid 且产生可验证的体积变化；缝边、退化边、半径过大或不适合构造圆角/倒角的边会返回全中文错误并保留修改前模型。
- 文件写回继续使用临时文件、备份与批量原子替换，成功后重新导出 STEP、STL、选择 STL、面映射和通用多零件 3MF，并重新生成稳定面/稳定边描述。稳定面和稳定边只能称为“几何签名匹配第一版”，不是永久拓扑命名。
- `localFeatures[]` 会保存 `surfaceGeometryType`、真实 `surfaceUv`、`targetFace`、`targetEdge` 与稳定 ID。参数化整模重建会重新执行同一条真实 UV、外法线、边距离、有效性、封闭性、单 Solid 和体积变化安全链；缺失或过期 UV、边签名不一致或 OpenCascade 失败都会拒绝整次重建。
- 全中文成功摘要会报告点击点到真实边距离；曲面所属边还会报告真实 UV、点击点到真实 UV 点距离和外法线点积。修改后 `triangleIndex`、曲面 UV 和稳定边选择全部失效，继续修改前必须重新选择。
- 第 34 阶段当时仍严格限制为一条稳定边；第 35 阶段已增加唯一平面边界整圈传播第一版。任意多边链、切线链、多个 Wire、可变半径圆角、圆角连续性控制或永久拓扑命名仍未实现；曲面整面偏移和曲面参数直接编辑也仍未实现。
2026-07-21 第 34 阶段完整回归通过：前端 18 个测试文件 134/134，Rust 28/28，稳定 CAD 单边特征 11/11，参数化局部特征安全重放 13/13，稳定 CAD Worker 12/12，曲面局部特征 23/23，平面局部特征 7/7，曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、全部 Modeling Python 语法编译和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

### 35. 平面边界整圈圆角与倒角第一版（已实现）

- 新增受限操作 `fillet-edge-loop` 与 `chamfer-edge-loop`。用户仍只点击一条种子稳定 CAD 边；确定性中文解析器仅在指令明确包含“整圈、一圈、整周、周边、轮廓边或边界圈”时生成整圈操作，没有整圈关键词时继续生成单边操作。
- Codex 只能返回受限 JSON，不能执行 Python、CadQuery、Shell 或任意代码。桌面 Tauri、浏览器本机 API、TypeScript 请求层和 Python Worker 均校验当前修订、`partId + stableFaceId + stableEdgeId`、0.20–50.00 毫米尺寸、空轮廓字段和零旋转角；Codex 不得切换种子边。
- 整圈第一版只接受 `PLANE` 所属边界。Worker 从当前 STEP 重新定位稳定平面和种子边，在该面的 Wire 中查找包含种子边的唯一边界，过滤退化边，并要求 2–64 条有效边；所有边一次性进入同一个 OpenCascade 圆角或倒角操作。在第 35 阶段交付时，非平面所属边仍只支持单边圆角或倒角；第 36 阶段已补充平面与非平面的唯一切线连续边链传播。
- 结果继续要求有效、封闭、单一 Solid、体积发生符合操作语义的变化，并重新导出 STEP、STL、选择 STL、面映射和通用 3MF。返回值增加 `affectedEdgeCount` 与 `edgeScope`，中文成功摘要会显示实际处理边数。
- `localFeatures[]` 只记录种子稳定边与几何签名快照。参数化重放时重新从当前稳定平面推导唯一 Wire；种子边失效、边界不唯一、边界数量越界或 OpenCascade 构造失败都会终止整次重建并保留最后有效模型。
- 第 35 阶段的中文界面明确区分种子边、平面边界整圈和曲面所属单边；当时尚未支持切线链自动传播。第 36 阶段现已支持唯一切线连续边链第一版，但仍不支持手工多选边链、分叉链、多个 Wire、可变半径、连续性等级控制或永久拓扑命名；稳定面和稳定边只能称为“几何签名匹配第一版”。

2026-07-21 第 35 阶段完整回归通过：前端 18 个测试文件 135/135，Rust 29/29，稳定 CAD 单边/整圈边特征 14/14，参数化局部特征安全重放 14/14，稳定 CAD Worker 12/12，曲面局部特征 23/23，平面局部特征 7/7，曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、全部 Modeling Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。


### 36. 切线连续边链圆角与倒角第一版（已实现）

- 新增 `fillet-edge-chain` 与 `chamfer-edge-chain`。用户只需选择一条种子稳定 CAD 边，并在中文指令中明确写“切线链、相切边、切线连续、连续边链、沿切线或顺着切线”；没有触发词时仍按单边执行。
- “整圈”和“切线链”同时出现时直接拒绝，避免自动猜测传播范围。Codex 仍只能返回受限 JSON，不得切换 `partId`、`stableFaceId` 或 `stableEdgeId`，也不能执行 Python、CadQuery、Shell 或任意代码。
- Worker 从种子边两个端点传播；连接顶点距离容差为 `max(1e-6, min(1e-3, 包围盒对角线 × 1e-7))`，切向夹角最大 5 度。每个端点必须只有一条符合条件的后继边；多个候选视为分叉并拒绝。两个方向都没有后继时拒绝，不能静默降级成单边。
- 最终切线链必须包含 2–64 条非退化边，并一次性交给同一个 OpenCascade 固定半径圆角或等距倒角操作。种子边所属面可以是平面或非平面；非平面仍必须通过真实 UV、真实曲面点、外法线和目标边距离复核。
- Worker 返回 `affectedEdgeCount` 与 `edgeScope=tangent-chain`；界面、状态、成功摘要、版本历史和错误均使用中文，并明确显示实际处理边数与不支持的能力边界。
- `localFeatures[]` 只保存种子稳定边和几何签名快照。参数化整模重建每次从当前实体重新推导切线链，不保存或复用旧边数组；任何重定位、唯一性、数量或 OpenCascade 构造失败都会拒绝整次重建并保留最后有效模型。
- 当前仍不支持手工多选边链、分叉链、可变半径、连续性等级控制、可配置切线阈值或永久拓扑命名；稳定面和稳定边只能称为“几何签名匹配第一版”。

2026-07-21 第 36 阶段完整回归通过：前端 18 个测试文件 136/136，Rust 29/29，稳定 CAD 单边/整圈/切线链边特征 18/18，参数化局部特征安全重放 15/15，曲面局部特征 23/23，平面局部特征 7/7，曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、全部 Modeling 回归、Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。


### 37. 手工多选边链圆角与倒角第一版（已实现）

- 新增 `fillet-edge-manual-chain` 与 `chamfer-edge-manual-chain`，并在全中文视口中提供“多选边链”模式。用户可按顺序选择同一 CAD 零件中的 2–64 条稳定边，选择可跨所属稳定面。
- 每条边必须完成 OpenCascade 精确解析。前端请求携带逐边稳定面 ID、稳定边 ID、真实毫米坐标、外法线、曲面类型和 UV；手工边链不得同时携带单一种子边 ID。
- Codex 仍只能返回受限 JSON，且必须逐项、同序返回用户选择的边列表。增边、删边、换序、替换边、退化为单边操作或普通单边计划夹带手工列表都会在调用 Worker 前拒绝。
- Worker 按真实 OpenCascade 物理边去重并构建端点连通图，只接受无分叉开放链或闭合链；重复物理边、不连续、分叉、退化或数量越界均用中文拒绝。整链一次执行统一半径圆角或统一距离倒角。
- 结果返回 `affectedEdgeCount` 与 `edgeScope=manual-chain`；中文状态、摘要、版本历史和失败提示均已覆盖。成功后清除过期选择，失败时保留修改前模型和当前手工边链。
- `localFeatures[]` 保存逐边稳定 ID、所属面和几何签名快照。参数化重放逐边重新定位当前物理边并刷新快照；稳定 ID 或几何签名失配时原子拒绝，不替换最后有效模型。
- 第一版不支持可变半径、连续性等级控制、分叉边网、多个独立边链或永久拓扑命名；稳定面与稳定边仍属于“几何签名匹配第一版”。

2026-07-21 第 37 阶段完整回归通过：前端 18 个测试文件 141/141，Rust 33/33，稳定 CAD 单边/整圈/切线链/手工边链边特征 22/22，参数化局部特征安全重放 17/17，曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。


### 38. 对象移动、旋转、缩放与颜色调整第一版（已实现）

- 三维视口顶部和右侧属性面板提供全中文“移动 / 旋转 / 缩放”入口。选中普通 CAD 零件、快速预览零件、上传 STL、制造拆件或参考元件后，可使用 Drei `TransformControls` 三维操控器，也可精确输入 X/Y/Z 毫米位置、X/Y/Z 角度和缩放值。
- 第一版只允许 0.05–20 倍均匀缩放，位置限制为 ±1000 毫米、旋转限制为 ±36000 度，避免单轴拉伸造成打印尺寸失真。颜色严格使用 `#RRGGBB`，所有输入在 Store、历史、持久化和导出前统一规范化。
- 用户真实变换位于零件局部层；装配基础位置和拆分视图临时展开偏移位于外层。切换装配/拆分视图不会丢失用户变换，临时展开距离也不会被错误写入导出文件。
- 三维操控器拖动过程只更新预览，拖动结束后提交一个中文版本；精确输入按一次编辑提交一个版本。历史版本以 `geometry / presentation` 区分几何修改和纯显示修改：撤销和重做会恢复位置、旋转、缩放和颜色，纯显示变换不会把 OpenCascade CAD 错误标记为需要重建，跨过局部 CAD 特征等几何版本时仍会要求重建。新建画布会清除对象显示状态，本机会话使用 `formai-object-presentations-v1` 持久化。
- 稳定 CAD 面点击继续通过 Three.js `worldToLocal` 返回零件原始局部毫米坐标，框选通过 `localToWorld` 反映用户变换；因此旋转、移动或缩放后仍沿用现有稳定面精确解析协议，不把显示坐标伪装成 OpenCascade 原始坐标。
- 新增 `export_transformed_model` 桌面命令与 `modeling/export_transformed_model.py`。源 STL 先由 OpenCascade Z 向上坐标转换为视口坐标，再按 Three.js Euler XYZ 顺序应用均匀缩放、旋转、用户位移和装配基础位置，最后转换回源坐标并重新计算三角面法线。
- 单零件 STL 会真正烘焙视口变换；完整装配 3MF 保存多个独立对象、毫米单位、中文对象名称、基础颜色及主体/上盖装配位置。3MF 是 Bambu Studio 可读取的标准几何 3MF，暂不伪装为包含拓竹专有切片配置、耗材、支撑和摆盘元数据的工程文件。
- STEP 保留参数化 OpenCascade 原始坐标，不应用视口显示变换；导出菜单明确标注“参数化原始坐标”。拆分视图的临时展开距离不进入 STL/3MF，制造拆件各自的用户变换可以进入对应 STL。CAD 拆件复用源 CAD 零件 ID，上传 STL 拆件使用正负侧独立 ID，导出时按来源读取与视口一致的对象状态。
- Rust 和 Python 双层限制输出文件名、源文件清单、对象数、三角面总量、颜色、位置、旋转与缩放；Worker 使用临时文件、输出重读验证和原子替换，Rust 再复制到 macOS 下载目录，禁止任意路径访问。

2026-07-21 第 38 阶段完整回归通过：前端 21 个测试文件 156/156，Rust 35/35，变换导出 Python 3/3；TypeScript、Vite 生产构建、Cargo 检查与测试、全部 Modeling Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。浏览器验收确认精确变换生成版本、三维操控器可启用、装配/拆分入口和中文导出边界可见，控制台无错误。仅保留既有 Vite 大分块警告。

### 39. 任意上传 STL 的顶点、边、面手工位移第一版（已实现）

- 该能力面向任意用户上传 STL，不绑定 ESP32、开发板或固定模型名称。只有当前视图正在显示原始上传模型、没有显示拆件结果且编辑模式不是关闭时，视口才接收网格元素选择。
- 全中文面板支持“选择顶点 / 选择边 / 选择面”、源模型 X/Y/Z 毫米位移、应用、取消选择和退出编辑。顶点显示黄色球体，边显示黄色粗线，面显示半透明黄色三角形；拆件结果显示期间提供“返回原始上传模型”。
- 选择协议携带 `selectionRevision + triangleIndex + elementIndex + triangleMm`。顶点与边根据当前命中三角面和源坐标点击点选择最近元素；面直接绑定当前三角面。外层对象移动、旋转和缩放通过 `worldToLocal` 消除，不会被烘焙进局部网格位移。
- Worker 读取当前 `imported-model-result.json` 并拒绝过期修订。STL 同一几何顶点可能在不同三角面重复出现，因此使用六位小数源坐标键同步移动全部同坐标副本，而不是只改一个数组位置。
- 请求拒绝 NaN、Infinity、全零位移、非法元素索引和每轴超出 -500 至 500 毫米。修改后先拒绝退化三角面，再重新导入 OpenCascade 并检查封闭性、实体有效性、修改前后 Solid 数量和体积；如果重新导入需要自动修洞或清理，整次编辑拒绝，避免修复流程掩盖非法网格拓扑。
- 工作 STL、STEP、模型清单和 `mesh-element-edit-result.json` 通过临时文件、批量原子替换和失败回滚提交。成功后旧拆件和壁厚分析失效，视口使用新修订重新加载模型；失败保留最后有效模型。
- 参数化 CAD 不走此协议，仍使用稳定面/稳定边局部特征。第 39 阶段不支持拓扑增删、挤出、焊接、分裂、框选多元素和自由雕刻；第 41 阶段仅补齐受限的同类元素框选批量位移。
- 修改前后会保存绑定对应上传模型修订号的本机快照并创建中文版本记录；第 40 阶段已经补齐工作 STL、STEP 与清单的直接恢复，因此网格修改现已接入真实文件级撤销、重做和版本历史恢复。

2026-07-21 第 39 阶段完整回归：前端 23 个测试文件 162/162、Rust 35/35、Python 网格元素编辑 4/4；TypeScript/Vite 生产构建、Cargo 检查与测试、Python 语法编译、Rust 格式和 `git diff --check` 通过。浏览器验收完成任意 STL 上传、中文模式切换、顶点命中和高亮、0.1 毫米安全位移、修订刷新、中文版本结果与控制台无错误。

### 40. 上传网格快照直接恢复与完整撤销/重做第一版（已实现）

- 任意 STL 首次导入固定生成 `imported-model.stl`、`imported-model-working.stl`、`imported-model-working.step` 和 `imported-model-result.json`。原始上传文件与当前工作文件分离，后续局部修改和恢复都以标准工作文件为唯一入口。
- `VersionSnapshot` 新增 `modelSource` 和 `modelRevision`；参数化 CAD 与上传 STL 使用不同快照语义。上传快照只复制固定清单和三个模型文件，版本、清单与调用方预期修订号必须完全一致。
- 新增 `restore_uploaded_model_snapshot` 桌面命令与 `modeling/restore_uploaded_model_snapshot.py`。Rust 只允许恢复当前项目 `artifacts/versions` 下的直接子目录，拒绝任意外部路径、路径穿越、符号链接逃逸、CAD 来源、修订不一致和缺失工作 STEP。
- Worker 使用 OpenCascade 重新检查快照 STL 与 STEP 的有效性、封闭性和 Solid 数量，并复核 STL/STEP 体积以及清单声明。全部检查通过后才通过临时文件、批量原子替换和失败回滚提交，失败时保留最后有效模型。
- `restoreVersion`、`undo` 和 `redo` 改为异步事务。上传模型只有真实文件恢复成功后才移动 `versionIndex`；失败时索引和当前模型不变。恢复成功后设置上传工作清单、切回上传 STL 视图，并清除拆件、壁厚、网格选择、局部修改和版本几何对比。
- 首次导入、网格顶点/边/面位移、上传 STL 局部凸台/圆孔修改和手动保存均创建来源与修订号正确的快照。异步恢复序号阻止旧响应覆盖较新的恢复请求。
- 版本历史使用全中文区分“精确 CAD 快照”“上传模型精确快照”和“上传模型版本缺少精确快照”；上传 STL 快照不能参与参数化 CAD STEP 布尔差集，Web 模式明确拒绝本机精确恢复。
- 当前不支持跨项目导入版本快照、任意外部快照路径和云端版本同步。

2026-07-21 第 40 阶段完整回归：前端 24 个测试文件 165/165、Rust 37/37、上传模型恢复 Worker 3/3、拆件与补面 27/27、网格元素编辑 4/4；TypeScript/Vite 生产构建、Cargo 检查与测试、全部 Modeling Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告。

**下一阶段：**实现任意上传 STL 的框选多顶点、多边和多面批量位移第一版，继续绑定当前修订和源毫米坐标；选择集合必须去重、限制数量、支持清除和高亮，并沿用退化面、封闭性、实体有效性、Solid 数量、体积、受管快照及失败回滚校验，不能扩展为未受约束的自由雕刻。

### 41. 任意上传 STL 框选多元素批量位移第一版（已实现）

- 全中文“网格元素编辑”面板支持顶点、边、面三种同类元素的“点击单选 / 框选多选”，并允许对当前选择集合输入统一 X/Y/Z 源模型毫米位移。
- 框选使用当前摄像机的屏幕投影：顶点投影源顶点、边投影三维端点中点、面投影三维重心；投影深度必须位于 `[-1, 1]`。第一版为穿透框选，可能包含被遮挡区域中的元素，界面已明确说明。
- 顶点按六位小数源毫米坐标去重，无向边按排序后的两个源端点去重，面按当前修订 `triangleIndex` 去重。单次最多保留 512 个不同候选，第 513 个候选出现时按网格遍历顺序截断并显示中文安全上限提示。
- 每个元素保存 `triangleIndex + elementIndex + triangleMm`，选择集合保存 `kind + selectionMethod + selectionRevision`。点击单选也统一使用长度为 1 的集合协议；任何模型修订、来源、分析、拆件、恢复或局部修改变化都会清除旧框选请求与过期集合。
- Vite、Tauri/Rust 和 Python Worker 三层独立校验来源、修订、方式、数量、索引、坐标、同类元素和位移；Rust 通过标准输入向 Python 传递选择集合，避免把批量 JSON 放入命令行参数。
- Python 使用 `triangleMm` 与当前工作 STL 逐三角面复核，从所有选中元素生成唯一源坐标键并同步移动全部同坐标 STL 顶点副本；结果继续检查退化面、OpenCascade 封闭性、有效性、Solid 数量、体积以及是否依赖自动修洞或清理。
- 工作 STL、STEP、清单和编辑结果仍通过临时文件、批量原子替换与失败回滚提交。成功后刷新修订、清除旧拆件和壁厚结果，并创建绑定修改前后上传模型精确快照的中文批量移动版本。
- 视口使用合并 `Points`、`LineSegments` 和面 `BufferGeometry` 显示黄色选择集合高亮，避免为每个元素创建独立三维对象；用户可随时清除集合。
- 能力边界仍是受限的同类元素统一位移，不支持拓扑增删、挤出、焊接、分裂或未受约束自由雕刻。

2026-07-21 第 41 阶段完整回归：前端 24 个测试文件 171/171、Rust 42/42、Python 网格元素编辑 7/7、上传模型恢复 3/3、拆件与补面 27/27；TypeScript/Vite 生产构建、Cargo 检查、全部 Modeling Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。浏览器实测任意上传 STL 的 8 个顶点、18 条无向边和 12 个三角面框选；8 个顶点统一沿 Z 轴移动 0.1 毫米后，版本从 2/2 更新到 3/3，体积保持 3200.00 立方毫米，选择随新修订失效，页面重载后控制台无错误。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

**下一阶段：**实现任意上传 STL 选择集合的统一旋转与均匀缩放第一版。旋转以选择集合唯一源坐标的几何中心为枢轴，按源模型 X/Y/Z 单轴和明确角度执行；缩放使用同一枢轴和受限比例。继续沿用当前修订绑定、`triangleMm` 复核、512 上限、OpenCascade 实体校验、受管快照和失败回滚，不扩展为拓扑增删或未受约束自由雕刻。

### 42. 任意上传 STL 选择集合统一旋转与均匀缩放第一版（已实现）

- 全中文“网格元素编辑”面板在统一位移之外新增统一旋转与均匀缩放，继续支持顶点、边、面以及点击单选和穿透框选多选；操作切换不会伪造新的元素类型或绕过当前修订绑定。
- 旋转与缩放先把集合涉及的源毫米坐标去重，再使用唯一坐标的算术平均值作为几何中心。旋转仅允许源模型 X/Y/Z 单轴、`-180° ～ 180°` 的非零角度；缩放仅允许 `0.25 ～ 4 倍` 且不能等于 1。
- `MeshElementTransformOperation` 在 TypeScript 中使用 `move / rotate / scale` 可辨识联合类型；Vite、Tauri/Rust 和 Python Worker 三层独立校验操作白名单、旋转轴、角度、缩放比例、有限数值和无变化情况。Rust 新增纯函数统一规范化三类参数，缺少旋转轴不再静默默认成 Z 轴。
- Python Worker 继续复核当前修订、512 上限、同类元素和每个元素的 `triangleMm`；旋转或缩放同步作用于所有唯一源坐标及其 STL 顶点副本。单顶点绕自身的旋转和缩放没有实际变化，必须中文拒绝。
- 写回前继续检查退化三角面、OpenCascade 封闭性、实体有效性、Solid 数量、体积和是否依赖自动修洞或清理；工作 STL、STEP、清单和结果文件使用临时文件、批量原子替换和失败回滚。
- 成功后刷新上传模型修订、清除旧网格选择、拆件和壁厚结果，创建中文旋转/缩放版本，并沿用第 40 阶段的桌面端上传模型精确快照、撤销、重做与版本恢复。Web 模式仍不伪造本机精确快照。
- 能力边界是受限的选择集合仿射变换，不支持非均匀缩放、任意轴旋转、手工枢轴、拓扑增删、挤出、焊接、分裂、多 Solid 编辑或未受约束自由雕刻。

2026-07-21 第 42 阶段完整回归：前端 24 个测试文件 173/173、Rust 45/45、Python 网格元素变换 9/9、上传模型快照恢复 3/3、拆件与补面 27/27；TypeScript/Vite 生产构建、Cargo 检查与测试、全部 Modeling Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。浏览器实测 8 个顶点绕 Z 轴旋转 10° 后体积保持 3200.00 立方毫米，再按 0.9 倍均匀缩放后体积为 2332.80 立方毫米；两个操作均生成中文结果和新修订、使旧选择失效，当前 1421 页面控制台无新增错误。Web 开发模式按既定边界拒绝缺少本机精确快照的真实撤销；桌面恢复事务由上传快照自动化测试覆盖。

**下一阶段：**实现参数化 CAD 零件转受管网格分支并复用网格元素编辑第一版。用户必须主动选择当前 CAD 零件并确认“转为网格分支后不再保证参数化特征可编辑”；系统保留原参数化 CAD 分支，把当前修订对应的真实毫米 STL 安全导入独立工作集，使任意 CAD 零件也能使用点击/框选、位移、旋转和均匀缩放。版本历史必须区分参数化分支与网格分支，并支持切回原 CAD；第一版不合并多个零件、不把编辑后的网格伪装回精确 B-Rep，也不开放拓扑增删或未受约束自由雕刻。

### 43. 参数化 CAD 零件转受管网格分支并复用网格元素编辑第一版（已实现）

- 参数化 CAD 视图新增全中文“CAD 转网格分支”卡片，用户可以从当前修订的 `parts[]` 主动选择任意零件；默认优先当前场景选中零件，否则使用清单第一个零件，不绑定主体、上盖、ESP32 或固定模型类别。
- 用户确认文案明确原参数化 CAD 分支会保留、新分支支持顶点/边/面编辑，以及网格修改后不再保证参数化特征可编辑。CAD 未完成精确生成、当前修订无零件或正在创建时按钮保持禁用并显示中文原因。
- Tauri 新增 `create_cad_mesh_branch`，Vite 新增 `POST /api/model/create-cad-mesh-branch`。两端都绑定当前 CAD 修订和目标零件 ID，只接受清单声明的普通 STL 文件名，并拒绝旧修订、未知零件、路径穿越、空文件和 50 MB 以上来源。
- 所选零件真实毫米 STL 复用现有 STL 实体检查链，生成原始 STL、工作 STL、工作 STEP 和模型清单。创建成功后切换到受管上传网格视图，清除旧拆件、壁厚、局部 CAD、框选请求和网格选择，并复用点击/框选、位移、X/Y/Z 单轴旋转和均匀缩放。
- 模型清单的 `branchSource` 保存源 CAD 修订、零件 ID、中文名称和源 STL 文件；版本的 `meshBranchSource` 固化同一来源。网格元素变换、局部 STL 布尔编辑和上传模型快照恢复均增加来源持久化自动化，避免后续编辑把 CAD 派生分支误判为普通上传模型。
- 版本历史全中文区分精确 CAD、普通上传模型和 CAD 网格分支，并显示“来源：参数化 CAD · 零件名”。恢复 CAD 继续走参数重建；恢复 CAD 派生网格继续走受管工作 STL、STEP 和清单事务。Web 模式缺少本机快照时明确拒绝真实恢复，不伪造成功。
- 第一版不合并多个 CAD 零件，不把编辑后的网格伪装回参数化 B-Rep，不开放拓扑增删、焊接、分裂、面挤出、多 Solid 网格编辑或未受约束自由雕刻。

2026-07-22 第 43 阶段完整回归：前端 25 个测试文件 177/177、Rust 47/47、Python 网格元素编辑 10/10、局部 STL 编辑 5/5、上传模型快照恢复 4/4、拆件与补面 27/27；TypeScript/Vite 生产构建、Cargo 检查与测试、全部 Modeling Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。浏览器实测当前 CAD 任意零件下拉、中文确认、真实网格分支创建、62.60 × 32.60 × 12.60 毫米尺寸、CAD 派生网格编辑面板和中文版本，页面控制台无新增警告或错误。

**下一阶段：**实现受管单 Solid 网格的单三角面法向挤出与压入第一版，使普通上传 STL 和 CAD 派生网格都能对当前修订中点击选择的单个三角面执行受限毫米距离的加料或切除。工具体必须由源 `triangleMm`、经实体内外分类确认的真实外法线和受限距离构造，继续执行当前修订复核、OpenCascade 有效性/封闭性/单 Solid/体积方向校验、临时文件与失败回滚、桌面精确快照和 `branchSource` 持久化；第一版不支持框选多面挤出、侧壁斜度、倒角联动、拓扑焊接、任意方向或自由雕刻。

### 44. 受管单 Solid 网格的单三角面法向挤出与压入第一版（已实现）

- 普通上传 STL 与 CAD 派生网格都可进入全中文“法向编辑”。入口固定为面元素和点击单选，只接受当前修订中的一个三角面；框选、多面、顶点和边请求在 Store、Vite、Tauri/Rust 和 Python 多层拒绝。
- 新增受限 `extrude-face` 协议，模式为 `add | cut`，距离为 0.20～100.00 毫米。前端结果显示真实外法线、距离、工具体积、修改前后体积和体积差。
- Worker 从 `triangleMm` 重新计算候选法线，使用 `BRepClass3d_SolidClassifier` 确认实体内外方向，不信任 STL 法线字段；以三角面、真实外法线、受限距离和极小重叠量构造封闭 OpenCascade 三角柱。
- `add` 执行真实布尔并集，`cut` 执行真实布尔切除。结果必须有效、封闭、单一 Solid，并满足体积增加或减少的操作语义；随后导出临时 STEP/STL，再通过通用 STL 导入链复核非流形边、封闭性、Solid 数量、体积误差和包围盒。
- 工作 STL、STEP、上传模型清单和编辑结果继续使用临时文件、批量原子替换、失败回滚和桌面精确快照。新清单保留 `branchSource`，普通上传 STL 与 CAD 派生网格不会混淆来源。
- STL 再导入失败增加“法向编辑导出结果未通过网格检查”中文前缀，避免把布尔后网格问题误报成原始文件损坏。选中三角面触碰锐边或开孔边界时，三角柱布尔可能在 STL 三角化后形成非流形共边；第一版安全拒绝且不覆盖最后有效模型。
- 修复 Vite 本机开发后端无返回值 Worker 的标准输出管道死锁：直接丢弃未使用的标准输出并继续收集标准错误，避免大型 `generation-result.json` 输出填满管道后阻塞 CAD 重建、STL 上传和后续编辑。
- 第一版不支持框选多面挤出、相邻面区域扩展、侧壁斜度、倒角联动、拓扑焊接、任意方向或自由雕刻。

2026-07-22 第 44 阶段完整回归：前端 25 个测试文件 179/179、Rust 49/49、Python 网格元素编辑 13/13、局部 STL 编辑 5/5、上传模型快照恢复 4/4、拆件与补面 27/27；TypeScript/Vite 生产构建、Cargo 检查与测试、全部 Modeling Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。浏览器实测通用 30 × 24 × 12 毫米长方体 STL：沿真实外法线加料 2 毫米，体积从 8640.00 增至 9360.00 立方毫米；重新上传原模型后沿真实内法线压入 2 毫米，体积从 8640.00 减至 7920.00 立方毫米。两次操作均生成中文结果、新修订和版本，并保持有效、封闭、单一 Solid。CAD 派生网格边界三角面导致的非流形 STL 被安全拒绝，最后有效模型未覆盖。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

**下一阶段：**实现受管单 Solid 网格的“点击种子三角面并扩展同一连续共面区域”加料与压入第一版。系统应从当前 `triangleMm` 沿共享无向边扩展法线夹角和面间距离均在受限公差内的连续三角面，构造单一封闭棱柱工具体，避免把一个平面补片拆成互相重叠的多个三角柱；继续保持当前修订复核、最大面数与面积上限、OpenCascade 有效性/封闭性/单 Solid/体积方向、STL 再导入、临时文件、失败回滚、精确快照和 `branchSource`。第一版只做连续共面区域，不扩展到曲面区域、穿越锐边、多区域框选、侧壁斜度、倒角联动或自由雕刻。

### 45. 点击种子三角面并扩展连续共面区域的加料与压入第一版（已实现）

- 普通上传 STL 与 CAD 派生网格仍只需选择当前修订中的一个种子三角面；全中文入口统一为“共面区域”，不绑定 ESP32、外壳或任何固定模型语义。
- Worker 以共享无向边建立邻接，从种子面 BFS 扩展连续共面区域。候选面法线夹角不得超过 0.5°，三个顶点到种子平面的距离不得超过动态公差 `max(0.00001, min(0.02, 模型对角线 × 0.000001))` 毫米，因此不会穿越锐边、曲面或断开的同平面区域。
- 区域最多 20,000 个三角面、200,000 平方毫米；种子三角面自身同样参与上限检查。共享边连接三个及以上三角面、边界不能形成闭环或平面 Face 构造失败时中文拒绝。
- 系统提取区域外环与全部孔洞环，构造一个带孔平面 Face 和一个封闭棱柱工具体，再执行一次 OpenCascade 并集或切除；不逐三角面建立重叠棱柱，原有贯穿孔不会被误封。
- 结果新增受影响三角面数量、区域面积、边界环数量、法线夹角公差和平面距离公差，并继续保留真实外法线、工具体积、修改前后体积与体积差。
- 当前修订复核、OpenCascade 有效性/封闭性/单 Solid/体积方向、STL 再导入、临时文件、批量原子替换、失败回滚、桌面精确快照和 `branchSource` 均继续生效。
- 第一版只做单个连续共面区域，不扩展到曲面区域、穿越锐边、多个种子、多区域框选、侧壁斜度、倒角联动、拓扑焊接、任意方向或自由雕刻。

2026-07-22 第 45 阶段完整回归：前端 25 个测试文件 179/179、Rust 49/49、Python 网格元素编辑 15/15、局部 STL 编辑 5/5、上传模型快照恢复 4/4、拆件与补面 27/27；TypeScript/Vite 生产构建、Cargo 检查与测试、全部 Modeling Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。带孔用例使用 20 × 16 × 10 毫米盒体和顶面中心直径 4 毫米贯穿孔，自动扩展 130 个三角面、识别 2 个边界环、区域面积约 307.44 平方毫米，加料后孔洞保持贯穿。浏览器使用 30 × 24 × 12 毫米通用长方体，点击顶面一个种子三角面后自动扩展 2 个三角面、区域面积 720.00 平方毫米；加料 2 毫米使体积 8640.00 → 10080.00，重新上传原模型后压入 2 毫米使体积 8640.00 → 7200.00，结果均有效、封闭并保持单一 Solid。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

**下一阶段：**实现连续共面区域的执行前选区预览与边界测量第一版。用户点击种子三角面后，视口应按与 Worker 一致的共享无向边、0.5° 法线夹角和动态平面距离规则高亮整个连续共面区域，而不是只高亮种子三角面；面板应在执行布尔前显示预计三角面数量、区域面积、边界环数量和公差，并在模型修订、元素类型、选择方式或操作模式改变时清除预览。预览只能作为交互反馈，后端仍必须独立重新扩展和执行全部安全校验；第一版不预览曲面连续区域、不穿越锐边、不开放多个种子或多区域框选。

### 46. 连续共面区域的执行前选区预览与边界测量第一版（已实现）

- 前端使用与 Worker 一致的共享无向边、0.5° 法线夹角、动态平面距离公差、20,000 面和 200,000 平方毫米上限扩展种子三角面。
- 视口高亮完整区域；中文面板显示预计三角面数量、区域面积、边界环数量和公差。非流形、开口、分叉或退化边界在执行前中文提示。
- 模型修订、元素类型、选择方式、变换操作、选择、导入/恢复和视图变化会清除预览。正式请求仍只发送单一种子面，Worker 独立重新扩展并执行全部安全校验。
- 第一版不预览曲面连续区域、不穿越锐边、不支持多种子或多区域框选。

2026-07-22 第 46 阶段验证：前端 25 个测试文件 185/185，TypeScript/Vite 生产构建和差异检查通过。浏览器从通用 CAD 主体创建网格分支并点击平面区域，执行前显示 264 个三角面、245.50 平方毫米、2 个边界环、0.5° 法线夹角公差和 0.00007 毫米平面距离公差。

**下一阶段：**实现连续共面区域边界环线框叠加与大网格预览性能优化第一版。预览应直接显示外环和孔洞环线框，帮助用户在执行前识别是否误跨区域；同时把全网格拓扑构建缓存到模型修订级别，避免每次点击重复扫描全部三角面。缓存必须随修订、导入、恢复和视图切换失效，并保持 Worker 独立复核、安全上限和全中文错误语义不变。


### 47. 连续共面区域边界环线框与修订级拓扑缓存第一版（已实现）

- `MeshPlanarRegionPreview` 返回源毫米坐标下的全部闭合边界环，且 `boundaryLoopCount` 与实际环数组长度保持一致。
- 视口保留黄色连续区域高亮，并叠加青绿色闭合线框显示外环和孔洞环；第一版不按外环/孔洞语义分别着色或命名。
- 当前模型修订第一次点击种子面时构建三角面索引、共享无向边 owners 和源坐标表；后续种子面选择复用 `MeshPlanarRegionTopology`，避免重复扫描整张网格。
- 缓存只存在于当前视口，不进入 Zustand Store；模型修订、导入、恢复、Three.js 几何、坐标变换或视图来源变化时失效。边界 `BufferGeometry` 随预览主动释放。
- Worker 继续从当前工作 STL 独立扩展区域、识别 Wire 并执行非流形、资源上限、OpenCascade 有效性、封闭性和单 Solid 检查；前端缓存与线框不构成安全边界。
- 单元测试覆盖单外环、带孔双环、同一 topology 多种子复用、不连续三角面索引，以及 iterable/topology 入口一致性。

2026-07-22 第 47 阶段验证：前端 25 个测试文件 187/187；TypeScript/Vite 生产构建和 `git diff --check` 通过；浏览器从通用 CAD 主体创建网格分支，侧面区域显示 2 个三角面、655.20 平方毫米、1 个边界环，带接口开孔区域显示 8 个三角面、194.76 平方毫米、2 个边界环；黄色区域与青绿色外环、孔洞环可见；切换操作会清除线框，重新选择约 41 毫秒出现预览；页面无 Console 错误或警告。仅保留既有 Vite 大分块警告。

**下一阶段：**实现连续共面区域外环/孔洞语义分类与尺寸测量第一版。把闭合环投影到种子平面二维坐标，使用环包含关系而不是数组顺序、三角面绕序或单纯有符号面积方向区分外环与孔洞；面板分别显示外环和孔洞数量，并提供各环周长与平面包围尺寸；视口使用不同中文图例或线型帮助识别。Worker 仍独立判断建模 Wire 语义。第一版不扩展到曲面区域、嵌套岛、多区域框选或自由编辑。


### 48. 连续共面区域外环/孔洞语义分类与尺寸测量第一版（已实现）

- 全部闭合边界环投影到由种子平面法线建立的稳定二维基底，并使用环包含深度分类外环与孔洞；不依赖 STL 三角面绕序、环数组顺序或有符号面积方向。
- `MeshPlanarRegionPreview` 新增外环数量、孔洞数量和可序列化 `boundaryLoops`；每个环保留源毫米点、语义、真实三维周长、平面二维包围尺寸和嵌套深度，同时保留 `boundaryLoopsMm` 兼容字段。
- 面板分别显示外环和孔洞数量，并逐环显示周长与包围尺寸；视口保持黄色区域面，外环改用青绿色线框，孔洞改用珊瑚色线框。
- 检测到嵌套深度大于 1 的岛结构时以中文安全拒绝；第一版不扩展到曲面区域、多区域框选或自由编辑。
- Worker 不读取或信任前端分类，仍独立扩展区域、判断 OpenCascade Wire 语义并执行全部建模安全检查。
- 单元测试覆盖无孔长方形、带孔区域、反转全部三角面绕序后的稳定分类，以及 Store 当前修订失效语义。

2026-07-22 第 48 阶段验证：前端 25 个测试文件 188/188；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器从通用 CAD 主体创建网格分支并以真实鼠标点击带接口开孔区域，显示 8 个三角面、194.76 平方毫米、外环 1 个、孔洞 1 个；外环周长 70.40 毫米、包围 24.60 × 10.60 毫米，孔洞周长 34.00 毫米、包围 11.00 × 6.00 毫米；黄色区域、青绿色外环和珊瑚色孔洞环均可见，正常鼠标交互下页面无 Console 错误。仅保留既有 Vite 大分块警告。

**下一阶段：**实现连续共面区域边界环点击聚焦与视口尺寸标注第一版。用户应能在测量列表中选择任意外环或孔洞，视口只强化当前环，并在模型附近显示该环的周长、平面包围宽度和高度；切换环、选择、操作模式、模型修订或视图来源时聚焦必须安全更新或清除。聚焦与标注只用于查看，不改变 Worker 请求、区域扩展或建模语义。第一版不提供拖动尺寸、直接编辑边界、嵌套岛、曲面标注或自由雕刻。


### 49. 连续共面区域边界环点击聚焦与视口尺寸标注第一版（已实现）

- 测量列表中的每个外环或孔洞使用可访问按钮，支持点击聚焦、点击其他环切换、再次点击当前环取消，并以全中文状态提示“点击定位”或“已聚焦”。
- 聚焦时视口降低其余环亮度，以加粗的青绿色或珊瑚色线条强化当前环，并在模型附近显示环名称、周长、平面包围宽度和高度。
- Store 只接受当前预览中的有效整数索引；设置新预览以及模型修订、导入、恢复、元素种类、选择方式、选择集合、变换操作或视图来源变化时清除聚焦。
- 聚焦状态不持久化、不进入版本快照或 Worker 请求，也不改变区域扩展、Wire 分类和正式建模语义。
- 自动化覆盖有效孔洞索引、越界索引安全回落、新预览重置以及预览失效时同步清除聚焦。

2026-07-22 第 49 阶段验证：前端 25 个测试文件 188/188；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器以真实鼠标分别聚焦带孔区域的孔洞 1 和外环 1：孔洞标注显示周长 34.00 毫米、宽度 11.00 毫米、高度 6.00 毫米，外环标注显示周长 70.40 毫米、宽度 24.60 毫米、高度 10.60 毫米；再次点击当前外环后标注消失，正常交互下页面无 Console 错误。仅保留既有 Vite 大分块警告。

**下一阶段：**实现连续共面区域边界环视口直接拾取与顺序导航第一版。外环和孔洞线框应能直接接收鼠标点击并切换聚焦，面板提供“上一个环/下一个环”在当前预览的全部环中循环定位，同时保留列表直接选择。拾取必须只在当前修订共面区域预览中启用，不能妨碍模型选面或把聚焦索引发送给 Worker。第一版不支持拖动边界、尺寸驱动修改、曲面环或自由编辑。

### 50. 连续共面区域边界环视口直接拾取与顺序导航第一版（已实现）

- 新增 `cycleMeshPlanarRegionLoopIndex` 纯函数：空环列表返回未聚焦，未聚焦时正向进入首环、反向进入末环，合法索引首尾循环，过期或负索引安全回落。
- 全中文预览面板在多环情况下显示“上一个环 / 当前：… / 下一个环”，并继续保留每个外环或孔洞的列表按钮；三种入口共享 Store 中的聚焦索引和 `aria-pressed` 状态。
- `LoadedCadMesh` 把当前修订预览的环统一转换为渲染数据，并继续用聚合 `BufferGeometry` 绘制可见外环和孔洞。每环额外使用透明宽 Drei `Line` 作为射线拾取层，真实命中后停止事件传播并切换或取消聚焦。
- 拾取层只在上传模型、共面区域操作、当前导入修订和有效预览同时成立时存在。点击非线框区域继续进入既有模型选面流程；聚焦索引不进入 Worker、建模请求、项目版本或快照。
- 保留聚合几何显式 `dispose()`、当前修订失效和 Store 越界保护；第一版不实现拖动边界、尺寸驱动修改、曲面环或自由编辑。

2026-07-22 第 50 阶段完整回归：前端 25 个测试文件 190/190；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器实测带孔区域为 8 个三角面、194.76 平方毫米，孔洞 34.00 毫米（11.00 × 6.00 毫米）、外环 70.40 毫米（24.60 × 10.60 毫米）；顺序导航首尾循环且与列表状态同步，精确点击孔洞环可取消聚焦、点击外环可聚焦，点击非线框区域可重新选择 138 个三角面的底面区域，Console 无错误。仅保留既有 Vite 大分块警告。

**下一阶段：**实现连续共面区域尺寸辅助线与标注避让第一版。基于聚焦环在种子平面上的稳定二维基底和包围范围，绘制宽度、高度方向的只读尺寸线、延伸线、端点短线与中文毫米数值；标签应偏移到环外，并按视口投影减少宽高标签互相遮挡或遮住孔洞。继续使用当前修订绑定，不改变 Worker 请求、区域扩展和 OpenCascade Wire 语义。第一版不支持尺寸驱动编辑、拖动辅助线、曲面环、嵌套岛或自由雕刻。

### 51. 连续共面区域尺寸辅助线与标注避让第一版（已实现）

- [x] 为每个 `MeshPlanarRegionBoundaryLoop` 保存稳定源毫米测量基底及二维 U/V 包围范围。
- [x] 新增纯函数，生成宽高主尺寸线、延伸线、端点短线和三个轮廓外标签锚点。
- [x] 在 `LoadedCadMesh` 统一转换辅助线坐标，按外环/孔洞语义色渲染只读线组。
- [x] 将宽度、高度拆成独立中文毫米标签，并把摘要卡移到第三个外侧方向且只保留名称与周长。
- [x] 保持当前修订绑定、聚焦切换、透明环拾取和非线框选面行为，不修改 Worker 与版本语义。
- [x] 覆盖 30 × 24 毫米长方形和 2 × 2 毫米小孔的长度、外侧方向、最小偏移、非零线段和有限坐标测试。

2026-07-22 第 51 阶段完整回归：前端 25 个测试文件 190/190；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器带孔区域为 8 个三角面、194.76 平方毫米；外环尺寸标签为 24.60 × 10.60 毫米、周长 70.40 毫米，孔洞标签为 11.00 × 6.00 毫米、周长 34.00 毫米。点击当前孔洞环可取消尺寸标注，非线框底面仍可生成 138 个三角面的预览，Console 无错误。仅保留既有 Vite 大分块警告。

### 52. 连续共面区域尺寸标注视口自适应翻转第一版（已实现）

- [x] 建立宽度、高度和摘要标签的四组稳定外侧候选，不改变源毫米测量基底和真实尺寸。
- [x] 把候选锚点投影到当前视口，识别顶部工具栏、右侧网格编辑面板和画布边缘安全区。
- [x] 按安全区溢出平方惩罚与标签矩形重叠惩罚选择候选，同分时保持输入顺序以减少抖动。
- [x] 使用 `useFrame` 随相机旋转、缩放、平移和视口变化重新投影，只有候选索引变化时更新组件状态。
- [x] 使用 `Html.calculatePosition` 对最终 DOM 坐标执行屏幕安全区夹紧，补足所有三维候选都靠近固定面板时的显示安全。
- [x] 保持纯显示和当前修订绑定，不进入 Worker、Store 持久状态、项目版本或精确快照。
- [x] 增加布局翻转、安全区溢出、标签重叠、空候选纯函数测试，并完成完整回归和浏览器多角度验收。

2026-07-22 第 52 阶段完整回归：前端 25 个测试文件 192/192；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器多角度旋转时布局索引在候选 0、1、2 之间按区域与相机自动切换，标签均未进入顶部工具栏、右侧面板或画布裁切区；带孔端壁仍为 8 个三角面、194.76 平方毫米，外环 24.60 × 10.60 毫米/周长 70.40 毫米，孔洞 11.00 × 6.00 毫米/周长 34.00 毫米。取消孔洞聚焦后标签全部清除，Console 无错误。仅保留既有 Vite 大分块警告。

### 53. 连续共面区域法向加料/压入工具体视口预演第一版（已实现）

- [x] 基于当前修订共面区域的外环、孔洞和稳定平面基底生成带孔二维轮廓，不依赖边界环数组顺序。
- [x] 根据“向外加料 / 向内压入”和有效毫米距离生成半透明棱柱预演几何，并显示全中文方向与距离提示。
- [x] 操作方向、作用距离、种子面、区域预览、模型修订或视图来源变化时实时更新；参数无效或上下文失效时立即清除。
- [x] 预演工具体和方向线禁用射线拾取，HTML 标签禁用指针事件，不拦截边界环拾取、模型选面或尺寸标签。
- [x] 前端有符号体积只用于修正闭合网格预演方向；后端继续独立扩展区域、确认真实外法线并执行 OpenCascade 全部安全校验。
- [x] 增加带孔轮廓、环顺序无关、正负方向、无效距离和闭合网格绕序修正测试，并完成生产构建与浏览器验收。

2026-07-22 第 53 阶段完整回归：前端 25 个测试文件 193/193；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器带孔端壁仍为 8 个三角面、194.76 平方毫米、外环 1 个和孔洞 1 个；默认显示向外 2.00 毫米工具体，切换后实时显示向内 6.00 毫米工具体，输入 0.1 毫米后预演立即清除并出现中文范围错误。聚焦孔洞后预演、宽高和周长标注同时可见，Console 无错误。仅保留既有 Vite 大分块警告。

第一版不显示布尔后的最终实体，不支持拖动工具体、侧壁斜度、倒角联动、曲面区域、多种子区域或自由雕刻。

### 54. 连续共面区域工具体预演轮廓强化与标签视口避让第一版（已实现）

- [x] 从当前工具体 profile 派生起始端和末端的外环/孔洞闭合线组，使半透明端面和贯穿孔边界在各种相机角度下可辨识。
- [x] 为法向方向线增加明确端点十字语义，并按加料/压入颜色显示，不引入可拖动操控器。
- [x] 将预演中文标签按当前相机投影限制在顶部工具栏、右侧编辑面板和画布边缘安全区之外。
- [x] 所有轮廓、端点和标签禁用鼠标事件，继续允许边界环拾取、模型选面和尺寸标签交互。
- [x] 增加线组有限坐标、起止端法向距离、孔洞保留、退化输入拒绝和安全区夹紧测试，并完成生产构建与浏览器多角度验收。

2026-07-22 第 54 阶段完整回归：前端 25 个测试文件 193/193；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器带孔端壁为 8 个三角面、194.76 平方毫米、外环 1 个、孔洞 1 个；向外加料 2.00 毫米与向内压入 6.00 毫米均显示起止端外环/孔洞轮廓和方向末端十字。多角度旋转时中文标签未进入顶部工具栏、右侧面板或画布裁切区，聚焦孔洞后尺寸标注与预演同时可见，Console 无错误。仅保留既有 Vite 大分块警告。

第一版继续只显示工具体，不计算或宣称近似布尔最终实体，不支持拖动、斜度、倒角联动、曲面区域或自由雕刻，也不修改 Worker 和版本语义。

### 55. 连续共面区域工具体起止端侧边连接线与遮挡层级强化第一版（已实现）

- [x] 从每个外环和孔洞的对应起止端唯一顶点派生侧边连接线，使半透明棱柱侧壁走向在各种相机角度下可辨识。
- [x] 外环连接线使用主要加料/压入语义层级，孔洞连接线使用较浅透明层级；保持现有起止端轮廓与方向端点语义。
- [x] 所有连接线禁用射线拾取、深度测试和深度写入，不拦截边界环拾取、模型选面、尺寸标签或相机交互。
- [x] 增加连接线数量、有限坐标、对应端点和法向距离测试，并覆盖带任意数量孔洞的通用 profile，不写死 Demo 模型。
- [x] 完成生产构建和浏览器多角度验收，确认连接线在起止端轮廓与半透明工具体同时显示时层级清晰且 Console 无错误。

2026-07-22 第 55 阶段完整回归：针对性测试 20/20，前端 25 个测试文件 193/193；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器带孔端壁为 8 个三角面、194.76 平方毫米、外环 1 个、孔洞 1 个；向外加料 2.00 毫米与向内压入 6.00 毫米均显示外环/孔洞侧边连接线。多角度旋转后线框层级清晰，预演标签保持在安全区，Console 无错误。仅保留既有 Vite 大分块警告。

第一版仍只强化工具体线框表达，不计算或宣称近似布尔最终实体，不支持拖动、侧壁斜度、倒角联动、曲面区域、多种子区域或自由雕刻，不修改 Worker、项目版本或精确快照。

### 56. 连续共面区域工具体面积与体积只读摘要第一版（已实现）

- [x] 以二维环面积计算唯一外环面积、任意孔洞总面积和净作用面积，结果使用平方毫米并拒绝非有限或非正净面积。
- [x] 使用净作用面积乘正作用距离计算理论工具体体积，结果使用立方毫米并明确标注“工具体估算”。
- [x] 在网格编辑面板和视口中文标签中同步显示净面积与估算体积，不把结果描述为布尔后的模型净体积变化或打印材料精确用量。
- [x] 操作方向、距离、区域、模型修订或视图来源变化时实时更新或清除摘要，不新增 Worker 请求、持久状态、版本字段和快照迁移。
- [x] 增加外环、单孔、多孔、反向绕序、非有限点、孔洞面积超过外环和无效距离测试，完成生产构建与浏览器验收。

2026-07-22 第 56 阶段完整回归：针对性测试 20/20，前端 25 个测试文件 193/193；TypeScript/Vite 生产构建和 `git diff --check` 通过。自动测试覆盖 100 平方毫米外环、4 平方毫米单孔、额外 0.5 平方毫米三角孔、反向绕序、非有限点、少点环、零面积、孔洞面积不小于外环和无效距离。浏览器使用 50.00 平方毫米通用共面区域验证向外 2.00 毫米估算 100.00 立方毫米、向内 6.00 毫米估算 300.00 立方毫米；面板与视口同步，0.10 毫米立即清除摘要并显示中文范围错误，标签位于安全区，Console 无错误。仅保留既有 Vite 大分块警告。

第一版只提供执行前理论工具体度量；真实加料/压入结果仍由桌面 Worker 执行 OpenCascade 布尔并重新测量，不支持材料用量、切片时间、支撑或自由雕刻估算。

### 57. 连续共面区域执行结果体积对照卡第一版（已实现）

- [x] 复用成功结果中的 OpenCascade 工具体积和模型体积变化，派生体积变化绝对值及相对工具体的作用比例。
- [x] 仅在当前上传模型修订对应的连续共面加料/压入结果上显示只读中文对照卡，其他网格编辑、过期修订或无结果时清除。
- [x] 明确说明加料工具体与已有实体重叠、压入工具体超出实体厚度时，实际模型体积变化可以小于工具体积；不得描述为材料利用率或打印耗材。
- [x] 对缺失、非有限、零工具体积和异常比例安全降级，不新增 Worker 字段、持久状态、项目版本或快照迁移。
- [x] 增加加料、压入、过期修订、无效数值和比例格式测试，完成生产构建与浏览器验收。

2026-07-22 第 57 阶段完整回归：针对性测试 24/24，前端 25 个测试文件 197/197；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器从通用 CAD 主体创建网格分支，在 50.00 平方毫米共面区域执行向外 2.00 毫米加料；OpenCascade 返回工具体积 103.58 立方毫米、模型体积变化 100.00 立方毫米，对照卡显示 96.54%，中文重叠说明与语义色正确，Console 无错误。仅保留既有 Vite 大分块警告。

第一版只对照单次成功执行结果，不提供材料估算、切片耗时、支撑分析、累计历史曲线或布尔差异几何。

### 58. 连续共面区域执行前平面估算与 OpenCascade 工具体积偏差对照第一版（已实现）

- [x] 从当前修订的成功 `extrude-face` 结果复用区域面积和作用距离，派生执行前平面理论体积。
- [x] 将平面理论体积与 OpenCascade 实际工具体积对照，显示绝对偏差、偏差方向和相对理论值百分比。
- [x] 对缺失、非有限、非正面积或距离、异常偏差安全降级；不得把差异描述为材料损耗、耗材误差或打印收缩。
- [x] 在现有结果卡中保持“平面估算 → 实际工具体 → 模型体积变化”的中文几何链路，不新增 Worker、Store、项目版本或快照字段。
- [x] 增加零偏差、正负偏差、过期修订和无效数值测试，完成生产构建与浏览器验收。

2026-07-22 第 58 阶段完整回归：针对性测试 27/27，前端 25 个测试文件 200/200；TypeScript/Vite 生产构建和 `git diff --check` 通过。测试覆盖精确一致、百万分之一内浮点一致、正负偏差、±50% 边界、超过边界、过期修订，以及缺失、非正和非有限面积/距离。浏览器从通用 CAD 主体创建网格分支，在 655.20 平方毫米区域沿真实外法线加料 2.00 毫米；结果卡显示平面估算 1310.40 立方毫米、实际工具体 1357.38 立方毫米、高于 46.98 立方毫米（+3.58%）、模型体积变化 1310.40 立方毫米和实际作用比例 96.54%，两列布局清晰，Console 无错误。仅保留既有 Vite 大分块警告。

第一版只解释单次平面轮廓到 OpenCascade 工具体构造的体积差异，不提供截面误差定位、Wire 可视化差集、材料预测或切片补偿。

### 59. 连续共面区域三段体积链路方向一致性与异常提示第一版（已实现）

- [x] 从当前修订成功 `extrude-face` 结果读取加料/压入模式与有符号模型体积变化，派生 `consistent / inconsistent / unchanged` 方向状态。
- [x] 加料结果只接受正体积变化或受公差约束的零变化，压入结果只接受负体积变化或受公差约束的零变化；模式与方向矛盾时显示全中文安全警告。
- [x] 复用平面估算、工具体构造偏差和实际作用比例，形成“平面轮廓 / 工具体 / 布尔作用”三段只读状态，不新增 Worker、Store、项目版本或快照字段。
- [x] 对过期修订、缺失模式、非有限数值和异常比较结果安全降级；不得把状态描述为材料利用率、耗材误差、打印收缩或切片质量。
- [x] 增加加料、压入、近零变化、方向矛盾和无效输入测试，完成生产构建与真实 Worker 浏览器验收。

2026-07-22 第 59 阶段完整回归：针对性测试 31/31，前端 25 个测试文件 204/204；TypeScript/Vite 生产构建和 `git diff --check` 通过。自动测试覆盖加料增量、压入减量、加料减量、压入增量、正负近零变化、过期修订、缺失模式、非有限值和明显异常比例。浏览器在 655.20 平方毫米区域执行向外加料 2.00 毫米，显示“平面估算已计算 / 高于平面估算 / 加料增量一致”，并保留平面估算 1310.40、工具体 1357.38、模型变化 1310.40 立方毫米、+3.58% 偏差和 96.54% 作用比例；Console 无错误。仅保留既有 Vite 大分块警告。

第一版只复核单次成功结果的几何方向与三段体积链路，不自动回滚结果，不修改 OpenCascade 布尔算法，不提供材料预测、切片分析或累计历史统计。

### 60. 连续共面区域执行结果中文诊断摘要与复制第一版（已实现）

- [x] 新增纯格式化函数，从当前修订成功结果和三份比较对象生成稳定的全中文多行诊断摘要。
- [x] 摘要覆盖操作模式、区域面积、作用距离、平面估算、实际工具体积、工具体偏差、模型有符号体积变化、实际作用比例和方向状态；无效字段使用安全降级而非伪造数值。
- [x] 在结果卡提供“复制几何诊断”入口，仅在用户点击时调用 Clipboard API，并显示全中文成功或失败反馈；受限预览环境在 API 超时或拒绝时只在同一次点击中尝试本地兼容复制。
- [x] 不把摘要写入 Store、项目版本、快照或 Worker，不包含模型文件绝对路径、账号、密钥或其他敏感信息。
- [x] 增加一致、压入、缺失偏差、过期修订、空文本和复制失败测试，完成生产构建与真实浏览器验收。

2026-07-22 第 60 阶段完整回归：针对性测试 36/36，前端 25 个测试文件 209/209；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器在 655.20 平方毫米区域执行向外加料 2.00 毫米后，复制入口显示“已复制几何诊断”，受支持上下文读取到包含平面估算 1310.40、实际工具体积 1357.38、+46.98 立方毫米（+3.58%）、模型变化 +1310.40、作用比例 96.54% 和“加料增量一致”的完整中文十行摘要；全新验收页 Console 无错误。仅保留既有 Vite 大分块警告。

第一版只复制当前单次执行结果的几何诊断文本，不自动发送给 Codex，不上传网络，不保存历史，不提供材料或切片判断。

### 61. 连续共面区域诊断摘要填入 Codex 指令草稿第一版（已实现）

- [x] 在“复制几何诊断”旁增加“交给 Codex 分析”入口，只在用户主动点击时把当前摘要填入本地建模指令输入框。
- [x] 生成稳定的全中文分析请求，包含诊断摘要和“请分析几何链路是否合理并提出下一步修改建议”，不得自动点击执行或调用 Codex/API。
- [x] 输入框已有用户文本时必须保留原内容并追加诊断块，不静默覆盖；同一诊断重复点击不得无限重复插入。
- [x] 当前修订变化、结果失效或摘要不可用时入口立即禁用；不新增 Worker、项目版本、快照、网络上传或敏感字段。
- [x] 增加草稿生成、保留已有文本、重复去重和失效结果测试，完成生产构建与真实浏览器验收。

2026-07-22 第 61 阶段完整回归：针对性测试 40/40，前端 25 个测试文件 213/213；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器从当前 CAD 主体创建网格分支，在 655.20 平方毫米区域沿真实外法线加料 2.00 毫米；已有“确认这个区域不会碰到内部元件”文字被完整保留，后方追加包含 1310.40 / 1357.38 / +1310.40 立方毫米、+46.98 立方毫米（+3.58%）、96.54% 和“加料增量一致”的中文诊断请求。重复点击两次后请求标题仍为 1 处，消息数量保持 3 条，未自动执行；Console 无错误。仅保留既有 Vite 大分块警告。

第一版只把文本放入当前页面的本地指令草稿，用户仍需自行检查并点击“执行建模指令”；不自动提交、不自动建模、不调用远程服务。

### 62. 连续共面区域诊断草稿块识别与局部移除第一版（已实现）

- [x] 在建模指令区显示当前是否包含系统生成的完整几何诊断请求，并提供全中文“移除诊断块”入口。
- [x] 新增纯函数只删除与当前稳定格式完整匹配的诊断块，保留块前后所有用户文字并归一化相邻空行。
- [x] 用户手工修改过诊断块、草稿只含部分诊断或存在歧义时不得模糊删除，必须显示中文安全提示。
- [x] 移除操作只修改当前页面草稿，不调用 Codex/API、Worker、网络，不写入 Store、项目版本或快照，也不自动执行剩余指令。
- [x] 增加完整移除、前后文字保留、已编辑块拒绝、重复块和空草稿测试，完成生产构建与真实浏览器验收。

2026-07-22 第 62 阶段完整回归：针对性测试 45/45，前端 25 个测试文件 218/218；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器从当前 CAD 主体创建网格分支，在 1552.52 平方毫米区域沿真实外法线加料 2.00 毫米，生成平面估算 3105.04、实际工具体积 3216.35、模型变化 +3105.04 立方毫米、+3.58% 偏差和 96.54% 作用比例的中文请求。状态卡识别唯一完整块，真实点击移除后前后两段用户文字完整保留、消息数量保持 3 条且未自动执行；再次注入并手工修改作用比例后进入安全拒绝，按钮禁用。网格编辑面板打开时指令面板自动避让，干净验收页 Console 无错误。仅保留既有 Vite 大分块警告。

第一版只提供本地草稿整理，不实现诊断历史、跨页面恢复、自动重写用户指令或远程同步。

### 63. 连续共面区域诊断草稿块只读摘要与精确定位第一版（已实现）

- [x] 唯一完整诊断块状态卡显示块行数、操作模式和方向状态的只读摘要，不重复保存诊断正文。
- [x] 新增纯函数从唯一完整匹配返回稳定字符起止范围和安全摘要；编辑、残缺、重复或歧义状态不得返回可猜测范围。
- [x] 提供全中文“定位诊断块”入口，用户点击后聚焦建模指令文本框、滚动到块所在位置并精确选中完整块。
- [x] 定位只操作 textarea 选择区，不改写草稿，不调用 Codex/API、Worker 或网络，不写入 Store、项目版本与快照，也不自动执行。
- [x] 增加前后文字、不同换行位置、编辑块拒绝、重复块拒绝和摘要安全降级测试，完成生产构建与真实浏览器验收。

2026-07-22 第 63 阶段完整回归：针对性测试 49/49，前端 25 个测试文件 222/222；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器在真实共面加料结果后生成完整 13 行诊断请求，状态卡显示“操作模式：向外加料 · 方向状态：加料增量一致”。草稿前置 12 行用户文字时，点击定位后文本框获得焦点、滚动到 174 像素，并精确选中字符 148 至 374 的完整请求；消息数量保持 3 条且未自动执行。手工修改方向状态后定位和移除按钮同时禁用，Console 无错误。仅保留既有 Vite 大分块警告。

第一版只定位当前页面唯一完整块，不实现多块选择器、跨页面恢复、诊断历史或自动折叠正文。

### 64. 连续共面区域旧诊断块替换为最新诊断第一版（已实现）

- [x] 当草稿中存在唯一完整的本页旧诊断块时，提供全中文“替换为最新诊断”入口；当前结果与旧块相同时显示无需替换。
- [x] 新增纯函数只用非空最新请求替换唯一完整旧块，保留块前后全部用户文字和原有换行位置，返回 replaced / duplicate / unsafe / invalid。
- [x] 编辑、残缺、重复、冲突或未登记旧块不得被覆盖，必须显示中文安全提示；不得回退到追加第二个块造成歧义。
- [x] 替换后原子更新当前页面草稿与系统块登记，不调用 Codex/API、Worker 或网络，不写入 Store、项目版本与快照，也不自动执行。
- [x] 增加前后文字保留、相同块、编辑块拒绝、重复块拒绝和空最新摘要测试，完成生产构建与真实浏览器验收。

第一版只替换当前页面唯一完整诊断块，不实现多块选择器、诊断历史、跨页面恢复或自动提交。

2026-07-22 第 64 阶段完整回归：针对性测试 53/53，前端 25 个测试文件 226/226；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器先在 1552.52 平方毫米区域沿真实外法线加料 2.00 毫米并注入旧诊断，再以 3.00 毫米生成最新结果；按钮切换为“替换为最新诊断”，点击后前后用户文字完整保留，标题仍只有 1 处，诊断更新为 4657.56 / 4768.87 / +4657.56 立方毫米、+2.39% 和 97.67%，消息数量保持 3 条且未自动执行。手工修改方向状态后再执行 1.00 毫米操作，按钮变为“诊断草稿需手工处理”并禁用，旧文字未被覆盖。Console 无错误，仅保留既有 Vite 大分块警告。

### 65. 连续共面区域旧诊断与最新诊断字段差异摘要第一版（已实现）

- [x] 仅在存在唯一完整本页旧诊断块且当前最新诊断不同的情况下，显示全中文只读差异摘要。
- [x] 新增纯函数从稳定请求中提取操作模式、区域面积、作用距离、平面估算、实际工具体积、构造偏差、模型体积变化、实际作用比例和方向状态。
- [x] 只返回实际变化的有限字段，按固定业务顺序显示“旧值 → 新值”；相同、缺失、编辑、残缺、重复、冲突或未登记状态不伪造差异。
- [x] 差异摘要不保存第二份完整正文，不修改草稿、系统块登记、Store、项目版本或快照，不调用 Codex/API、Worker 或网络，也不自动替换或执行。
- [x] 增加字段提取、固定顺序、相同值过滤、缺失字段和不安全状态测试，完成生产构建与真实浏览器验收。

第一版只显示文本字段差异，不计算几何容差、不做数值重要性排序，也不增加确认弹窗。

2026-07-22 第 65 阶段完整回归：针对性测试 56/56，前端 25 个测试文件 229/229；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器在 1552.52 平方毫米共面区域比较 2.00→3.00 毫米真实加料诊断，按固定顺序显示作用距离、执行前平面估算、实际工具体积、工具体构造偏差、模型体积变化和实际作用比例共 6 项；替换前草稿、前后用户文字与 3 条消息保持不变，替换后差异卡消失。手工修改方向字段后按钮禁用且差异卡隐藏，Console 无错误。仅保留既有 Vite 大分块警告。

### 66. 连续共面区域诊断字段差异按需复制第一版（已实现）

- [x] 仅在当前存在安全可替换的有限差异项时显示全中文“复制差异摘要”入口，差异消失或进入不安全状态时立即隐藏或禁用。
- [x] 新增纯格式化函数，把当前变化项按固定顺序输出为简洁中文标题、变化数量和逐项“字段：旧值 → 新值”，不得包含完整旧诊断或最新诊断正文。
- [x] 剪贴板写入使用可注入异步函数，成功显示“差异摘要已复制，不会自动发送或执行”，失败显示中文权限提示；不得自动替换草稿或调用 Codex/API、Worker、网络和执行链路。
- [x] 复制不写入 Store、页面诊断块登记、项目版本或快照，不持久化剪贴板内容；替换、重新计算或用户编辑后旧反馈必须失效。
- [x] 增加空差异、固定格式、字段顺序、成功、失败和安全状态变化测试，完成生产构建与真实浏览器验收。

第一版只复制文本差异，不增加系统分享面板、文件导出、差异历史、数值重要性排序或自动提交。

2026-07-22 第 66 阶段完整回归：针对性测试 59/59，前端 25 个测试文件 232/232；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器在 1552.52 平方毫米共面区域比较 2.00→3.00 毫米真实加料诊断，复制固定顺序的 6 个变化字段；捕获文本只含差异标题、数量和逐项变化，不含完整请求标题或分析提示。复制不修改旧诊断、前后用户文字和 3 条消息；替换后差异卡与旧反馈同时消失，标题保持 1 处、消息仍为 3 条。干净验收页 Console 无错误，仅保留既有 Vite 大分块警告。

### 67. 连续共面区域诊断差异复制内容预览第一版（已实现）

- [x] 在安全可复制的差异卡中提供全中文“预览复制内容 / 收起复制内容”入口，默认收起，不增加确认弹窗。
- [x] 预览必须直接显示当前 `diagnosticDifferenceSummary`，与实际写入剪贴板的字符串完全同源，不重新拼接字段或保存第二份正文。
- [x] 预览使用只读、可选择文本容器，保留换行并明确提示“仅供检查，不会自动发送或执行”；不得修改草稿、系统块登记、Store、版本或快照。
- [x] 差异替换、重新计算或进入不安全状态后预览自动关闭；重新出现不同摘要时不得沿用旧展开状态或旧复制反馈。
- [x] 增加默认收起、展开/收起、同源文本、摘要身份变化和安全状态隐藏测试，完成生产构建与真实浏览器验收。

第一版只预览当前有限差异文本，不增加编辑、搜索、差异历史、文件导出、系统分享面板、自动发送或执行。

2026-07-22 第 67 阶段完整回归：针对性测试 62/62，前端 25 个测试文件 235/235；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器在 1552.52 平方毫米共面区域真实比较 3.00→2.00 毫米加料诊断，差异卡固定显示 6 项且预览默认收起；展开后正文与捕获到的剪贴板文本严格相同，保留多行格式、可选择并可键盘聚焦。收起后正文消失；再次展开并替换为最新诊断后差异卡、预览和旧复制反馈同时清除；随后生成不同摘要时重新默认收起且不继承旧反馈。草稿前后用户文字保持不变，干净验收页 Console 无错误，仅保留既有 Vite 大分块警告。

### 68. 连续共面区域诊断差异预览一键全选第一版（已实现）

- [x] 仅在差异复制内容预览已展开时显示全中文“全选预览内容”入口，收起或差异失效时入口随预览一起消失。
- [x] 点击后只选中当前只读预览正文的完整文本，支持长文本和多行换行，不调用 Clipboard API、不自动复制。
- [x] 选择失败或当前浏览器不支持 Selection/Range 时显示全中文、非阻断反馈；不得修改草稿、系统块登记、Store、版本或快照。
- [x] 摘要身份变化、替换或进入不安全状态后旧选择反馈必须失效；新摘要重新展开后不得复用旧反馈。
- [x] 增加成功、失败、空引用、摘要身份变化和安全状态隐藏测试，完成生产构建与真实浏览器验收。

第一版只提供当前预览正文的一键全选，不增加自动复制、搜索、编辑、文件导出、系统分享面板、自动发送或执行。

2026-07-22 第 68 阶段完整回归：针对性测试 65/65，前端 25 个测试文件 238/238；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器展开 6 项差异摘要后点击“全选预览内容”，Selection 文本与预览正文严格相同并保留多行换行；剪贴板调用次数为 0，草稿前后保持不变。模拟 Selection 不可用时显示中文失败反馈；收起后入口和反馈消失，重新展开恢复初始状态；替换最新诊断后差异卡、预览、入口和反馈全部清除。干净验收页 Console 无错误，仅保留既有 Vite 大分块警告。

### 69. 连续共面区域诊断差异预览内容统计第一版（已实现）

- [x] 仅在差异复制内容预览展开时显示当前摘要的全中文“共 N 行 · N 个字符”只读统计，收起或差异失效时随正文隐藏。
- [x] 行数和字符数必须直接从当前 `diagnosticDifferenceSummary` 纯派生；字符数按 Unicode 码点统计，换行仍计入字符总数。
- [x] 统计不得截断、重写或保存第二份摘要正文，不修改草稿、系统块登记、Store、版本、快照或浏览器 Selection。
- [x] 摘要身份变化、替换或进入不安全状态后旧统计必须消失；不同摘要重新展开时只显示新摘要统计。
- [x] 增加空摘要、单行、多行、Unicode、摘要身份变化和安全状态隐藏测试，完成生产构建与真实浏览器验收。

第一版只显示行数和 Unicode 字符数，不增加搜索、字段筛选、编辑、文件导出、系统分享面板、自动发送或执行。

2026-07-22 第 69 阶段完整回归：针对性测试 67/67，前端 25 个测试文件 240/240；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器在 1552.52 平方毫米共面区域比较 2.00→3.00 毫米真实加料诊断，6 项差异预览显示“共 8 行 · 230 个字符”，与从预览正文独立计算的结果严格一致；一键全选 Selection 不受影响。收起后统计隐藏，重新展开显示相同统计且没有旧反馈；替换后差异卡、预览和统计全部消失，草稿前后用户文字完整保留。干净验收页 Console 无错误，仅保留既有 Vite 大分块警告。

### 70. 任意封闭模型六向打印方向评估第一版（已实现）

- [x] 对当前任意封闭 CAD 零件或用户上传 STL 的三角网格评估 +X、-X、+Y、-Y、+Z、-Z 六个轴向朝上候选，不绑定电子外壳、开发板、手办或固定模型名称。
- [x] 每个候选直接从毫米坐标和三角面法线纯计算旋转后宽、深、高、P1S 256×256×256 毫米成型空间适配、近似底面接触面积，以及超过 45° 打印悬垂阈值的需要支撑面积与比例。
- [x] 使用稳定、可解释的有限评分规则推荐一个可打印候选；成型空间不适配必须优先判为不可用，结果全中文显示推荐原因和每个候选的尺寸、接触、悬垂风险，不伪装为切片器或有限元结果。
- [x] 第一版只读分析，不自动旋转模型、不写入对象变换、Store 持久化、版本或快照；模型来源、几何或修订变化后旧评估必须清除或重新派生。
- [x] 增加立方体、长方体、索引与非索引等价、非对称悬垂、超出 P1S 空间、三角面绕序与无效网格测试，并完成生产构建与真实浏览器验收。

第一版只比较六个轴向打印方向，不计算任意角度、真实支撑树、切片时间、耗材、桥接、热变形、受力或疲劳寿命，也不自动应用推荐方向。

2026-07-22 第 70 阶段完整回归：针对性测试 6/6，前端 26 个测试文件 246/246；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器完成 CAD 主体、CAD 上盖、快速预览安全降级和任意上传 STL 验收：上传文件 `formai-stage70-imported-model.stl` 显示 1,608 个有效三角面，六向候选完整，推荐 Z 正方向朝上，尺寸 62.6 × 32.6 × 12.6 毫米、悬垂 22.0 平方毫米（0.3%）、底面接触 2027.0 平方毫米；来源或零件变化后旧结果立即清除。验收页 Console 无错误或警告，仅保留既有 Vite 大分块警告。

### 71. 六向打印方向建议一键应用第一版（已实现）

- [x] 仅在六向分析已有可打印推荐且来源身份仍与当前模型一致时显示“应用推荐方向”，明确展示将作用的对象名称、目标朝上方向和应用后宽、深、高。
- [x] 把六个候选映射为确定性的 90° 轴向旋转；用户确认后只更新当前 CAD 零件或上传 STL 对象的旋转，不改变缩放、颜色、其他装配零件或几何文件。
- [x] 复用现有对象变换、中文版本记录、撤销和重做链路；应用成功后清除旧分析并提示重新分析，不得静默累计重复旋转或自动执行第二次。
- [x] 当前对象变化、来源变化、推荐不可用、快速预览或分析过期时入口必须隐藏或禁用；确认取消和写入失败不得留下部分变换或伪版本。
- [x] 增加六向旋转映射、对象身份、重复应用保护、确认取消、版本提交和安全失效测试，并完成生产构建与真实浏览器验收。

第一版只应用六个轴向对应的受限对象旋转，不做任意角度优化、装配整体重排、自动落床、切片器同步、支撑生成或导出时隐式旋转。

2026-07-22 第 71 阶段完整回归：针对性测试 18/18，前端 26 个测试文件 253/253；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器验证 CAD 主体从 X 旋转 30° 到推荐绝对方向：取消后旋转和版本 2/2 不变，确认后旋转归零并生成版本 3/3，撤销恢复 30°、重做恢复 0°，重新分析显示“当前对象已是推荐打印方向”。主体 1.5 倍缩放后输出 93.9 × 48.9 × 18.9 毫米、悬垂 49.5 平方毫米、底面接触 4560.8 平方毫米；任意上传 STL 同样完成对象身份隔离与确认应用。验收页 Console 为 0 个错误、0 个警告。

### 72. 推荐打印方向应用后的自动落床预览与确认第一版（已实现）

- [x] 核对业务 `positionMm` 与 Three.js 场景垂直轴映射，基于当前精确 STL、推荐绝对旋转和均匀缩放纯计算旋转后最低点、当前平台高度和所需垂直位移，不绑定固定模型名称。
- [x] 推荐方向成功应用并重新分析确认后，显示全中文只读落床预览；明确当前对象名称、最低点、目标平台 0 毫米、位移方向和位移量，不得分析完成后自动移动。
- [x] 用户二次确认后只调整当前对象的垂直位置，使旋转缩放后的最低点落在平台 0 毫米；保持水平位置、旋转、缩放、颜色、其他零件和几何文件不变。
- [x] 复用对象展示版本、撤销和重做链路；已落床时显示完成状态，来源、对象、修订、旋转、缩放或位置变化后旧预览必须失效，取消和失败不得创建伪版本。
- [x] 增加六向旋转最低点、坐标映射、缩放、已有位置、重复落床、对象身份、确认取消、版本提交和安全失效测试，并完成生产构建与真实浏览器验收。

第一版只让单个当前对象沿打印平台垂直轴落到 0 毫米，不自动居中、不排列多个零件、不保持或重建装配约束、不同步切片器，也不修改 STL、STEP 或 3MF 几何文件。

2026-07-22 第 72 阶段完整回归：针对性测试 24/24，前端 26 个测试文件 259/259；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器验证 CAD 主体从 Y=10 毫米预览向下移动 10.00 毫米，任意上传 STL 从 Y=7.5 毫米预览向下移动 7.50 毫米；取消保持位置和版本，确认后最低点为 0.00 毫米并生成中文版本，撤销和重做可恢复，重新分析后显示已落床且不再重复应用。成功提示在来源身份更新后继续保留，验收页 Console 为 0 个错误、0 个警告。

### 73. 自动落床后的平台边界适配与只读居中预览第一版（已实现）

- [x] 基于当前精确 STL、坐标映射、归一化层级、绝对旋转、均匀缩放和对象位置，纯计算视口 X/Z 平面的最小值、最大值、宽度和深度，不绑定固定模型名称。
- [x] 对照 Bambu Lab P1S 256 × 256 毫米平台计算左、右、前、后四边余量；存在越界时显示方向和毫米越界量，完整适配时显示最小安全余量。
- [x] 对已落床对象显示全中文只读居中预览，明确居中后的目标 X/Z 位置与水平位移方向和数值；来源、对象、修订、位置、旋转、缩放或归一化空间变化后旧结果必须失效。
- [x] 第一版不自动移动对象、不调用对象展示版本链、不排列多个零件、不修改装配关系、几何文件、导出结果或切片器状态。
- [x] 增加 CAD、任意上传 STL、对象内与对象外归一化、旋转、缩放、已有水平位置、平台内、四向越界和来源失效测试，并完成生产构建与真实浏览器验收。

第一版只提供单个当前对象的 P1S 平台边界适配与居中位移建议，不处理喷头禁入区、热床夹具、裙边、Brim、支撑占地、多对象排布或自动提交。

2026-07-22 第 73 阶段完整回归：针对性测试 29/29，前端 26 个测试文件 264/264，TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器验证 CAD 主体 1.5 倍缩放、X=100/Z=-30 时右侧越界 18.95 毫米并建议向左 100.00、向前 30.00 毫米；任意上传 STL 默认四边余量 96.70/96.70/111.70/111.70 毫米，X=-110/Z=40 时左侧越界 13.30 毫米并建议向右 110.00、向后 40.00 毫米。

### 74. 平台居中建议确认应用第一版（已实现）

- [x] 仅在当前对象已经落床、平台预览来源身份有效且对象尚未水平居中时显示“应用居中位置”，并以全中文二次确认明确对象名称、当前 X/Z、目标 X/Z 和两轴位移。
- [x] 新增纯函数只替换对象展示状态中的 `positionMm.x` 与 `positionMm.z`；必须保留 Y、旋转、均匀缩放、颜色、其他对象、装配关系和几何文件。
- [x] 确认写入前重新核对对象、修订、位置、旋转、缩放、基础位置、归一化空间和平台结果，拒绝过期来源、已居中、非有限目标或越出对象变换安全范围。
- [x] 复用对象展示编辑、全中文版本、撤销和重做链路；取消、失败和重复应用不得创建伪版本，成功后旧分析失效并提示重新分析。
- [x] 增加 CAD、任意上传 STL、X/Z 双轴及单轴移动、状态保留、确认取消、版本提交、撤销重做、重复保护和来源失效测试，并完成生产构建与真实浏览器验收。

第一版只把单个当前对象移动到平台中心，不排列多个对象，不计算模型间碰撞，也不处理 Brim、裙边、支撑占地、热床夹具或喷头禁入区。

2026-07-22 第 74 阶段完整回归：针对性测试 32/32，前端 26 个测试文件 267/267，TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器验证任意上传 STL 从 X=-110/Z=40 移动到平台中心：取消保持版本 7/7，确认生成版本 8/8，撤销恢复 -110/40、重做恢复 0/0；重新分析显示四边余量 96.70/96.70/111.70/111.70 毫米并隐藏重复应用入口。CAD 主体在 1.5 倍缩放、X=100/Z=30 时确认后只把 X/Z 改为 0/0，撤销和重做保持缩放；来源位置变化会清除旧预览，验收页 Console 为 0 个错误、0 个警告。

### 75. 平台安全边距设置与单对象可打印区域预览第一版（已实现）

- [x] 为平台边界卡增加全中文安全边距输入，默认 5 毫米；只接受有限、非负且不会把 P1S 256 × 256 毫米平台有效宽深压缩到 0 的毫米值。
- [x] 在纯计算层从物理平台四边向内收缩安全边距，输出有效平台边界、有效宽深、四边安全余量、安全越界量和是否位于安全区域；物理平台边界结果继续保留。
- [x] 已落床且来源有效时同时显示物理平台适配与安全区域适配；安全区域外时给出回到有效区域所需的最小 X/Z 修正方向和毫米值，区域内时显示最小安全余量。
- [x] 安全边距变化只重新计算只读结果，不修改对象、不创建展示版本、不改变居中确认目标，也不得污染来源身份、撤销或重做链路。
- [x] 增加默认值、零边距、上限边界、非法输入、CAD、任意上传 STL、安全区域内外、单轴和双轴越界测试，并完成生产构建与真实浏览器验收。

第一版只处理单个对象相对矩形有效区域的静态包围盒，不计算 Brim、裙边、支撑占地、热床夹具、喷头禁入区、多对象碰撞或 Bambu Studio 切片结果，也不自动移动对象。

2026-07-22 第 75 阶段完整回归：针对性测试 39/39，前端 26 个测试文件 274/274，TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器验证 CAD 主体 1.5 倍缩放、X=20/Z=0 时，80 毫米边距形成 96.00 × 96.00 毫米有效区域，右侧越界 18.95 毫米并只读建议向左修正 18.95 毫米，物理平台仍完整适配且版本保持 6/6；任意上传 STL 默认 5 毫米时有效区域为 246.00 × 246.00 毫米，四边安全余量为 91.70/91.70/106.70/106.70 毫米，改为 20 毫米后分析保留且版本保持 2/2。独立验收页 Console 为 0 个错误、0 个警告。

### 76. 安全区域修正建议确认应用第一版（已实现）

- [x] 仅在当前对象已落床、物理平台预览与安全边距快照来源有效、对象可仅靠平移完全进入有效区域、当前不在安全区域且最小修正非零时显示“应用安全区域修正”。
- [x] 新增纯函数只把安全预览的 `correctionDeltaMm.x/z` 加到对象 `positionMm.x/z`；保留 Y、绝对旋转、均匀缩放、颜色、其他对象、装配关系、几何文件和物理平台居中目标。
- [x] 使用全中文二次确认明确对象名称、安全边距、当前 X/Z、修正方向与数值、修正后 X/Z；取消、重复、对象过大、建议为零、边距变化或来源过期不得创建伪版本。
- [x] 写入前重新计算当前物理平台与安全区域并核对修正结果确实位于有效区域；成功复用对象展示中文版本、撤销和重做，随后清除旧分析并提示重新分析。
- [x] 增加 CAD、任意上传 STL、单轴和双轴修正、状态保留、边距变化、来源失效、对象过大、取消、版本、撤销重做和重复保护测试，并完成生产构建与真实浏览器验收。

第一版只应用单个对象进入矩形有效区域所需的最小水平平移，不强制居中，不排列多对象，也不处理 Brim、裙边、支撑、热床夹具、喷头禁入区或切片器结果。

2026-07-22 第 76 阶段完整回归：针对性测试 44/44，前端 26 个测试文件 279/279，TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器验证 CAD 主体在 80 毫米边距下从 X=20 最小修正至 X=1.05：确认卡明确对象、边距、当前与目标位置，取消保持版本 1/1，确认生成版本 2/2，撤销恢复 X=20、重做恢复 X=1.05，Y、旋转和 1.5 倍缩放保持。任意上传 STL 在 X=110/Z=120 下显示双轴修正；确认期间把边距从 5 改为 10 毫米会关闭旧快照且版本保持 4/4，重新确认后移动到 X=86.70/Z=101.70 并生成版本 5/5，重新分析显示“位于安全区域”和 0.00 毫米最小余量。独立验收页 Console 为 0 个错误、0 个警告。

### 77. 平台安全区域边界三维视口可视化第一版（已实现）

- [x] 为三维视口定义独立只读打印平台叠加协议，包含 P1S 物理平台矩形、安全有效区域矩形、当前对象水平包围框和四边越界状态，不进入对象展示版本或几何导出。
- [x] 在已落床且平台预览来源有效时显示平台叠加层；物理边界、安全边界和对象占地使用可区分的颜色、线型与全中文图例，缩放、旋转和平移视角时保持毫米坐标一致。
- [x] 当前对象超出安全区域时按左、右、前、后方向高亮对应边界；位于安全区域或对象尺寸过大时使用不同状态表达，不伪造可修正结果。
- [x] 安全边距、对象来源、位置、旋转、缩放、模型切换或重新分析变化时同步刷新或清除叠加；只读显示不得移动对象、创建版本、影响选择或阻断三维操控器。
- [x] 增加 CAD、任意上传 STL、安全区域内外、单轴和双轴越界、对象过大、边距变化、来源失效和视口交互回归测试，并完成生产构建与真实浏览器验收。

第一版只显示单个当前对象相对矩形平台的静态水平轮廓，不显示 Brim、裙边、支撑、热床夹具、喷头禁入区、多对象排布或 Bambu Studio 切片占地。

2026-07-22 第 77 阶段完整回归：针对性测试 53/53，前端 28 个测试文件 288/288，TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器验证 CAD 主体 X=110/Z=120 在 5 毫米边距下显示右侧 18.30 毫米、前侧 13.30 毫米双轴越界，改为 10 毫米后同步更新为 23.30/18.30 毫米且版本保持 3/3；实际旋转、滚轮缩放和右键平移均生效，图例命中穿透到 Canvas。任意上传 `imported-model.stl` 显示通用名称，10 毫米安全修正至 X=86.70/Z=101.70 后重新分析为区域内，分析和叠加不创建版本。独立验收页 Console 为 0 个错误、0 个警告。

### 78. 打印平台叠加一键俯视与适配视野第一版（已实现）

- [x] 仅在有效打印平台叠加存在时提供全中文“俯视并适配平台”入口；叠加失效或清除后入口同步隐藏。
- [x] 新增纯函数根据物理平台与当前对象水平边界并集计算俯视中心、控制器目标和安全相机距离，使完整平台与越界对象都进入视口安全范围。
- [x] 相机切换使用临时视口请求，不修改对象位置、旋转、缩放或颜色，不创建模型版本，不进入撤销重做、项目持久化、导出或打印分析来源身份。
- [x] 聚焦完成后继续允许鼠标旋转、滚轮缩放和右键平移；连续点击、窗口尺寸变化、来源变化和无效边界不得产生 NaN、跳到模型内部或阻断选择。
- [x] 增加平台内、单轴/双轴越界、对象大于平台、宽高比变化、重复请求和来源清理测试，并完成生产构建与真实浏览器验收。

第一版只提供当前平台叠加的一次性俯视适配，不保存相机书签、不自动跟随对象、不修改正交/透视模式，也不替代用户自由视角。

2026-07-23 第 78 阶段完整回归：针对性测试 18/18，前端 29 个测试文件 297/297，TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器验证越界对象与安全区域内对象均可进入完整平台俯视范围，重复点击、旋转、滚轮缩放和平移继续正常，操作不创建版本；来源变化会清除入口，重新分析后的新来源首次请求即使编号重新为 1 仍实际改变 Canvas。独立验收页 Console 为 0 个错误、0 个警告。

### 79. 打印平台俯视前视角返回第一版（已实现）

- [x] 第一次执行俯视适配前，临时捕获当前相机位置和 `OrbitControls.target`；同一来源内重复俯视不得覆盖最初快照。
- [x] 仅在打印平台叠加仍有效且存在同来源可返回快照时显示全中文“返回原视角”；返回使用与俯视一致的平滑相机切换并在完成后清除快照。
- [x] 快照和返回请求只存在于视口临时状态，不进入模型版本、撤销重做、项目存储、打印分析身份或 STL/STEP/3MF 导出。
- [x] 来源失效、对象变换、模型切换、新建画布或叠加清除时立即删除旧快照；非法或非有限相机数值必须安全拒绝，不得跳入模型或生成空白视口。
- [x] 返回完成后继续允许旋转、缩放、平移和选择；增加首次捕获、重复俯视不覆盖、成功返回、来源清理和无效快照测试，并完成生产构建与真实浏览器验收。

第一版只支持当前平台叠加会话内的一次临时返回，不提供相机书签列表、命名视图、跨项目持久化、正交/透视切换或多级视角历史。

2026-07-23 第 79 阶段完整回归：针对性测试 23/23，前端 29 个测试文件 302/302，TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器验证首次俯视后出现“返回原视角”，手工旋转再重复俯视不会覆盖最初快照；返回后的 Canvas PNG 与原视角逐字节一致，入口自动隐藏且版本保持 1/1。再次俯视后修改对象 X 位置，旧平台叠加、俯视和返回入口同步清除。独立验收页 Console 为 0 个错误、0 个警告。

### 80. 打印平台半透明床面与中心、前向标识第一版（已实现）

- [x] 在有效打印平台叠加中，根据物理平台边界派生真实毫米中心、宽度和深度，显示低透明度床面但不得遮挡当前对象或安全区域线框。
- [x] 在床面上绘制中心十字，并在前侧边界显示全中文“前侧（Z 正）”方向标识；方向必须与现有前/后越界语义一致，不依赖固定对象或模型类型。
- [x] 床面、中心十字和方向标识不接收鼠标事件，不参与对象选择、操控器、框选或射线命中，不进入模型版本、撤销重做、项目存储或 STL/STEP/3MF 导出。
- [x] 来源、对象变换、模型切换、新建画布或叠加清除时同步移除；非法、非有限或退化平台边界必须安全拒绝，不得生成 NaN 几何或遮蔽视口。
- [x] 增加床面尺寸与中心、中心十字、前向坐标、非法边界和来源生命周期测试，并完成生产构建与真实浏览器交互验收。

第一版只显示矩形 P1S 物理平台的只读空间提示，不还原热床纹理、磁吸板厚度、擦嘴区、校准区、喷头禁入区、夹具、Brim、裙边或切片路径。

2026-07-23 第 80 阶段完整回归：针对性测试 12/12，前端 29 个测试文件 305/305，TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器验证半透明床面、金色中心十字和“前侧（Z 正）”标识可见；标签事件穿透，真实鼠标旋转后 Canvas 变化而版本保持 4/4。对象来源变化后床面标识、俯视和返回入口同步清除，验收页 Console 为 0 个错误、0 个警告。

### 81. 打印平台毫米网格与坐标刻度第一版

- [ ] 根据当前物理平台边界按真实毫米坐标生成 10 毫米次网格和 50 毫米主网格；线段必须裁剪在床面内，不能依赖 P1S 固定中心或对象类型。
- [ ] 在 X 轴和 Z 轴主网格位置显示只读毫米刻度，原点和正负方向必须与对象水平位置、前侧最大 Z 及现有越界语义一致，所有辅助文案使用中文。
- [ ] 主次网格需保持清晰层级但不得遮挡对象、中心十字、安全区域、对象占地或越界高亮；俯视和自由视角下均可辨认。
- [ ] 网格线和刻度标签不接收鼠标、触控或射线事件，不参与选择、框选、操控器、版本、项目存储或 STL/STEP/3MF 导出；来源失效时同步移除。
- [ ] 增加非对称边界、跨零点与不跨零点、主次线分类、边界裁剪、刻度文本、非法边界和来源生命周期测试，并完成生产构建与真实浏览器验收。

第一版只提供固定 10/50 毫米只读网格，不增加网格间距设置、英制单位、吸附、测量工具、热床纹理、切片路径或喷头运动预览。

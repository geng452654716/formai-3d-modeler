# FormAI — AI 三维建模

FormAI 是面向 macOS 和三维打印的 AI 参数化建模软件。用户可以提供自然语言指令、参考图片、关键尺寸和电子元件数据，由 Codex 规划建模修改，再由本机 OpenCascade/CadQuery 内核生成和校验精确实体。

## 产品重点

- 通过自然语言和参考图片创建基础模型；
- 以毫米为统一单位进行一比一精确建模；
- 支持内部掏空、壁厚、装配间隙、开孔、圆角、倒角和电子元件包络；
- 支持对象、部件和局部区域选择后向 Codex 下达修改指令；
- 自动执行 AI 修改，并在每次修改前创建可回滚版本；
- 面向 Bambu Lab P1S，导出 STL、STEP 和几何 3MF；
- 首发平台为 macOS，不依赖 Fusion 360。

详细需求见 [产品规格](docs/product-spec.md)，技术方案见 [架构设计](docs/architecture.md)，当前交付边界见 [实施计划](docs/implementation-plan.md)。

## 当前已经实现

- React Three Fiber 三维视口，以及视角旋转、缩放、平移和对象选择；
- 选中任意 CAD 零件、上传 STL、制造拆件或参考元件后，可使用全中文三维操控器或毫米/角度精确输入移动、旋转和 0.05–20 倍均匀缩放，并调整基础颜色；一次连续拖动只生成一个可撤销版本；
- 装配基础位置、拆分视图临时展开偏移和用户真实变换分层保存；切换装配/拆分视图不会丢失用户变换，临时展开距离不会写入制造文件；
- STL/3MF 桌面导出会真正烘焙视口变换；完整装配 3MF 保留独立对象、毫米单位、中文名称、基础颜色和装配位置。STEP 明确保留 OpenCascade 参数化原始坐标；
- 默认装配视图，以及明确的“装配视图 / 拆分视图”切换；
- “新建模型”画布重置和版本历史初始化；
- PNG、JPEG、WebP 图片导入、拍照入口、双点真实尺寸标定和毫米/像素比例计算；
- 通过本机 Codex `--image` 逐张识别物体、尺寸参数、USB-C/按钮/LED/排针等接口，并在照片上叠加中文标记；
- 识别完成后可切换到“编辑接口”模式，直接在已标定照片上选择和拖动接口框、拖动右下角调整宽高，并可恢复本次 Codex 原始识别值；二维编辑会同步更新毫米尺寸、横向偏移和底部偏移，并使旧联合标定失效；
- 同一项目可管理正面、背面、左侧、右侧、顶部、底部和透视照片；每个视角独立保存双点标定、识别结果和接口观测；
- 至少两个已标定视角可执行联合标定：使用毫米/像素比例中位数融合尺度，报告最大相对偏差，并按稳定 ID 或类型与尺寸形成“已匹配 / 待确认”接口对应；
- 联合结果提供全中文接口人工复核：可确认同一接口、把错误候选拆为独立接口、忽略误识别，并修正 ID、类型、所在面、尺寸、照片位置、物理偏移、是否开孔和开孔轮廓；待确认项未处理完时禁止直接生成模型；
- 多视角元数据和联合结果保存到本机应用存储；图片二进制不写入该存储，重新识别时需要重新选择原图；
- 多视角尺寸估算按参数取中位数后写入模型；所有经过人工复核、需要开孔且可确定正交接口面的观测均可进入通用精确开孔协议，支持正面、背面、左侧、右侧、顶部和底部，以及圆孔、矩形孔、圆角矩形孔和槽孔；顶部开孔切上盖，其余五面切主体；
- 新生成的照片开孔保存“接口面水平中心 + 底边”的毫米定位锚点；外壳尺寸变化时自动重新计算开孔中心坐标，接口宽高、水平偏移和底部偏移保持毫米值，不按比例缩放；
- OpenCascade 封闭 CAD 实体的平面拆件、自动补面、有效性和体积守恒验证，并已接入桌面按钮、装配/拆分视图及拆件 STL/STEP 导出；
- 圆柱定位销、D 形防转销、燕尾榫、球头/球窝和磁铁孔已由 OpenCascade 精确布尔写入拆件实体；PLA/PETG 可拆悬臂卡扣、M2/M2.5/M3 螺丝柱、打印友好圆脊近似内外螺纹，以及 M2/M2.5/M3 ISO 公制粗牙 60° 内外螺纹均已实体化；
- 精确内核自动检测两侧可附着位置，并检查封闭性、悬浮 Solid、至少 1.20 毫米的连接结构设计壁厚、0.10–1.00 毫米装配间隙和公母干涉；
- 支持用户上传任意文件名和模型类别的 STL，完成拓扑诊断、退化/重复三角面清理、简单平面孔洞自动修复、实体校验、X/Y/Z 平面拆件、视口预览和两侧 STEP/STL 导出；
- 首次导入任意 STL 时固定保留 `imported-model.stl` 原始文件，并生成 `imported-model-working.stl` 与 `imported-model-working.step` 标准工作文件；界面展示修洞数、补面三角形、修复前后开放边和连通网格数；
- 上传 STL 激活后，场景树、项目标题、右侧模型信息、候选连接位置和导出菜单均由上传模型的真实文件名、包围盒与拆件结果驱动，不使用 ESP32 或固定外壳结构；
- 支持对任意封闭 STEP/STL 执行全局壁厚“表面法向射线采样估算”，按危险（<0.80 毫米）、偏薄（0.80–1.20 毫米）、建议（1.20–2.00 毫米）和充足（≥2.00 毫米）分级，并在三维视口显示红/橙/黄/绿热力图、覆盖率、最薄值和 5% 分位；
- 热力图支持点击局部区域或一键定位最薄处，并把零件、原始毫米坐标、风险等级和估算壁厚附加到下一条 Codex 指令；CAD 参数化修改完成后会自动重新采样并报告该区域修改前后的壁厚变化；
- 上传任意封闭单 Solid STL 后，可在壁厚热力图中选择局部采样点，沿选中表面法向执行“圆形凸台加厚”或“圆孔切除”；修改后自动重新执行“表面法向射线采样估算”；
- 上传 STL 的连续局部编辑读取 `imported-model-result.json` 指向的当前工作文件，不会每次回到原始 STL；原始上传文件始终保留，STEP/STL 工作文件及结果清单采用带备份回滚的批量替换；
- 局部修改结果会经过 OpenCascade 有效性、封闭性、单一 Solid、体积方向和 STL 再导入检查；
- 任意上传的封闭单 Solid STL 支持顶点、无向三角边和三角面的点击单选或屏幕投影穿透框选，并可对最多 512 个同类元素执行统一 XYZ 毫米位移、绕选择集合几何中心的 X/Y/Z 单轴旋转，以及围绕同一中心的均匀缩放；顶点按源毫米坐标去重、边按无向源端点去重、面按当前修订三角面索引去重，选中集合使用合并点、线和面几何统一高亮；
- 对受管单 Solid 网格点击一个种子三角面后，可沿共享无向边自动扩展同一连续共面区域并执行真实外法线加料或内法线压入；系统不跨锐边或曲面，支持外环与孔洞环，并继续执行封闭性、有效性、单一 Solid、体积方向、原子回滚和精确版本快照检查；
- 参数化 CAD 视图支持主动选择当前修订中的任意零件并创建独立受管网格分支。系统保留原参数化 CAD 历史，安全导入所选零件的真实毫米 STL，并复用上述顶点、边、面选择与位移、旋转、缩放；桌面端版本保存工作 STL、STEP 和模型清单的精确快照，版本历史明确区分普通上传模型与 CAD 派生网格；
- 当前电子元件保护壳模板：参考元件包络、掏空主体、接口开孔、定位导轨和独立上盖；
- ESP32-S3 N16R8 参数仅保留为 `modeling/examples/` 中的示例 Demo，不属于通用内核协议；
- 壁厚、底厚、圆角、倒角、间隙和接口尺寸的参数化修改；
- 撤销、重做、版本记录和本机版本快照；参数化 CAD 与上传 STL 使用相互独立的精确快照。上传模型快照绑定不可变修订号，只有工作 STL、STEP 和清单真实恢复成功后才移动历史索引；恢复失败保留最后有效模型。全中文“版本历史与对比”面板仍可恢复任意参数化版本，并查看参数差异、精确 CAD 视觉对比与 OpenCascade 双向布尔差集；
- 参数化精确 CAD 会逐 OpenCascade 面独立三角化，并按稳定面顺序拼接成专用选择 STL；面映射文件为每个稳定面记录连续的 `triangleIndex` 区间，选择 STL 与面向用户的导出 STL 相互独立；
- 三维视口支持点击选择单个稳定 CAD 面，以及框选当前视角可见的多个稳定 CAD 面；框选会先判断屏幕空间三角形与矩形是否相交，再通过摄像机射线的第一命中过滤被遮挡的背面；
- 选择上下文会将装配视图或拆分视图中的命中位置逆变换回零件原始毫米坐标，并同步逆变换外法线；稳定面 ID、零件包围盒、摄像机矩阵、尺寸上下文和局部截图会一起发送给 Codex，截图通过 PNG 附件传输而不是写入文本；
- 点击任意稳定 CAD 面后，会按当前 `revision + partId + stableFaceId + triangleIndex` 调用 OpenCascade 重新定位 STEP 真实裁剪面，将选择网格命中点投影为真实曲面点，解析考虑面方向的外法线、曲面 UV 和 UV 边界，并回传投影距离与法线点积；异步旧结果不会覆盖后续选择，失败时只保留网格预览且不会伪造精确 UV；
- 点击单个稳定 CAD 平面后，可用全中文指令沿 OpenCascade 重新定位面的真实外法线增加圆形或旋转矩形凸台、切除圆孔/旋转矩形孔/槽孔，也可将整个平面向外拉伸或向内偏移；Codex 登录时只返回受限结构化计划，`partId + stableFaceId` 必须与当前点击选择完全一致，中心和法线始终取自已验证的选择上下文；Codex 不可用时才使用确定性中文尺寸解析器；
- 点击圆柱面等非平面稳定 CAD 面后，可基于当前修订中 OpenCascade 精确解析的真实三维点、外法线、UV 和单位 U 切向，沿真实外法线生成受限圆形或矩形凸台，或沿真实内法线切除圆孔、矩形孔和槽孔；曲面矩形与槽孔都在真实 UV 点击位置建立切平面，`0°` 沿真实 U 切向，正角度围绕真实外法线旋转。矩形使用半对角线、槽孔使用总长度一半作为曲率、裁剪边界、壁厚、自交和相邻面干涉检查的保守包络半径。该能力仍是点击位置切平面的安全近似，不是沿曲面贴合或测地线轮廓；盲孔要求至少保留 1.20 毫米壁厚，通孔只放行局部壁厚附近的正常出口接触，修改记录可参与参数化安全重放；
- 曲面圆形、矩形或受限槽孔特征在自动调用 OpenCascade Worker 前，会在目标零件上显示绑定当前 `revision + partId + stableFaceId + surfaceUv` 的半透明三维预览：凸台沿真实外法线显示，孔和槽孔沿真实内法线显示，矩形与槽孔预览包含实际宽高或宽长、深度和旋转角；预览具有“准备自动执行 / 正在自动执行 / 检测到曲面干涉，已阻止执行 / 执行失败，预览已保留”中文状态，成功后清除；干涉风险使用橙色预览并在布尔和文件写回前阻止执行，普通失败则保留原预览供调整尺寸重试，重新选择、模型修订变化或参数修改后自动失效；
- 点击任意稳定 CAD 面所属的一条种子稳定 CAD 边后，可用全中文指令执行 0.20–50.00 毫米的固定半径单边圆角或等距倒角；明确写“切线链”“相切边”“切线连续”“连续边链”“沿切线”或“顺着切线”时，可从种子边两端传播到唯一且夹角不超过 5 度的切线连续边链；若种子边所属面为平面，并明确写“整圈”“一圈”“周边”或“轮廓边”，还可对种子边所属的唯一平面边界 Wire 执行整圈圆角或倒角。视口的“多选边链”工具还支持按顺序手工选择同一零件中的 2–64 条稳定边，可跨所属稳定面组成无分叉开放链或闭合链，并一次执行统一半径圆角或统一距离倒角。每条手工边都必须完成 OpenCascade 精确解析，Codex 必须原样返回逐边稳定面/稳定边列表，不得增删、换序或替换目标；Worker 会拒绝重复物理边、不连续边和分叉拓扑。当前仍不支持可变半径、连续性等级控制、分叉边网、多个独立边链或永久拓扑命名；
- 稳定 CAD 局部修改完成后会重新导出 STEP/STL/通用多零件 3MF，重新生成面描述、面内边描述、几何签名匹配摘要、选择 STL 和面三角映射，并把记录写入 `generation-result.json` 的 `localFeatures[]`；后续参数化整模重建会从基础参数化实体开始按记录顺序安全重放，并使用修改前 `targetFace` / `targetEdge` 几何签名快照辅助重新定位；
- 曲面圆形、矩形和受限槽孔特征会把曲率、局部壁厚、剩余壁厚、通孔状态、自交与相邻面干涉、干涉稳定面及接触采样诊断持久化到局部特征记录；参数化重放会按当前几何重新计算，版本历史可用全中文界面对比特征新增、删除，以及圆形直径、矩形宽高、槽孔宽长、旋转角、真实 U 切向、作用深度和诊断字段变化，并兼容旧版本缺少诊断快照或真实 U 切向的记录；
- 曲面精确工具体预检无论通过或阻断都会独立留档，版本历史可查看最近 50 条预检证据；阻断记录不会创建模型版本。风险面板支持同目标预检前后对比，并提供经过受限参数校验的圆形、矩形、槽孔、深度和小范围旋转安全候选，应用后仍必须重新通过 OpenCascade 精确预检和正式 Worker；
- CadQuery + OpenCascade 精确实体自动重建；
- 实体有效性、体积、包围盒和 P1S 成型尺寸检查；
- “OpenCascade 精确实体”和“快速参数预览”切换；
- 分件 STL、STEP，以及包含上下壳两个对象的标准几何 3MF 导出；
- Tauri 2 macOS 桌面壳和中文原生菜单；
- Rust 到 Python CAD Worker 的本机进程通信；
- 复用本机 Codex 登录状态的 Codex CLI 指令适配器；
- “修改前快照 → Codex 参数规划 → OpenCascade 重建 → 实体校验 → 提交版本”的自动执行闭环；
- Codex 不可用时自动回退到本地中文规则解析；
- 拆件策略、圆柱销、D 形销、燕尾榫、球头、磁铁孔、卡扣、螺丝柱和内外螺纹的制造方案规划器。

当前的标准几何 3MF 已保存多对象、毫米单位、基础颜色和视口变换，可由 Bambu Studio 打开，但还没有写入 Bambu Studio 专有的切片配置、耗材和支撑工程元数据。

新生成的照片精确开孔会保存底边定位锚点，并在外壳长、宽、高变化时按当前接口面尺寸重新计算中心坐标；接口宽高和物理偏移不会按比例缩放。老项目如果没有定位锚点元数据，仍按原固定毫米坐标兼容加载，界面会提示重新复核。单张透视照片不能单独确定精确接口面；打印前仍应使用卡尺复核孔位和孔径。边缘余量、孔间距、实体相交、有效性和单一 Solid 检查只提供几何与制造保护，不等同于受力、疲劳或有限元分析。

## 尚未实现

- 参数化精确 CAD 已支持任意稳定面的 OpenCascade 真实点击点、外法线、曲面 UV 和真实 U 切向解析，支持稳定平面的局部轮廓、整面偏移、单边、唯一切线连续边链、唯一平面边界整圈以及手工多选无分叉边链圆角/倒角，也支持非平面曲面的第一版圆形凸台、圆孔、矩形凸台、矩形孔、受限槽孔，以及所属边的单边、唯一切线连续边链或手工多选无分叉边链圆角/倒角；曲面整面偏移、曲面参数编辑、沿任意曲面贴合或测地线轮廓、分叉边网、多个独立边链、可变半径圆角、连续性等级控制和框选多面布尔仍未实现；
- 任意上传的封闭单 Solid STL 与 CAD 派生网格分支已支持顶点、无向三角边和三角面的点击单选、屏幕投影穿透框选、最多 512 个同类元素的批量毫米位移、单轴旋转和均匀缩放，也支持单个连续共面区域沿真实法线的受限加料与压入、绑定精确快照的历史恢复以及完整撤销/重做；写回或恢复前会检查退化面、封闭性、实体有效性、Solid 数量和体积。曲面区域挤出、多个种子、多区域框选、任意方向挤出、侧壁斜度、倒角联动、焊接、分裂、未受约束自由雕刻、任意自由曲面重建、多 Solid 局部编辑和复杂非流形/自相交修复仍未实现；
- 稳定面以及所属稳定面内的稳定边都使用“几何签名匹配第一版”，不是 OpenCascade 原生永久拓扑命名；大幅拓扑变化、对称面或布尔重建仍可能使编号变化。`triangleIndex` 只对本次生成的选择网格有效，任何局部修改后都会立即失效，稳定边也必须重新选择；整模重建只会在稳定面、稳定边、记录中心、外法线和尺寸全部通过安全校验时重放 `localFeatures[]`，任一记录失败会拒绝整次重建并保留最后有效模型；
- Codex App Server 持久会话；
- 发布版内置 Python/CadQuery 运行环境。目前开发模式使用 `modeling/.venv`；
- 面向复杂破损手办网格的非流形、自相交、分叉/嵌套边界、非共面破面和细小孤岛修复，以及曲面拆件、任意角度打印方向优化、真实切片支撑、基于曲率/受力的结构薄弱分析和自动连接器布置；
- 从照片直接生成任意物体三维网格；当前照片能力只做逐张识别、尺寸标定、尺度中位数融合和接口候选匹配；
- Bambu Studio 专有工程 3MF。

## 本地运行

先准备能够导入 CadQuery 的 Python 环境。当前项目默认使用：

```bash
modeling/.venv/bin/python
```

生成初始模型并运行网页开发模式：

```bash
pnpm install
pnpm run model:generate
pnpm run dev
```

运行 macOS 桌面应用：

```bash
pnpm run desktop:dev
```

桌面端默认复用本机 Codex 桌面应用或 Codex CLI 的现有登录配置，不会读取或显示认证密钥。

## 验证与构建

```bash
pnpm run model:wall-test
pnpm run model:opening-test
pnpm run model:local-cad-feature-test
pnpm run model:local-cad-replay-test
pnpm run model:local-cad-curved-test
pnpm run model:local-cad-edge-test
pnpm run model:surface-hit-test
pnpm run test
pnpm run build
pnpm run desktop:check
pnpm run desktop:test
pnpm run desktop:build
```

网页生产构建会把当前已生成模型复制到 `dist/generated/`。桌面发布包暂未内置完整 CadQuery 运行环境，因此从 Finder 独立启动发布版时，需要通过 `FORM_AI_PYTHON_PATH` 指向可用的 Python，或完成后续的运行环境捆绑工作。

### 30. 非平面矩形凸台与矩形孔安全执行（已实现）

- 非平面稳定面现已允许 `add-rectangle` 和 `cut-rectangle`。矩形位于当前修订真实 UV 点击位置的切平面，`rotationDeg=0` 沿 OpenCascade 真实 U 切向，正角度围绕真实外法线；凸台沿外法线加料，矩形孔沿内法线切削。
- Worker 会在当前 STEP、稳定面和 UV 上重新计算真实 U 切向，并校验请求上下文中的切向没有过期、反向或退化；Codex 受限 JSON 不包含可编辑切向字段，不能生成或改写该方向。
- 曲面矩形以 `sqrt(width² + height²) / 2` 作为保守包络半径，统一参与曲率比、裁剪边界、局部壁厚、剩余壁厚、通孔出口、自交和相邻稳定面干涉检查，不会只检查矩形四角或低估作用范围。
- Three.js 使用与 OpenCascade 相同的真实 U 切向、真实外法线和旋转角构建半透明矩形凸台或矩形孔预览；`localFeatures[]` 持久化宽、高、深度、旋转角、真实 U 切向和曲面诊断。
- 参数化安全重放会按当前 OpenCascade 几何重新计算 U 切向和半对角线诊断；旧记录没有 U 切向时可安全补写，携带反向或不一致切向时会在修改模型前拒绝。
- 全中文版本历史支持“曲面矩形凸台”和“曲面矩形孔”，并比较轮廓宽度、矩形高度、旋转角、曲面 U 切向、作用深度及曲率、壁厚、通孔和干涉诊断。

能力边界：曲面矩形仍是点击位置切平面的安全近似，不是沿任意曲面贴合的矩形，也不是测地线轮廓。稳定面仍只能称为“几何签名匹配第一版”；`triangleIndex`、曲面 UV 和真实 U 切向都只对当前修订有效，模型重建或局部修改后必须重新选择。

2026-07-21 完整回归通过：前端 17 个测试文件 119/119、Rust 28/28、稳定 CAD Worker 9/9、曲面局部特征 23/23、参数化局部特征安全重放 11/11、曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、全部 Modeling Python 语法编译和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

### 31. OpenCascade 精确工具体预演与结构化曲面干涉反馈（已实现）

- 非平面圆形、矩形和槽孔特征在正式写入前，必须先由 OpenCascade 使用当前 STEP、稳定面、真实 UV、真实外法线、真实 U 切向、曲率和局部壁厚生成精确工具体并执行结构化预检。
- 三维视口显示的是正式布尔运算实际使用的工具体 STL，而不是 Three.js 仅按参数绘制的近似体；该工具体用于说明作用范围，不代表最终布尔结果。
- 预检通过后才保存修改前快照并自动执行正式布尔。预检返回 `blocked` 或生成异常时，不创建新版本、不写入模型，并保留当前 CAD、稳定面选择和精确工具体供用户查看。
- 结构化反馈包含：是否再次接触目标曲面、是否碰到非目标稳定面、干涉稳定面 ID、最近干涉距离、接触面数、接触采样数、工具体有效性、封闭性、Solid 数、体积和毫米包围盒。
- 精确工具体只绑定当前 `revision + partId + stableFaceId + surfaceUv + surfaceTangentU`；模型重建、局部修改或重新选择后自动失效，不能跨修订复用。

能力边界：曲面矩形和槽孔仍是在真实 UV 点击位置建立的切平面安全近似，不是沿任意曲面贴合的轮廓，也不是测地线轮廓；精确工具体预演不等同于最终布尔结果。稳定面仍只能称为“几何签名匹配第一版”，不是永久拓扑命名。

2026-07-21 完整回归通过：前端 17 个测试文件 120/120、Rust 28/28、稳定 CAD Worker 12/12、曲面局部特征 23/23、参数化局部特征安全重放 11/11、曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、全部 Modeling Python 语法编译和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

### 32. 精确工具体测量、干涉面定位与风险参数调整（已实现）

- 曲面精确预检阻止执行后，界面会显示工具体体积、毫米包围盒、最近干涉距离、接触面数和接触采样数。
- 三维视口以红色显示全部干涉稳定面，以亮红色和中文标签定位当前干涉面；定位不会改变原目标曲面、真实 UV 或当前模型。
- 风险面板可调整圆形直径、矩形宽高、槽孔宽长、切入深度/凸出高度和旋转角，并支持恢复原参数。
- 点击“重新预检并自动执行”后仍会重新运行受限中文解析、OpenCascade 单 Solid 校验和精确干涉预检；只有通过后才保存快照和写入模型。
- 曲面矩形和槽孔仍是真实 UV 点击位置上的切平面安全近似，不是任意曲面贴合或测地线轮廓；稳定面仍是“几何签名匹配第一版”。

2026-07-21 完整回归通过：前端 17 个测试文件 125/125、Rust 28/28、稳定 CAD Worker 12/12、曲面局部特征 23/23、参数化安全重放 11/11、曲面命中 7/7；生产构建、Rust 检查、Python 语法编译和界面风险面板检查均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。


### 36. 切线连续边链圆角与倒角第一版（已实现）

- 新增受限操作 `fillet-edge-chain` 与 `chamfer-edge-chain`。用户仍只点击一条种子稳定 CAD 边；只有中文指令明确包含“切线链、相切边、切线连续、连续边链、沿切线或顺着切线”时才启用传播，没有这些关键词时继续执行单边操作。
- Worker 从种子边两个端点开始，在当前 OpenCascade 实体中按顶点距离容差 `max(1e-6, min(1e-3, 包围盒对角线 × 1e-7))` 查找后继边；相邻边切向夹角不得超过 5 度，每个端点只能存在一个合格后继。没有可传播后继、出现多个候选形成分叉、边链少于 2 条或超过 64 条时都会用中文拒绝，不会静默退化为单边。
- 切线链允许种子边所属面为平面或非平面；非平面仍必须通过当前修订真实 UV、真实曲面点、外法线和目标边距离复核。整圈与切线链关键词同时出现时会拒绝，要求用户明确选择传播范围。
- 所有传播边一次性交给同一个 OpenCascade 固定半径圆角或等距倒角操作。结果继续要求有效、封闭、单一 Solid、体积变化可验证，并返回 `affectedEdgeCount` 与 `edgeScope=tangent-chain`。
- `localFeatures[]` 只保存种子稳定边、几何签名快照和受限参数；参数化重放每次都从当前实体重新推导切线链，不保存或复用旧边数组。稳定面和稳定边仍只能称为“几何签名匹配第一版”，不是永久拓扑命名。
- 当前仍不支持手工多选边链、分叉链、可变半径、连续性等级控制和永久拓扑命名。

2026-07-21 第 36 阶段完整回归通过：前端 18 个测试文件 136/136，Rust 29/29，稳定 CAD 单边/整圈/切线链边特征 18/18，参数化局部特征安全重放 15/15，曲面局部特征 23/23，平面局部特征 7/7，曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、全部 Modeling 回归、Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。


### 37. 手工多选边链圆角与倒角第一版（已实现）

- 新增视口“多选边链”模式和受限操作 `fillet-edge-manual-chain`、`chamfer-edge-manual-chain`。用户可在同一个 CAD 零件中按顺序选择 2–64 条稳定边；每次点击都要完成 OpenCascade 精确曲面点、外法线、UV 和稳定边解析。
- 手工目标可跨不同所属稳定面，但必须构成一条无分叉开放链或闭合链。Worker 会按真实物理边去重，拒绝重复选择同一物理边、不连续选择、分叉拓扑、退化边和数量越界，不会猜测或补全用户没有选择的边。
- Codex 结构化计划必须原样保留逐边 `stableFaceId + stableEdgeId` 顺序，不能增边、删边、换序、替换边或退化为单边操作；Tauri、浏览器本机 API、TypeScript 请求层和 Python Worker 都执行相同的目标绑定校验。
- 整条边链一次进入同一个 OpenCascade 固定半径圆角或等距倒角操作，返回 `affectedEdgeCount` 与 `edgeScope=manual-chain`。成功后中文摘要显示实际边数，旧选择自动失效；失败时保留修改前模型和全部手工选择。
- `localFeatures[]` 持久化逐边稳定 ID、所属面和几何签名快照。参数化重放按原始顺序逐边重新定位当前物理边并刷新快照；任一边失配会原子拒绝整次重建，不替换最后有效模型。
- 当前不支持可变半径、连续性等级控制、分叉边网、多个独立边链或永久拓扑命名；稳定面与稳定边仍属于“几何签名匹配第一版”。

2026-07-21 第 37 阶段完整回归通过：前端 18 个测试文件 141/141，Rust 33/33，稳定 CAD 单边/整圈/切线链/手工边链边特征 22/22，参数化局部特征安全重放 17/17，曲面命中 7/7；TypeScript/Vite 生产构建、`cargo check`、Rust 测试、Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

### 39. 任意上传 STL 的顶点、边、面手工位移第一版（已实现）

- 面向用户上传的任意 STL，不绑定 ESP32 或任何固定 Demo。上传模型处于原始装配视图时，全中文“网格元素编辑”面板支持选择单个顶点、单条三角边或单个三角面，并按源模型 XYZ 毫米坐标输入精确位移。
- 视口从当前命中三角面选择最近顶点或最近边，分别显示黄色球体、粗线或半透明三角面高亮。选择绑定当前模型修订号；模型被重新导入或修改后，过期选择会被拒绝并要求重新点击。
- STL 分面网格常把同一几何顶点重复存储在多个三角面中。Worker 使用六位小数坐标键同步移动全部同坐标副本，避免只移动一个三角面的数组项而撕开封闭模型。
- 修改前拒绝 NaN、Infinity、全零位移、越界索引和超过 ±500 毫米的单轴位移；修改后拒绝退化三角面、非封闭结果、无效实体、Solid 数量变化和需要自动修洞或清理才能成立的结果。
- Worker 重新导入 OpenCascade，并通过临时文件、批量原子替换和失败回滚更新工作 STL、STEP 与模型清单。任何校验失败都不会覆盖最后有效模型。
- 成功后刷新上传模型修订号，清除旧拆件和壁厚结果，创建中文版本记录及修改前后本机快照；第 40 阶段已实现工作 STL、STEP 和清单的直接恢复，因此上传网格修改已经支持真实文件级撤销、重做和版本恢复。
- 参数化 CAD 继续使用稳定面和稳定边局部特征，不把 OpenCascade BRep 顶点直接改写伪装成安全能力。该阶段不支持拓扑增删、挤出、焊接、分裂、框选多元素或自由雕刻；框选多元素已在第 41 阶段以受限批量位移方式补齐。

2026-07-21 第 39 阶段回归结果：前端 23 个测试文件 162/162，Rust 35/35，Python 网格元素编辑 4/4；生产构建、OpenCascade 写回和浏览器交互验收通过。浏览器实测上传任意 STL、选择顶点、执行 0.1 毫米位移、刷新修订、生成中文结果并保持控制台无错误。

### 40. 上传模型精确快照恢复与完整撤销/重做（已实现）

- 首次导入任意 STL 就固定生成原始 STL、工作 STL、工作 STEP 和模型清单；上传模型版本快照与参数化 CAD 快照分开保存，并绑定创建时的不可变模型修订号。
- 只允许恢复 `artifacts/versions` 下受管版本目录的直接子目录，固定读取 `imported-model.stl`、`imported-model-working.stl`、`imported-model-working.step` 和 `imported-model-result.json`；任意外部路径、路径穿越、符号链接逃逸和缺失文件都会被拒绝。
- Python Worker 使用 OpenCascade 重新检查 STL 与 STEP 的有效性、封闭性、Solid 数量和体积一致性，并复核清单声明；全部通过后才以临时文件、批量原子替换和失败回滚恢复工作模型。
- 撤销、重做和版本历史恢复只有在真实模型文件恢复成功后才移动历史索引；失败时保留当前版本和最后有效上传模型。恢复成功后旧拆件、壁厚、网格选择、局部修改和版本几何对比全部失效。
- Web 模式不提供本机精确上传快照恢复；上传 STL 快照不能参与参数化 CAD STEP 布尔差集。当前仍不支持跨项目导入版本快照、任意外部快照路径或云端版本同步。

2026-07-21 第 40 阶段完整回归：前端 24 个测试文件 165/165、Rust 37/37、上传快照恢复 Worker 3/3、拆件与补面 27/27、网格元素编辑 4/4；生产构建、Cargo 检查、Python 语法、Rust 格式和差异格式检查均通过。

### 41. 任意上传 STL 框选多元素批量位移第一版（已实现）

- 全中文“网格元素编辑”面板新增“点击单选 / 框选多选”。框选矩形使用当前摄像机把源三角面的顶点、三维边中点和三维面重心投影到屏幕，深度超出 `[-1, 1]` 的候选不会进入集合；第一版为穿透框选，因此会明确提示可能包含被遮挡区域中的元素。
- 顶点按六位小数源毫米坐标去重，无向边按排序后的两个源端点去重，面按当前上传模型修订中的 `triangleIndex` 去重。超过 512 个不同候选时按网格遍历顺序保留前 512 个，并在中文界面提示已达到安全上限。
- 每个元素继续携带 `selectionRevision + triangleIndex + elementIndex + triangleMm`。Vite、Tauri/Rust 和 Python Worker 三层独立校验集合类型、同类元素、数量、索引、坐标和修订；Rust 通过标准输入把选择集合交给 Python，避免把大 JSON 写入命令行参数。
- Python 会使用 `triangleMm` 与当前工作 STL 逐三角面复核，并从集合生成唯一源坐标键；统一位移后继续执行退化面、OpenCascade 封闭性、有效性、Solid 数量和体积检查。工作 STL、STEP、清单与结果文件仍通过临时文件、批量原子替换和失败回滚提交。
- 视口使用合并 `Points`、`LineSegments` 和面 `BufferGeometry` 高亮整个选择集合。修改成功后旧选择按修订失效，并创建中文“批量移动上传模型顶点/边/面”版本；上传模型快照、撤销、重做和版本恢复沿用第 40 阶段的真实文件事务。
- 该能力是受限的同类元素统一位移，不支持未受约束自由雕刻、拓扑增删、焊接、分裂或挤出。

2026-07-21 第 41 阶段完整回归：前端 24 个测试文件 171/171、Python 网格元素编辑 7/7、拆件与补面 27/27、Rust 42/42；TypeScript/Vite 生产构建、Cargo 检查、全部 Modeling Python 语法编译、Rust 格式和 `git diff --check` 均通过。浏览器实测框选 8 个顶点、18 条无向边和 12 个三角面，并将 8 个顶点统一沿 Z 轴移动 0.1 毫米；修订与版本从 2/2 更新到 3/3，体积保持 3200.00 立方毫米，页面重载后控制台无错误。

### 42. 任意上传 STL 选择集合统一旋转与均匀缩放第一版（已实现）

- “网格元素编辑”面板在位移之外新增全中文“旋转”和“缩放”操作；点击单选与框选多选、顶点/边/面三种类型继续复用同一选择集合协议。
- 旋转与缩放都先对选择集合涉及的唯一源毫米坐标去重，再以这些坐标的几何中心作为唯一枢轴。旋转只允许源模型 X、Y、Z 单轴，角度限制为 `-180° ～ 180°` 且不能为 0；均匀缩放限制为 `0.25 ～ 4 倍` 且不能等于 1。
- TypeScript、Vite、Tauri/Rust 和 Python Worker 独立校验操作类型、旋转轴、角度和缩放比例；缺少旋转轴、越界数值、无实际变化、未知操作和单顶点无效旋转/缩放都会用中文拒绝。
- Worker 继续逐三角面复核 `triangleMm`，同步更新所有同源坐标的 STL 顶点副本，并执行退化面、封闭性、OpenCascade 有效性、Solid 数量和体积安全检查；工作 STL、STEP、清单和结果文件保持临时写入、批量原子替换与失败回滚。
- 修改成功后生成绑定新修订的中文版本，清除已过期选择、旧拆件和壁厚结果，并沿用桌面端上传模型精确快照的真实文件级撤销、重做和版本恢复。Web 开发模式不会伪造本机精确快照恢复。
- 能力边界仍是受限仿射变换，不改变网格拓扑，不支持挤出、焊接、分裂、补洞式雕刻或任意自由曲面重建。

2026-07-21 第 42 阶段完整回归：前端 24 个测试文件 173/173、Rust 45/45、Python 网格元素变换 9/9、上传模型快照恢复 3/3、拆件与补面 27/27；TypeScript/Vite 生产构建、Cargo 检查与测试、全部 Modeling Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。浏览器实测框选 8 个顶点后绕 Z 轴旋转 10°，体积保持 3200.00 立方毫米；重新框选后按 0.9 倍均匀缩放，体积变为 2332.80 立方毫米，两个操作均更新修订和中文版本并使旧选择失效，当前 1421 页面控制台无新增错误。

### 43. 参数化 CAD 零件转受管网格分支第一版（已实现）

- 参数化 CAD 视图新增全中文“CAD 转网格分支”入口。用户必须主动选择当前修订中的任意零件并确认；实现不绑定主体、上盖、ESP32 或固定模型角色。
- Tauri/Rust 和浏览器开发后端都按当前 CAD 修订解析 `generation-result.json`，只接受清单声明的普通 STL 文件名，拒绝旧修订、未知零件、路径穿越、空文件和超过 50 MB 的来源。
- 所选零件的真实毫米 STL 会进入独立的 `imported-model.stl`、工作 STL、工作 STEP 和模型清单。原参数化 CAD 版本不被覆盖，用户可从版本历史切回并重新生成精确实体。
- 网格分支复用点击单选、穿透框选、顶点/边/面高亮，以及统一位移、X/Y/Z 单轴旋转和均匀缩放；界面明确说明网格修改后不再保证参数化特征可编辑。
- `branchSource` 持久化源 CAD 修订、零件 ID、中文名称和源 STL 文件；网格变换、局部 STL 布尔修改、桌面精确快照和恢复后都继续保留来源。版本历史区分“精确 CAD”“普通上传模型”和“CAD 网格分支”。
- Web 开发模式可以真实创建和编辑工作网格，但没有本机受管版本目录时会明确显示缺少精确快照，不会伪造可恢复状态。第一版不合并多个 CAD 零件，不把编辑后的网格伪装回精确 B-Rep，也不开放拓扑增删、焊接、分裂、面挤出或自由雕刻。

2026-07-22 第 43 阶段完整回归：前端 25 个测试文件 177/177、Rust 47/47、Python 网格元素编辑 10/10、局部 STL 编辑 5/5、上传模型快照恢复 4/4、拆件与补面 27/27；TypeScript/Vite 生产构建、Cargo 检查与测试、全部 Modeling Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。浏览器实测从当前 CAD 的“主体”创建真实网格分支，尺寸为 62.60 × 32.60 × 12.60 毫米，进入“CAD 派生网格分支”编辑面板并生成中文版本，页面控制台无新增警告或错误。

### 44. 受管单 Solid 网格的单三角面法向加料与压入第一版（已实现）

- 普通上传 STL 与任意 CAD 派生网格分支共用全中文“法向编辑”入口。进入后固定切换为“选择面 + 点击单选”，只接受当前模型修订中的一个真实三角面；框选、多面、顶点和边请求不会进入 Worker。
- 用户可选择“向外加料”或“向内压入”，作用距离限制为 0.20～100.00 毫米。Worker 从 `triangleMm` 重新计算几何法线，并使用 OpenCascade 实体内外分类确认真实外法线，不信任 STL 法线字段。
- 工具体是由源三角面、真实外法线、受限距离和极小实体重叠量构造的封闭三角柱。向外模式执行真实布尔并集，向内模式执行真实布尔切除；成功结果返回外法线、作用距离、工具体积和修改前后体积。
- 写回前必须通过 OpenCascade 有效性、封闭性、单一 Solid、体积变化方向、STEP/STL 导出和 STL 再导入检查。工作 STL、STEP、模型清单和结果清单继续使用临时文件、批量原子替换与失败回滚，并保留 `branchSource`、桌面精确快照、撤销、重做和版本恢复语义。
- 如果选中三角面触碰现有锐边、开孔边界或复杂分面边界，OpenCascade 布尔后的 STL 可能出现非流形共边。第一版不会绕过检查或猜测拓扑，而是以“法向编辑导出结果未通过网格检查”开头的中文错误安全拒绝，保留最后有效模型。
- Vite 本机开发后端已把无返回值建模 Worker 的标准输出改为直接丢弃并继续读取标准错误，避免大型模型清单填满未消费管道而阻塞 CAD 重建、STL 上传和后续编辑请求。
- 第一版不支持框选多面挤出、相邻面区域自动扩展、侧壁斜度、倒角联动、拓扑焊接、任意方向或自由雕刻。

2026-07-22 第 44 阶段完整回归：前端 25 个测试文件 179/179、Rust 49/49、Python 网格元素编辑 13/13、局部 STL 编辑 5/5、上传模型快照恢复 4/4、拆件与补面 27/27；TypeScript/Vite 生产构建、Cargo 检查与测试、全部 Modeling Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。浏览器验收使用 30 × 24 × 12 毫米、8640.00 立方毫米的普通上传长方体：单三角面沿真实外法线加料 2 毫米后体积为 9360.00 立方毫米；重新上传原模型后，沿真实内法线压入 2 毫米后体积为 7920.00 立方毫米。两次操作都生成中文结果、新修订和版本，并通过单 Solid 与封闭性检查。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

### 45. 点击种子三角面并扩展连续共面区域的加料与压入第一版（已实现）

- 用户只需点击当前修订中的一个种子三角面并选择“共面区域”。Worker 以共享无向边建立邻接关系，从种子面执行连续区域扩展；只接收法线夹角不超过 0.5°、全部顶点到种子平面距离不超过动态公差的相邻三角面，不跨越锐边、曲面或断开的同平面区域。
- 动态平面距离公差为 `max(0.00001, min(0.02, 模型对角线 × 0.000001))` 毫米。区域最多包含 20,000 个三角面、面积最多 200,000 平方毫米；种子三角面自身也参与数量和面积上限检查，超限时以中文拒绝且不写回模型。
- Worker 从区域边界提取闭合外环和孔洞环，构造一个带孔平面 Face，再沿经实体内外分类确认的真实法线生成一个封闭棱柱工具体。加料只执行一次并集，压入只执行一次切除，避免把共面补片拆成互相重叠的多个三角柱。
- 共享边出现三个及以上三角面、边界无法形成闭环、环自交或平面 Face 无法构造时，系统安全拒绝。成功结果新增受影响三角面数量、区域面积、边界环数量、法线夹角公差和平面距离公差，并继续保留当前修订复核、OpenCascade 有效性/封闭性/单 Solid/体积方向、STL 再导入、原子回滚、桌面精确快照和 `branchSource`。
- 带孔测试使用 20 × 16 × 10 毫米盒体和顶面中心直径 4 毫米贯穿孔：自动扩展 130 个三角面，识别 2 个边界环，区域面积约 307.44 平方毫米；向外加料 2 毫米后的体积增量与区域面积乘距离一致，中心孔保持贯穿。

2026-07-22 第 45 阶段完整回归：前端 25 个测试文件 179/179、Rust 49/49、Python 网格元素编辑 15/15、局部 STL 编辑 5/5、上传模型快照恢复 4/4、拆件与补面 27/27；TypeScript/Vite 生产构建、Cargo 检查与测试、全部 Modeling Python 语法编译、Rust 格式检查和 `git diff --check` 均通过。浏览器验收使用 30 × 24 × 12 毫米、8640.00 立方毫米的通用长方体 STL：点击顶面一个种子三角面后自动扩展 2 个连续共面三角面、区域面积 720.00 平方毫米；向外加料 2 毫米后体积为 10080.00 立方毫米，重新上传原模型后向内压入 2 毫米后体积为 7200.00 立方毫米，体积变化分别为 +1440.00 和 -1440.00 立方毫米。两次结果均有效、封闭并保持单一 Solid。仅保留既有 Vite 大分块警告与 CadQuery/Pyparsing 弃用警告。

### 46. 连续共面区域执行前选区预览与边界测量第一版（已实现）

- 点击一个种子三角面后，前端会按与桌面 Worker 相同的共享无向边、0.5° 法线夹角、动态平面距离公差、20,000 面和 200,000 平方毫米上限扩展区域，并高亮完整连续共面补片。
- 中文面板在执行布尔前显示预计三角面数量、区域面积、边界环数量、法线夹角公差和平面距离公差；非流形共享边、开口、分叉和退化边界会在前端中文阻断。
- 元素类型、点击/框选方式、变换操作、选择、模型修订、导入/恢复或视图变化都会清除旧预览。预览只用于交互反馈，正式执行仍只发送一个种子三角面，由 Worker 独立重新扩展并执行完整安全检查。

2026-07-22 第 46 阶段验证：前端 25 个测试文件 185/185，TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器从通用 CAD 主体创建任意网格分支后，点击共面区域显示 264 个三角面、245.50 平方毫米、2 个边界环、0.5° 和 0.00007 毫米公差；页面成功显示完整中文预览卡。


### 47. 连续共面区域边界环线框与修订级拓扑缓存第一版（已实现）

- 连续共面区域预览现在返回源毫米坐标下的全部闭合边界环；视口在黄色区域高亮之上叠加青绿色线框，同时显示区域外环和孔洞环，帮助执行前检查是否误跨区域。
- 第一版把外环和孔洞环统一使用青绿色显示，不按语义分别命名、着色或排序；Worker 仍独立重建拓扑、识别 Wire 语义并执行全部安全校验，前端线框不是可信建模输入。
- 视口只在当前模型修订第一次选择种子三角面时扫描全部三角面并构建索引、共享无向边 owners 与源坐标表；同一修订切换种子面直接复用拓扑缓存。
- 缓存不进入 Zustand Store，并会随模型修订、重新导入、版本恢复、Three.js 几何实例、坐标变换或视图来源变化失效；边界线框几何随预览生命周期主动释放。
- 单元测试覆盖普通单外环、带孔双环、同一拓扑多种子复用、不连续三角面索引，以及迭代入口与缓存入口结果一致。

2026-07-22 第 47 阶段验证：前端 25 个测试文件 187/187；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器从通用 CAD 主体创建网格分支，侧面区域显示 2 个三角面、655.20 平方毫米和 1 个边界环；带接口开孔区域显示 8 个三角面、194.76 平方毫米和 2 个边界环。黄色区域与青绿色外环、孔洞环均可见；切换操作会清除线框，重新选择约 41 毫秒出现预览；当前页面无 Console 错误或警告。仅保留既有 Vite 大分块警告。

### 48. 连续共面区域外环/孔洞语义分类与尺寸测量第一版（已实现）

- 闭合边界环先投影到由种子平面法线建立的稳定二维基底，再使用环包含关系计算嵌套深度；外环和孔洞不依赖 STL 三角面绕序、边界数组顺序或有符号面积方向。
- 执行前预览分别显示外环与孔洞数量，并为每个环显示真实三维闭合周长和种子平面二维包围尺寸。`boundaryLoops` 保留源毫米坐标、语义、周长、包围尺寸和嵌套深度，兼容字段 `boundaryLoopsMm` 继续保留。
- 视口保持黄色区域面，外环使用青绿色线框，孔洞使用珊瑚色线框；面板提供全中文测量列表和图例，多个孔洞可独立列出。
- 前端分类只用于交互反馈。桌面 Worker 不读取或信任 `boundaryLoops`，仍从当前工作 STL 独立扩展区域、判断 OpenCascade Wire 语义并完成全部安全校验。
- 第一版只接受单层“外环 + 孔洞”结构；检测到嵌套岛时以中文安全拒绝，不扩展到曲面区域、多区域框选或自由编辑。

2026-07-22 第 48 阶段验证：前端 25 个测试文件 188/188，TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器从通用 CAD 主体创建网格分支并以真实鼠标点击带接口开孔区域，显示 8 个三角面、194.76 平方毫米、外环 1 个、孔洞 1 个；外环周长 70.40 毫米、包围 24.60 × 10.60 毫米，孔洞周长 34.00 毫米、包围 11.00 × 6.00 毫米。黄色区域、青绿色外环和珊瑚色孔洞环均可见，正常鼠标交互下页面无 Console 错误。仅保留既有 Vite 大分块警告。

### 49. 连续共面区域边界环点击聚焦与视口尺寸标注第一版（已实现）

- 外环和孔洞测量列表改为可访问按钮，用户可点击任意环进行聚焦；当前项显示“已聚焦”，再次点击同一项可取消，切换其他项会直接移动聚焦状态。
- 视口在聚焦时降低其余环线框亮度，并使用加粗的原语义颜色线条强化当前外环或孔洞；模型附近显示全中文尺寸卡，包含环名称、真实周长、平面包围宽度和高度。
- 聚焦索引由 Store 管理，只允许引用当前 `MeshPlanarRegionPreview.boundaryLoops` 中的有效环；新预览、模型修订、导入、恢复、元素种类、选择方式、选择集合或变换操作变化时自动清除。
- 聚焦和标注只属于执行前查看状态，不进入 Worker 请求、不改变共面区域扩展，也不改变 OpenCascade Wire 和布尔语义。

2026-07-22 第 49 阶段验证：前端 25 个测试文件 188/188，TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器以真实鼠标在 8 个三角面、194.76 平方毫米的带孔区域中分别聚焦孔洞 1 和外环 1：孔洞标注显示周长 34.00 毫米、宽度 11.00 毫米、高度 6.00 毫米；外环标注显示周长 70.40 毫米、宽度 24.60 毫米、高度 10.60 毫米。再次点击外环后标注消失，页面无 Console 错误。仅保留既有 Vite 大分块警告。

### 50. 连续共面区域边界环视口直接拾取与顺序导航第一版（已实现）

- 带多个边界环的共面区域预览新增全中文“上一个环 / 下一个环”导航；未聚焦时向后从第一个环开始、向前从最后一个环开始，首尾循环，并同步显示“当前：尚未聚焦 / 外环 1 / 孔洞 1”。
- 视口为每个有效边界环建立透明宽线拾取层。点击真实外环或孔洞线框会聚焦、切换或取消当前环，并阻止该次命中继续落到种子三角面；点击非线框区域仍可正常重新选择面。
- 环坐标转换、名称、颜色、中心和闭合点列统一由同一份只读渲染数据生成；既有青绿色/珊瑚色聚合 `BufferGeometry` 继续显式释放，避免破坏修订级拓扑缓存和显存清理约定。
- 拾取与导航只在当前上传模型修订的“共面区域”预览中启用；聚焦索引不进入 Worker、版本快照或正式建模请求，预览失效时仍由 Store 清除。
- 第一版不支持拖动边界、尺寸驱动修改、曲面环、自由编辑或改变 OpenCascade Wire 语义。

2026-07-22 第 50 阶段验证：前端 25 个测试文件 190/190，TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器实测带接口开孔区域仍为 8 个三角面、194.76 平方毫米、外环 1 个和孔洞 1 个；“上一个环 / 下一个环”可首尾循环，当前文本与列表按下状态同步；直接点击孔洞环可取消聚焦、点击外环可聚焦，点击非线框底面可重新生成 138 个三角面的预览，页面无 Console 错误。仅保留既有 Vite 大分块警告。

**下一阶段：**实现连续共面区域尺寸辅助线与标注避让第一版。为聚焦环在种子平面上绘制宽度、高度方向的只读尺寸辅助线、端点短线和中文数值标注，并根据环包围范围把标注偏移到轮廓外侧，减少遮挡模型和孔洞。继续保持当前修订绑定和只读语义，不扩展为尺寸驱动编辑、曲面测量或自由雕刻。

### 51. 连续共面区域尺寸辅助线与标注避让第一版（已实现）

- 每个平面边界环现在保留种子平面的稳定测量基底、二维 U/V 包围范围和源毫米原点；这些数据只服务于当前前端预览，不进入 Worker 请求、项目版本或精确快照。
- 新增纯函数生成宽度与高度主尺寸线、两侧延伸线、端点短线和三个互相分离的轮廓外标签锚点；偏移会随环尺寸变化，并对小孔保持至少 1.5 毫米的可读间距。
- 聚焦外环或孔洞时，视口使用对应的青绿色或珊瑚色绘制尺寸线；宽度、高度分别显示独立中文毫米标签，原摘要卡移动到第三个外侧方向且只保留环名称和周长，减少遮挡孔洞与模型。
- 尺寸辅助线继续绑定当前上传模型修订和共面区域预览；切换环时旧辅助线立即消失，取消聚焦时全部尺寸线与标签清除，透明边界拾取层和非线框模型选面保持原有行为。
- 第一版仍为只读显示，不支持拖动辅助线、尺寸驱动编辑、曲面环、嵌套岛或自由雕刻。

2026-07-22 第 51 阶段完整回归：前端 25 个测试文件 190/190；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器实测带孔区域仍为 8 个三角面、194.76 平方毫米；外环显示“宽度 24.60 毫米”“高度 10.60 毫米”，孔洞显示“宽度 11.00 毫米”“高度 6.00 毫米”，摘要卡分别显示环名称和 70.40/34.00 毫米周长。直接点击当前孔洞环可取消全部尺寸标注，点击非线框底面可重新生成 138 个三角面的预览，Console 无错误。仅保留既有 Vite 大分块警告。

**下一阶段：**实现连续共面区域尺寸标注视口自适应翻转第一版。根据当前相机投影和视口安全区判断宽度、高度及摘要标签是否接近顶部工具栏、右侧编辑面板或视口边缘，并在候选外侧方向之间自动翻转，避免标签被裁切或压到操作面板下方。继续保持纯显示、当前修订绑定和 Worker 请求不变；第一版不支持标签拖动、尺寸驱动编辑、曲面测量或自由雕刻。


### 52. 连续共面区域尺寸标注视口自适应翻转第一版（已实现）

- 基于同一稳定源毫米测量基底，为宽度、高度和摘要生成四组有限外侧候选；宽度与摘要始终位于 U/V 平面的相反侧，高度可在左右两侧翻转，避免相机变化时重新推导测量结果。
- 每帧仅把候选标签锚点投影到当前相机和画布坐标，按顶部工具栏、右侧网格编辑面板与画布边缘安全区计算溢出惩罚，并叠加标签矩形重叠惩罚；同分时保持候选顺序，只有候选索引变化才更新 React 状态。
- Drei `Html.calculatePosition` 对最终屏幕坐标再次夹紧，确保所有三维候选都接近遮挡区时，中文标签仍不会进入工具栏、右侧面板或画布裁切区。
- 相机旋转、缩放、平移、视口尺寸和聚焦环变化都会自动重新投影；取消聚焦后候选、辅助线和标签一起清除。该能力仍是纯显示状态，不进入 Worker、Store 持久数据、项目版本或精确快照。
- 第一版不支持手工拖动标签、尺寸驱动编辑、曲面环、嵌套岛或自由雕刻。

2026-07-22 第 52 阶段完整回归：前端 25 个测试文件 192/192；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器多角度旋转时布局索引在候选 0、1、2 之间按区域与相机自动切换；所有标签右边界均小于右侧面板起点 606 像素，且未进入顶部工具栏或画布裁切区。带孔端壁外环仍显示 24.60 × 10.60 毫米和 70.40 毫米周长，孔洞仍显示 11.00 × 6.00 毫米和 34.00 毫米周长；取消孔洞聚焦后标签全部清除，Console 无错误。仅保留既有 Vite 大分块警告。

**下一阶段：**实现连续共面区域法向加料/压入工具体视口预演第一版。根据当前区域外环、孔洞、真实法向操作和毫米距离，在执行前显示带孔半透明棱柱与方向提示；参数、选择或修订变化时实时更新或清除，后端仍独立重建并完成全部实体安全校验。

### 53. 连续共面区域法向加料/压入工具体视口预演第一版（已实现）

- 当前修订的共面区域预览会从唯一外环、全部孔洞环和稳定 U/V 测量基底生成带孔二维轮廓；环的读取按 `kind` 分类，不依赖预览数组顺序。
- 前端根据闭合 STL 的有符号体积修正种子三角面绕序，得到仅供预演使用的外法线；开口或近零体积样本保留种子方向作为回退。桌面 Worker 仍会独立使用 OpenCascade 确认真正实体内外。
- “向外加料 / 向内压入”和作用距离已改为视口与面板共享的瞬时状态。有效范围 0.20 至 100.00 毫米内，视口实时生成青绿色或珊瑚色半透明带孔棱柱、法向方向线和全中文毫米标签；无效输入或上下文失效时立即清除。
- 工具体和方向线显式禁用射线拾取，HTML 标签禁用指针事件，不会阻止边界环聚焦、尺寸标注或模型选面；预演不写入 Worker 请求、项目版本和精确快照。
- 第一版只显示工具体，不声称显示 OpenCascade 布尔后的最终实体，也不支持拖动工具体、侧壁斜度、倒角联动、曲面区域、多种子区域或自由雕刻。

2026-07-22 第 53 阶段完整回归：前端 25 个测试文件 193/193；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器在带接口端壁验证 8 个三角面、194.76 平方毫米、外环 1 个、孔洞 1 个；默认显示“向外加料预演 · 2.00 毫米”，切换后显示“向内压入预演 · 6.00 毫米”，输入 0.1 毫米时预演立即清除并显示中文范围错误。聚焦孔洞后工具体、11.00 × 6.00 毫米尺寸与 34.00 毫米周长仍可同时查看，Console 无错误。仅保留既有 Vite 大分块警告。

### 54. 连续共面区域工具体预演轮廓强化与标签视口避让第一版（已实现）

- 工具体 profile 现在同时派生起始端和末端的外环、全部孔洞闭合线组；末端轮廓更粗、更亮，孔洞使用较浅的加料/压入语义色，使半透明端面和贯穿孔在倾斜视角下仍可辨识。
- 法向方向线末端增加沿稳定 U/V 平面轴生成的只读十字标记，标记半径限制为 0.35 至 2.00 毫米；方向线、十字和全部轮廓都不接收射线拾取且关闭深度写入，不妨碍边界环聚焦、模型选面和尺寸标签交互。
- 中文预演标签复用屏幕安全区夹紧能力，随相机投影保持在顶部工具栏、右侧编辑面板和画布裁切区之外；标签仍禁用指针事件。
- 纯函数会拒绝无效距离、少于三个点或零面积的环、非有限点及零长度基底轴，并验证起止端对应点沿法向相隔真实作用距离。所有新增数据只供显示，不进入 Worker、项目版本或精确快照。
- 第一版仍只显示工具体，不计算或宣称近似布尔最终实体，也不支持拖动、侧壁斜度、倒角联动、曲面区域、多种子区域或自由雕刻。

2026-07-22 第 54 阶段完整回归：前端 25 个测试文件 193/193；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器在带接口端壁验证 8 个三角面、194.76 平方毫米、外环 1 个和孔洞 1 个；向外加料 2.00 毫米与向内压入 6.00 毫米均显示起止端轮廓、孔洞轮廓和方向末端十字，多角度旋转时中文标签保持在安全区内，聚焦孔洞后尺寸标注与预演可同时查看，Console 无错误。仅保留既有 Vite 大分块警告。

### 55. 连续共面区域工具体起止端侧边连接线与遮挡层级强化第一版（已实现）

- 每个外环和任意数量孔洞的唯一顶点现在都会连接对应起止端，形成真实毫米长度的侧边线；闭合重复点不会额外生成连接线。
- 外环侧边使用主要加料/压入语义色和较高透明度，孔洞侧边使用较浅颜色和透明度，使贯穿孔侧壁与外轮廓在半透明工具体内保持层级区分。
- 同类侧边统一合并为外环、孔洞两份 `LineSegments` 几何，避免复杂 STL 的长边界逐边创建 React 组件；几何随 profile、修订或操作变化释放并重建。
- 所有连接线禁用射线拾取、深度测试和深度写入，不拦截环拾取、模型选面、尺寸标签、相机旋转或缩放；本阶段仍不修改 Worker、项目版本和精确快照。

2026-07-22 第 55 阶段完整回归：针对性测试 20/20，前端 25 个测试文件 193/193；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器带孔端壁为 8 个三角面、194.76 平方毫米、外环 1 个和孔洞 1 个；向外加料 2.00 毫米及向内压入 6.00 毫米时，起止端外环/孔洞轮廓、方向十字和侧边连接线均可同时查看，多角度旋转后层级清晰，标签保持在安全区，Console 无错误。仅保留既有 Vite 大分块警告。

### 56. 连续共面区域工具体面积与体积只读摘要第一版（已实现）

- 执行前预演会从唯一外环面积减去任意数量孔洞总面积，得到与当前二维工具体一致的净作用面积；环绕序反转不会改变结果。
- 使用净作用面积乘当前正作用距离得到理论体积，并在面板和视口统一标注“工具体估算”；该值不代表 OpenCascade 布尔后的模型净体积变化，也不是打印材料精确用量。
- 非有限点、少于三个点、零面积环、孔洞总面积不小于外环或非正距离会拒绝生成摘要；操作方向、距离、区域、修订或视图来源变化后会实时更新或清除。
- 面板同步显示外环面积、孔洞总面积、净作用面积和工具体估算；视口标签显示操作方向、距离、净作用面积和估算体积，并扩大安全区占位避免进入顶部工具栏、右侧面板或画布裁切区。

2026-07-22 第 56 阶段完整回归：针对性测试 20/20，前端 25 个测试文件 193/193；TypeScript/Vite 生产构建和 `git diff --check` 通过。自动测试覆盖单孔、多孔、外环与孔洞反向绕序、非有限点、少点环、零面积、孔洞面积不小于外环和无效距离。浏览器以 50.00 平方毫米通用共面区域验证向外 2.00 毫米得到 100.00 立方毫米、向内 6.00 毫米得到 300.00 立方毫米，面板与视口数值同步；0.10 毫米输入会立即清除度量并显示中文范围错误，标签保持在安全区，Console 无错误。仅保留既有 Vite 大分块警告。

### 57. 连续共面区域执行结果体积对照卡第一版（已实现）

- 网格编辑面板复用当前成功结果中的 OpenCascade 工具体积与模型体积变化，统一以立方毫米显示“实际工具体积”“模型体积变化”和“实际作用比例”；压入结果使用体积变化绝对值，不把负号误显示为负比例。
- 对照卡只接受当前上传模型修订的 `extrude-face` 结果和合法加料/压入模式。切换到其他网格编辑结果、恢复到其他修订、重新导入模型或结果缺失时，旧卡片不会继续显示。
- 工具体积必须为正有限数，体积变化必须有限；轻微浮点超出 100% 会夹紧，明显超过工具体积的异常结果会安全隐藏，避免用错误数据解释模型变化。
- 加料卡说明工具体可能与已有实体重叠，压入卡说明工具体可能超出实体厚度并被裁剪；两者都明确提示该比例不是打印材料用量。

2026-07-22 第 57 阶段完整回归：针对性测试 24/24，前端 25 个测试文件 197/197；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器从通用 CAD 主体创建受管网格分支，在 50.00 平方毫米区域沿真实外法线加料 2.00 毫米；OpenCascade 返回实际工具体积 103.58 立方毫米、模型体积变化 100.00 立方毫米，对照卡显示实际作用比例 96.54%，中文说明和加料语义色完整，Console 无错误。仅保留既有 Vite 大分块警告。

### 58. 连续共面区域平面估算与 OpenCascade 工具体积偏差对照第一版（已实现）

- 成功的当前修订共面加料/压入结果会复用 `regionAreaMm2` 与 `distanceMm`，计算“执行前平面估算”，并与 OpenCascade 返回的实际工具体积形成有符号差值和相对理论值百分比。
- 结果卡现在以两列四项保持“执行前平面估算 → 实际工具体积 → 模型体积变化 → 实际作用比例”链路，并在独立一行显示“与平面估算一致 / 高于 / 低于”；高于使用黄色、低于使用蓝色，不改变加料与压入的原语义色。
- 浮点差值在理论体积百万分之一或 `0.000001` 立方毫米以内归一为一致；绝对偏差超过 50% 时只隐藏偏差行，原 OpenCascade 执行结果卡仍可安全显示。
- 该对照只解释平面轮廓到工具体构造的几何差异，中文说明使用“可能来自 Wire 重建和几何容差”，不扩展为材料损耗、耗材误差、打印收缩或切片补偿。

2026-07-22 第 58 阶段完整回归：针对性测试 27/27，前端 25 个测试文件 200/200；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器从通用 CAD 主体创建受管网格分支，在 655.20 平方毫米共面区域沿真实外法线加料 2.00 毫米；卡片显示执行前平面估算 1310.40 立方毫米、实际工具体积 1357.38 立方毫米、高于 46.98 立方毫米（+3.58%）、模型体积变化 1310.40 立方毫米和实际作用比例 96.54%，两列布局清晰，Console 无错误。仅保留既有 Vite 大分块警告。

### 59. 连续共面区域三段体积链路方向一致性与异常提示第一版（已实现）

- 新增纯函数复核当前修订成功结果的 `faceExtrusionMode` 与有符号 `volumeDeltaMm3`：加料对应体积增加、压入对应体积减少，反向变化标记为方向矛盾。
- 工具体积百万分之一或 `0.000001` 立方毫米以内的变化归一为“体积近似未变化”，避免浮点噪声误报方向；过期修订、缺失模式、非有限值或既有体积比例异常时安全降级。
- 结果卡新增“平面轮廓 / 工具体 / 布尔作用”三段只读状态。方向一致使用青绿色、近似未变化使用黄色、方向矛盾使用红色；矛盾时额外显示“几何方向安全警告”和实际增减量。
- 状态只复核单次布尔几何，不自动回滚结果，不改变 Worker、Store、项目版本或快照协议，也不解释为材料利用率、耗材误差、打印收缩或切片质量。

2026-07-22 第 59 阶段完整回归：针对性测试 31/31，前端 25 个测试文件 204/204；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器在 655.20 平方毫米共面区域执行向外 2.00 毫米加料，三段状态显示“平面估算已计算 / 高于平面估算 / 加料增量一致”，同时保留平面估算 1310.40、实际工具体 1357.38、模型变化 1310.40 立方毫米、+3.58% 偏差和 96.54% 作用比例；布局清晰，Console 无错误。仅保留既有 Vite 大分块警告。

### 60. 连续共面区域执行结果中文诊断摘要与复制第一版（已实现）

- 当前修订的成功共面加料/压入结果现在可生成稳定的十行全中文诊断摘要，覆盖操作模式、区域面积、作用距离、平面估算、实际工具体积、构造偏差、有符号模型体积变化、实际作用比例和方向状态。
- 摘要复用既有三份纯比较结果；面积、距离或偏差不可用时显示“暂不可用”，过期修订、异常作用比例或无效结果不生成摘要，不伪造几何数值。
- 结果卡新增“复制几何诊断”。系统只在用户点击时调用剪贴板能力，成功显示“已复制到系统剪贴板，不会自动发送或保存”，失败显示中文权限提示；受限预览环境可在同一次点击中使用不持久化的兼容复制。
- 摘要不包含绝对路径、账号、密钥或源文件字段，也不会写入 Store、项目版本、精确快照或 Worker，更不会自动发送给 Codex 或上传网络。

2026-07-22 第 60 阶段完整回归：针对性测试 36/36，前端 25 个测试文件 209/209；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器真实加料结果复制出完整中文十行摘要，包含 655.20 平方毫米、2.00 毫米、1310.40 / 1357.38 / +1310.40 立方毫米、+46.98 立方毫米（+3.58%）、96.54% 和“加料增量一致”；成功反馈布局清晰，全新验收页 Console 无错误。仅保留既有 Vite 大分块警告。

### 61. 连续共面区域诊断摘要填入 Codex 指令草稿第一版（已实现）

- 结果卡在“复制几何诊断”旁新增“交给 Codex 分析”。只有用户主动点击时，页面才把当前有效诊断包装成全中文分析请求并追加到本地建模指令草稿。
- 请求固定包含完整十行诊断和“请分析几何链路是否合理并提出下一步修改建议”。指令区改为可编辑多行文本框，用户可继续补充要求，并可使用按钮或 Command/Ctrl + Enter 手动执行。
- 草稿已有用户文字时保留原文并在后方追加诊断块；同一完整诊断重复点击只保留一份。过期修订、无效结果或空摘要不会生成可用入口。
- 草稿状态只保存在当前页面 React 状态中，不进入 Zustand Store、项目版本、快照或 Worker；点击入口不会调用 Codex、API、网络或“执行建模指令”。

2026-07-22 第 61 阶段完整回归：针对性测试 40/40，前端 25 个测试文件 213/213；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器使用真实 Worker 在 655.20 平方毫米区域向外加料 2.00 毫米，先写入“确认这个区域不会碰到内部元件”的用户要求，再点击入口；草稿完整保留原文并追加 1310.40 / 1357.38 / +1310.40 立方毫米、+46.98 立方毫米（+3.58%）、96.54% 和“加料增量一致”的中文诊断。连续点击两次后诊断标题仍只有一处，消息数量保持 3 条且未自动执行，页面 Console 无错误。仅保留既有 Vite 大分块警告。

### 62. 连续共面区域诊断草稿块识别与局部移除第一版（已实现）

- `App` 会在当前页面内登记由“交给 Codex 分析”实际注入的完整诊断请求；手工粘贴的相似文本不会被误认成系统可删除块，也不写入 Zustand Store、项目版本或快照。
- 指令区新增“几何诊断草稿”状态卡。唯一完整块显示“移除诊断块”；已编辑、残缺、重复或混合冲突的块显示全中文保护提示并禁用移除，不做标题猜测或模糊删除。
- 纯函数只删除唯一完整匹配的本页系统块，保留块前后全部用户文字，并把相邻分隔空行归一为一个空行；移除后明确提示剩余指令尚未执行。
- 网格编辑面板打开时，指令面板会自动左移，避免操作按钮被右侧编辑面板遮挡；该布局调整不改变建模状态或视口拾取。

2026-07-22 第 62 阶段完整回归：针对性测试 45/45，前端 25 个测试文件 218/218；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器真实 Worker 在 1552.52 平方毫米区域向外加料 2.00 毫米，完整诊断包含平面估算 3105.04、实际工具体积 3216.35、模型变化 +3105.04 立方毫米、+3.58% 偏差和 96.54% 作用比例。唯一完整块可点击移除，前后两段用户文字均保留、消息数量保持 3 条且未自动执行；重新注入后手工修改作用比例，状态立即变为安全拒绝且按钮禁用。干净验收页 Console 无错误，仅保留既有 Vite 大分块警告。

### 63. 连续共面区域诊断草稿块只读摘要与精确定位第一版（已实现）

- 唯一完整诊断块的状态卡现在显示块行数、操作模式和方向状态；摘要直接从当前完整块派生，不复制诊断正文到第二份状态，字段缺失时显示“未识别”。
- 新增纯函数返回唯一完整块的精确字符起止范围。已编辑、残缺、重复或冲突状态一律返回空范围，不按标题或行内容猜测定位。
- “定位诊断块”会聚焦建模指令文本框、把完整块滚动到可见区域并精确选中；它只改变 textarea 的选择区和滚动位置，不改写草稿、不执行指令，也不调用网络、Worker 或版本能力。
- 编辑或歧义状态下，“定位诊断块”和“移除诊断块”同时禁用，继续保留中文安全说明。

2026-07-22 第 63 阶段完整回归：针对性测试 49/49，前端 25 个测试文件 222/222；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器真实 Worker 诊断显示“共 13 行 · 操作模式：向外加料 · 方向状态：加料增量一致”，在 12 行用户文字之后点击定位，文本框获得焦点、滚动到 174 像素并精确选中第 148 至 374 个字符的完整 13 行请求；消息数量保持 3 条且未自动执行。手工修改方向状态后两个管理按钮均禁用，Console 无错误。仅保留既有 Vite 大分块警告。

### 64. 连续共面区域旧诊断块替换为最新诊断第一版（已实现）

- 当前共面操作产生的新诊断与草稿中唯一完整本页旧块不同时，结果卡显示“替换为最新诊断”；相同内容显示“当前诊断已在草稿”并禁用重复操作。
- 纯函数只接受非空最新摘要和唯一完整旧块，返回 `replaced / duplicate / unsafe / invalid`；成功时精确替换块内文本，块外用户文字、空格和换行位置保持原样。
- `App` 在同一点击事件内更新受控草稿和页面级系统块登记，旧登记被最新完整块替代；编辑、残缺、重复、冲突或未登记文本统一进入保护状态，不回退为追加第二个块。
- 替换只修改当前页面 React 状态，不调用 Codex/API、Worker 或网络，不写入 Zustand Store、项目版本与快照，也不自动执行建模指令；用户手工修改后旧成功反馈会立即失效。

2026-07-22 第 64 阶段完整回归：针对性测试 53/53，前端 25 个测试文件 226/226；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器先在 1552.52 平方毫米区域向外加料 2.00 毫米并注入诊断，再以 3.00 毫米生成最新结果；点击替换后前后用户要求完整保留，诊断标题仍为 1 处，旧 2.00 毫米被 3.00 毫米及 4657.56 / 4768.87 / +4657.56 立方毫米、+2.39%、97.67% 替代，消息数量保持 3 条且未自动执行。手工修改方向状态后再执行 1.00 毫米操作，按钮变为“诊断草稿需手工处理”并拒绝覆盖，Console 无错误。仅保留既有 Vite 大分块警告。

### 65. 连续共面区域旧诊断与最新诊断字段差异摘要第一版（已实现）

- 仅在草稿包含唯一完整本页旧诊断、当前结果已生成不同的最新诊断且现有安全动作允许替换时，显示“最新诊断将更新 N 项”全中文只读卡片。
- 纯比较函数按固定业务顺序提取操作模式、区域面积、作用距离、执行前平面估算、实际工具体积、工具体构造偏差、模型体积变化、实际作用比例和方向状态，只返回两侧均存在且值确实变化的字段。
- 相同请求返回空差异；字段缺失不补造，编辑、残缺、重复、冲突、未登记或空最新摘要统一不返回可展示差异，组件不会绕过原有替换安全边界。
- 卡片只从当前草稿和最新摘要即时派生，不保存第二份完整正文；点击替换前不会修改草稿、系统块登记、Store、项目版本或快照，也不调用 Codex/API、Worker、网络或自动执行。

2026-07-22 第 65 阶段完整回归：针对性测试 56/56，前端 25 个测试文件 229/229；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器以 1552.52 平方毫米区域完成真实 2.00→3.00 毫米加料对照，差异卡按固定顺序显示作用距离、执行前平面估算、实际工具体积、工具体构造偏差、模型体积变化和实际作用比例共 6 项；替换前旧草稿及前后用户文字保持不变，消息数量保持 3 条，替换后卡片消失。手工修改方向字段后按钮进入安全拒绝且差异卡隐藏，Console 无错误。仅保留既有 Vite 大分块警告。

### 66. 连续共面区域诊断字段差异按需复制第一版（已实现）

- 差异卡只在当前草稿仍可安全替换且存在有限变化字段时显示“复制差异摘要”；替换完成、差异重新计算或草稿进入编辑/残缺/重复等不安全状态后，入口与旧反馈会随当前摘要身份自动失效。
- 纯格式化函数把现有固定顺序差异数组输出为“【共面区域诊断字段差异】”、变化数量和逐项“字段：旧值 → 新值”；空数组、空标签或空旧/新值安全返回空，不复制完整旧诊断或最新诊断正文。
- 剪贴板写入通过可注入异步函数执行，成功显示“差异摘要已复制，不会自动发送或执行”，失败显示全中文权限提示且不向界面抛出异常。
- 复制不会修改指令草稿、系统块登记、Store、项目版本或快照，不调用 Codex/API、Worker、网络和执行链路，也不会自动替换或提交。

2026-07-22 第 66 阶段完整回归：针对性测试 59/59，前端 25 个测试文件 232/232；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器以 1552.52 平方毫米区域完成真实 2.00→3.00 毫米加料对照，差异卡按固定顺序显示 6 项，复制内容仅包含标题、变化数量和有限字段，不含完整请求标题或分析提示。复制后草稿仍保留旧诊断及前后用户文字，3 条消息保持不变；替换为最新诊断后差异卡和旧复制反馈同时消失，标题保持 1 处且消息仍为 3 条。干净验收页 Console 无错误，仅保留既有 Vite 大分块警告。

### 67. 连续共面区域诊断差异复制内容预览第一版（已实现）

- 安全差异卡新增“预览复制内容 / 收起复制内容”，默认收起；展开后直接显示当前 `diagnosticDifferenceSummary`，预览和剪贴板写入严格复用同一字符串。
- 预览使用只读、可聚焦且可选择的多行文本容器，保留原始换行，并明确提示“以下内容仅供检查，不会自动发送或执行。”
- 预览展开状态和复制反馈仅保存在以摘要文本为 `key` 的局部组件中；替换诊断、重新计算出不同摘要或进入不安全状态时组件重新挂载或卸载，旧状态不会串到新摘要。
- 预览不会修改指令草稿、系统块登记、Store、项目版本或快照，不调用 Codex/API、Worker、网络和执行链路，也不提供编辑入口。

2026-07-22 第 67 阶段完整回归：针对性测试 62/62，前端 25 个测试文件 235/235；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器真实比较 3.00→2.00 毫米加料诊断并显示固定顺序的 6 项差异：默认不存在预览，展开后正文与捕获到的复制文本严格相同，样式为 `pre-wrap`、可选择且可键盘聚焦；收起后正文消失。再次展开并替换为最新诊断后，差异卡、预览和旧复制反馈同时清除；随后生成不同摘要时重新默认收起且不继承旧反馈。草稿前后用户文字保持不变，干净验收页 Console 无错误，仅保留既有 Vite 大分块警告。

### 68. 连续共面区域诊断差异预览一键全选第一版（已实现）

- 差异预览展开后显示“全选预览内容”，收起、替换或差异失效时入口随预览一起消失；选择只作用于当前只读 `<pre>` 正文。
- 可注入选择函数拒绝空引用和空正文，并把浏览器 Selection/Range 不可用或异常统一转换为非阻断失败状态。
- 成功显示“已全选当前预览内容，不会自动复制或执行。”，失败显示“无法全选预览内容，请手动拖动选择。”；点击不会调用 Clipboard API。
- 选择反馈与预览、复制反馈共享摘要身份隔离；收起再展开或出现不同摘要时恢复“全选预览内容”，不修改草稿、系统块登记、Store、版本或快照。

2026-07-22 第 68 阶段完整回归：针对性测试 65/65，前端 25 个测试文件 238/238；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器真实展开 6 项差异摘要后，一键选择文本与预览正文严格相同，完整保留多行换行；剪贴板调用次数为 0，草稿保持不变。模拟 Selection 不可用时显示中文失败反馈；收起后入口和反馈消失，重新展开恢复初始状态，替换最新诊断后差异卡、预览与选择反馈全部清除。干净验收页 Console 无错误，仅保留既有 Vite 大分块警告。

### 69. 连续共面区域诊断差异预览内容统计第一版（已实现）

- 差异复制预览展开时显示“共 N 行 · N 个字符”，收起、替换或差异失效时统计与正文一起消失。
- 行数和字符数直接从当前预览正文纯派生；字符数按 Unicode 码点统计，CRLF、LF 和 CR 均按实际字符计入总数，Emoji 不会被按 UTF-16 代理项重复计数。
- 统计不截断、不改写、不缓存第二份摘要正文，不影响“全选预览内容”的 Selection 范围，也不修改草稿、系统块登记、Store、版本或快照。
- 摘要身份变化继续复用 keyed 子组件隔离；不同摘要重新展开时只展示新正文对应的统计，不继承旧选择反馈。

2026-07-22 第 69 阶段完整回归：针对性测试 67/67，前端 25 个测试文件 240/240；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器在 1552.52 平方毫米共面区域比较 2.00→3.00 毫米真实加料诊断，6 项差异预览显示“共 8 行 · 230 个字符”，与从预览正文独立计算的结果严格一致；一键全选 Selection 不受影响。收起后统计隐藏，重新展开显示相同统计且没有旧反馈；替换后差异卡、预览和统计全部消失，草稿前后用户文字完整保留。干净验收页 Console 无错误，仅保留既有 Vite 大分块警告。

### 70. 任意封闭模型六向打印方向评估第一版（已实现）

- 当前精确 CAD 零件和任意上传 STL 均可执行 +X、-X、+Y、-Y、+Z、-Z 六个轴向打印方向评估；CAD 优先分析场景中当前选中零件，上传模型使用当前工作 STL，不绑定电子外壳、开发板、手办或固定文件名。
- 独立纯计算模块支持索引网格和 STL 展开后的非索引三角网格，并使用封闭体有向体积统一整体三角面绕序；每个候选计算旋转后宽、深、高、P1S 256 × 256 × 256 毫米成型空间适配、近似底面接触面积、45° 悬垂支撑面积和表面积占比。
- 推荐评分只在成型空间适配的候选中比较悬垂比例、打印高度和底面接触；结果卡全中文展示推荐理由、六个方向的尺寸、风险等级、接触与悬垂数据，全部超出空间时明确建议先拆件或缩小模型。
- 分析结果只存在于面板局部状态，不修改对象变换、Store、版本或快照；模型来源、CAD 零件、工作 STL 或修订变化时立即清除旧结果，异步旧请求也不能覆盖新模型。
- 结果明确标注为轴向几何估算，不伪装为切片器生成的真实支撑、耗材、打印时间、桥接、热变形或受力结果。

2026-07-22 第 70 阶段完整回归：针对性测试 6/6，前端 26 个测试文件 246/246；TypeScript/Vite 生产构建和 `git diff --check` 通过。浏览器分别分析 CAD 主体、CAD 上盖和通过“拆件与连接”上传的任意 STL：主体与上传 STL 均显示 1,608 个有效三角面并推荐 Z 正方向朝上，尺寸 62.6 × 32.6 × 12.6 毫米、悬垂 22.0 平方毫米（0.3%）、底面接触 2027.0 平方毫米；切换上盖、模型来源和快速预览时旧结果立即清除，快速预览入口禁用并显示精确实体提示。验收页 Console 无错误或警告，仅保留既有 Vite 大分块警告。

### 71. 六向打印方向建议一键应用第一版（已实现）

- 六向分析存在可打印推荐且来源身份仍有效时，结果卡提供“应用推荐方向”；确认卡明确显示当前对象名称、目标朝上方向和应用后毫米尺寸，取消不会修改对象或创建版本。
- 六个候选映射为确定性的绝对 Three.js XYZ 欧拉角，使用绝对旋转而不是累计增量；模 360° 等价判断会阻止重复应用同一方向。
- 应用只修改当前 CAD 零件或任意上传 STL 对象的 `rotationDeg`，完整保留位置、均匀缩放、颜色、其他零件和几何文件；上传 STL 始终写入真实视口对象 `uploaded-model`。
- 写入复用对象展示编辑、中文版本、撤销和重做链路；成功后清除旧分析并提示重新分析，来源、对象、修订或缩放变化也会使旧分析失效。
- 六向分析已纳入当前对象均匀缩放：尺寸、面积和体积分别按缩放、缩放平方和缩放立方计算，并以缩放后的真实毫米尺寸检查 P1S 成型空间。

2026-07-22 第 71 阶段完整回归：针对性测试 18/18，前端 26 个测试文件 253/253；TypeScript/Vite 生产构建和 `git diff --check` 通过。真实浏览器验证 CAD 主体从 X 旋转 30° 应用到推荐绝对方向，取消保持版本 2/2，确认生成版本 3/3，撤销恢复 30°、重做恢复 0°，重新分析后显示“当前对象已是推荐打印方向”。主体 1.5 倍缩放后尺寸为 93.9 × 48.9 × 18.9 毫米、悬垂 49.5 平方毫米、底面接触 4560.8 平方毫米；任意上传 STL 同样完成确认应用。验收页 Console 为 0 个错误、0 个警告。

### 72. 推荐打印方向应用后的自动落床预览与确认第一版（已实现）

- 推荐方向应用并重新分析后，结果卡会显示全中文“自动落床预览”，列出当前最低点、打印平台 0 毫米、目标 Y 位置和垂直位移；系统不会在分析完成后自动移动模型。
- 原始 STL 使用 Z 轴向上，视口使用 Y 轴向上：原始 X 映射视口 X、原始 Z 映射视口 Y、原始 Y 映射视口 -Z。CAD 零件采用对象内归一化，任意上传 STL 采用对象外世界归一化，避免旋转后最低点计算混淆。
- 用户二次确认后只修改当前对象的 `positionMm.y`，保留水平位置、绝对旋转、均匀缩放、颜色、其他零件和几何文件；已落床时直接显示完成状态，不再提供重复操作。
- 确认写入复用对象展示版本、撤销和重做链路，版本名为“将‘对象名称’落到打印平台”；取消、来源过期、目标越界或写入失败不会产生伪版本。
- 预览身份绑定对象、模型修订、位置、旋转、缩放、基础显示位置和归一化空间；任一条件变化都会使旧预览失效。成功提示通过待处理引用跨来源身份刷新保留，避免写入后被立即清除。

2026-07-22 第 72 阶段完整回归：针对性测试 24/24，前端 26 个测试文件 259/259；TypeScript/Vite 生产构建和 `git diff --check` 通过。真实浏览器验证 CAD 主体 Y=10 毫米和任意上传 STL Y=7.5 毫米两条路径，覆盖取消、确认、撤销、重做、重复落床保护与成功提示保留；确认后两者最低点均为 0.00 毫米，验收页 Console 为 0 个错误、0 个警告。

### 73. 自动落床后的平台边界适配与只读居中预览第一版（已实现）

- 已落床对象会基于当前精确 STL、绝对旋转、均匀缩放、对象位置和真实归一化层级计算视口 X/Z 包围范围；CAD 对象内归一化与任意上传 STL 对象外世界归一化分别处理，不绑定固定模型名称或业务类型。
- P1S 平台按 256 × 256 毫米、中心 X=0/Z=0 计算左、右、前、后四边余量；完整适配时显示最小安全余量，任一方向越界时显示对应毫米越界量。
- 只读居中建议会给出 X/Z 移动方向、位移量和目标水平位置。分析结果不会移动对象、创建版本、排列其他零件、修改装配关系或改写 STL、STEP、3MF 几何文件。
- 平台卡仅在推荐方向已应用、对象已经落床且分析来源仍有效时显示；对象、修订、位置、旋转、缩放、基础位置或归一化空间变化后旧预览立即失效。

2026-07-22 第 73 阶段完整回归：针对性测试 29/29，前端 26 个测试文件 264/264；生产构建和 `git diff --check` 通过。真实浏览器验证 CAD 主体 1.5 倍缩放并移动到 X=100/Z=-30 后右侧越界 18.95 毫米，建议向左 100.00 毫米、向前 30.00 毫米；任意上传 STL 默认占地 62.60 × 32.60 毫米并水平居中，移动到 X=-110/Z=40 后左侧越界 13.30 毫米，建议向右 110.00 毫米、向后 40.00 毫米，目标均为 X=0/Z=0。

### 74. 平台居中建议确认应用第一版（已实现）

- 已落床、来源有效且尚未水平居中的当前对象会显示“应用居中位置”；全中文确认卡列出对象名称、当前 X/Z、目标 X/Z 和两轴位移，取消不会修改对象或创建版本。
- `createPrintPlatformCenterPresentation` 通过规范化对象展示状态后只替换 `positionMm.x` 与 `positionMm.z`，完整保留 Y、绝对旋转、均匀缩放、颜色、其他对象、装配关系和 STL/STEP/3MF 几何文件。
- 写入前会重新核对来源身份、推荐方向、落床状态、Store 实时位置/旋转/缩放、平台目标与位移一致性，并拒绝过期来源、已居中、非有限目标或超出对象变换安全范围的目标。
- 确认复用对象展示版本、撤销和重做链路，中文版本名为“将‘对象名称’移动到打印平台中心”；成功后旧分析失效并提示重新分析，重复居中时只显示完成状态。

2026-07-22 第 74 阶段完整回归：针对性测试 32/32，前端 26 个测试文件 267/267；TypeScript/Vite 生产构建和 `git diff --check` 通过。真实浏览器验证任意上传 STL 从 X=-110/Z=40 居中到 0/0，取消保持版本 7/7，确认生成版本 8/8，撤销恢复原位置、重做恢复 0/0，重新分析后显示四边余量 96.70/96.70/111.70/111.70 毫米且不再提供重复应用；CAD 主体在 1.5 倍缩放、X=100/Z=30 时确认后只把 X/Z 改为 0/0，撤销和重做保持缩放。来源位置变化会立即清除旧预览，验收页 Console 为 0 个错误、0 个警告。

### 75. 平台安全边距设置与单对象可打印区域预览第一版（已实现）

- 平台卡新增全中文“平台安全边距”毫米输入，默认 5 毫米；只接受有限、非负且能为 P1S 256 × 256 毫米平台保留正宽深有效区域的值，非法输入显示中文原因并隐藏无效预览。
- `evaluatePrintPlatformSafetyArea` 从既有物理平台四边向内收缩边距，独立输出有效边界、宽深、四边安全余量或越界量、最小安全余量、是否可仅靠平移适配，以及最小 X/Z 修正。
- 物理平台适配、物理四边余量和原居中目标继续保留；安全区域外时显示“只读修正建议”，对象大于有效区域时明确提示单纯移动无法完全进入，不伪造可执行结果。
- 安全边距是面板本地只读设置，变化只重新派生安全区域，不修改对象、不创建版本、不清除当前分析，也不改变来源身份、居中确认目标、撤销或重做链路。

2026-07-22 第 75 阶段完整回归：针对性测试 39/39，前端 26 个测试文件 274/274；TypeScript/Vite 生产构建和 `git diff --check` 通过。真实浏览器验证 CAD 主体 1.5 倍缩放、X=20/Z=0 时，把安全边距改为 80 毫米后有效区域为 96.00 × 96.00 毫米，右侧越界 18.95 毫米并只读建议向左修正 18.95 毫米，物理平台仍完整适配且版本保持 6/6；任意上传 STL 默认 5 毫米时有效区域为 246.00 × 246.00 毫米，四边安全余量为 91.70/91.70/106.70/106.70 毫米，改为 20 毫米后分析保留且版本保持 2/2。独立验收页 Console 为 0 个错误、0 个警告。

### 76. 安全区域修正建议确认应用第一版（已实现）

- “应用安全区域修正”只在当前对象已落床、来源和安全边距有效、对象可仅靠平移进入有效区域、当前尚未适配且最小修正非零时出现；已适配、对象过大或建议为零时不会提供伪入口。
- 全中文确认卡冻结对象名称、安全边距、当前 X/Z、两轴修正方向和毫米值以及修正后 X/Z；安全边距变化会立即关闭旧确认，确认时仍会重新核对来源、实时变换和快照一致性。
- `createPrintPlatformSafetyCorrectionPresentation` 只把建议的最小 X/Z 位移加到当前对象位置；Y、绝对旋转、均匀缩放、颜色、其他对象、装配关系、物理居中目标和 STL/STEP/3MF 几何文件保持不变，不强制移动到平台中心。
- 写入前通过平移后的物理平台预览重新计算安全区域，只有修正后同时位于物理平台和有效区域内才提交；成功生成“将‘对象名称’移动到平台安全区域”中文展示版本，支持撤销和重做并提示重新分析。

2026-07-22 第 76 阶段完整回归：针对性测试 44/44，前端 26 个测试文件 279/279；TypeScript/Vite 生产构建和 `git diff --check` 通过。真实浏览器验证 CAD 主体在 80 毫米边距下从 X=20 最小修正到 X=1.05 而非 0，取消保持版本 1/1，确认生成版本 2/2，撤销和重做保持 Y、旋转与 1.5 倍缩放；任意上传 STL 在 X=110/Z=120 下完成双轴修正，10 毫米边距目标为 X=86.70/Z=101.70，边距变化关闭旧确认，重新分析显示“位于安全区域”。独立验收页 Console 为 0 个错误、0 个警告。

### 77. 平台安全区域边界三维视口可视化第一版（已实现）

- 新增独立 `PrintPlatformOverlay` 只读协议，把 P1S 物理平台、安全有效区域、当前对象 X/Z 水平占地、四边越界量和 `inside / overflow / too-large` 状态从打印分析结果安全复制到三维视口；协议不携带对象展示状态或可执行修正。
- 已落床且来源仍有效时，三维视口按真实毫米坐标绘制蓝灰短虚线物理边界、绿色长虚线安全边界和青/橙/红对象占地；左、右、前、后真实越界边使用更宽的橙色或红色线段高亮，并配套全中文图例。
- 叠加使用独立 Store 临时状态，不进入 `ModelVersion`、撤销重做快照或 STL/STEP/3MF 导出；图例和三维线框不接收鼠标事件，不影响对象选择、操控器、视角旋转、缩放和平移。
- 对象来源、位置、旋转、缩放、模型切换或重新分析变化时旧叠加立即清除；合法安全边距变化从当前分析同步刷新轮廓和越界量，旧来源 cleanup 不会误删后来写入的新结果。
- 第一版只显示单个当前对象相对矩形平台的静态水平轮廓，不显示 Brim、裙边、支撑、热床夹具、喷头禁入区、多对象排布或 Bambu Studio 切片占地。

2026-07-22 第 77 阶段完整回归：针对性测试 53/53，前端 28 个测试文件 288/288；TypeScript/Vite 生产构建和 `git diff --check` 通过。真实浏览器验证 CAD 主体 X=110/Z=120 时同时高亮右侧 18.30 毫米和前侧 13.30 毫米越界，安全边距从 5 改为 10 毫米后同步更新为 23.30/18.30 毫米且版本保持 3/3；实际鼠标旋转、滚轮缩放和右键平移均改变视角，图例命中测试穿透到 Canvas。任意上传 `imported-model.stl` 显示通用名称和相同双轴状态，10 毫米修正至 X=86.70/Z=101.70 后重新分析切换为“位于安全有效区域”，分析与叠加不创建版本。独立验收页 Console 为 0 个错误、0 个警告。

### 78. 打印平台叠加一键俯视与适配视野第一版（已实现）

- 有效打印平台叠加存在时，视口图例下方显示全中文“俯视并适配平台”；叠加因对象来源、位置、旋转、缩放、模型切换或重新分析失效时，入口同步清除。
- `printPlatformCamera.ts` 使用纯函数合并 P1S 物理平台与当前对象水平占地，按视口宽高比、34° 垂直视场角和安全倍率计算俯视中心、控制器目标与有限相机距离；对象单轴、双轴越界或大于平台时仍完整纳入视野。
- 每次点击只创建递增的临时视口请求，并在约 0.46 秒内平滑插值相机位置和 `OrbitControls.target`；请求按来源身份与请求编号共同去重，来源变化后编号重新从 1 开始也不会吞掉新请求。
- 相机适配只修改 Three.js 临时状态，不修改对象位置、旋转、缩放、颜色，不写入 Zustand 模型版本、撤销重做、项目存储、打印分析身份或 STL/STEP/3MF 导出；远裁剪面会随较大适配距离安全扩展。
- 聚焦完成后仍可继续鼠标旋转、滚轮缩放和右键/修饰键平移；图例保持事件穿透，按钮单独接收鼠标和键盘焦点。

2026-07-23 第 78 阶段完整回归：针对性测试 18/18，前端 29 个测试文件 297/297；TypeScript/Vite 生产构建和 `git diff --check` 通过。真实浏览器覆盖平台内、越界与安全修正后的对象，连续点击不会进入模型内部或产生空白，旋转、缩放、平移仍可用，版本分别保持 1/1 与 3/3。来源失效后按钮立即清除；重新分析生成新来源时，其首次俯视请求即使编号同为 1 仍实际改变 Canvas。验收页 Console 为 0 个错误、0 个警告。

### 79. 打印平台俯视前视角返回第一版（已实现）

- 第一次执行“俯视并适配平台”前，视口临时复制当前相机位置和 `OrbitControls.target`；同一打印平台来源内重复俯视只复用最初快照，不会把俯视后或用户中途旋转后的视角覆盖为新的返回点。
- 快照存在且仍属于当前打印平台来源时显示全中文“返回原视角”；返回复用约 0.46 秒平滑动画，同时恢复相机位置和控制器目标，完成后消费快照并隐藏入口。
- 俯视与返回请求共享递增编号但带独立类型，消费键同时包含请求类型、来源身份和编号；返回请求只携带来源，不复制第二份相机快照。
- 原视角快照会校验相机及目标三轴有限值，并拒绝两者重合；来源失效、对象变换、模型切换、新建画布或叠加清除时同步删除快照和入口。
- 快照、请求和动画只存在于 React/Three.js 临时视口状态，不进入 Zustand 模型状态、版本、撤销重做、项目存储、打印分析身份或 STL/STEP/3MF 导出；返回后仍可继续旋转、缩放、平移和选择。

2026-07-23 第 79 阶段完整回归：针对性测试 23/23，前端 29 个测试文件 302/302；TypeScript/Vite 生产构建和 `git diff --check` 通过。真实浏览器在初始视角俯视后显示唯一“返回原视角”，手工旋转再重复俯视仍保留最初快照；返回后的 Canvas PNG 与俯视前逐字节一致，入口自动隐藏且版本保持 1/1。再次俯视后修改对象 X 位置，旧平台叠加、俯视和返回入口同时清除。验收页 Console 为 0 个错误、0 个警告。

### 80. 打印平台半透明床面与中心、前向标识第一版（已实现）

- 有效打印平台叠加会从物理边界派生真实毫米中心、宽度和深度，并在 X/Z 平面显示低透明度床面；床面关闭深度写入，不会遮挡模型、安全有效区域或对象占地线框。
- 床面中心显示金色十字，前侧边界内显示全中文“前侧（Z 正）”；前侧固定对应物理平台最大 Z，与现有前侧越界和安全修正语义一致，不依赖 CAD、上传 STL 或任何示例模型名称。
- `PrintPlatformBedGuide` 只保存来源身份和派生显示几何；床面、十字与标签均关闭射线命中或 DOM 指针事件，不参与选择、框选、操控器、版本、撤销重做、项目保存和 STL/STEP/3MF 导出。
- 来源失效或叠加清除后全部床面提示同步移除；空来源、非有限值和零宽/零深退化边界由纯函数安全拒绝，不会创建 NaN 几何或导致空白视口。

2026-07-23 第 80 阶段完整回归：针对性测试 12/12，前端 29 个测试文件 305/305；TypeScript/Vite 生产构建和 `git diff --check` 通过。真实浏览器验证半透明床面、金色中心十字和“前侧（Z 正）”标识可见，标识 `pointer-events: none`，点击标识后仍可在画布旋转，Canvas 发生变化而版本保持 4/4；对象来源变化后床面标识、俯视和返回入口同步清除。验收页 Console 为 0 个错误、0 个警告。

### 81. 打印平台毫米网格与坐标刻度第一版（已实现）

- 有效打印平台叠加会从物理边界按真实毫米生成固定 10 毫米次网格和 50 毫米主网格；所有线段严格裁剪在床面范围内，不假设平台以原点对称，也不依赖对象、上传文件或示例模型类型。
- X/Z 主网格显示全中文毫米刻度，正坐标显式带“+”，零刻度显示“（原点）”；X 坐标线沿 Z 方向、Z 坐标线沿 X 方向，最大 Z 继续对应既有“前侧”语义。
- 主次网格采用低透明度和不同线宽，位于床面之上、中心十字与安全区域之下；网格线关闭射线命中，刻度标签设置 `pointer-events: none`，不会阻断选择、框选、操控器或视角操作。
- 网格协议只消费来源身份与物理平台边界，不增加 Store、模型版本、撤销重做、项目保存或 STL/STEP/3MF 导出字段；来源失效、退化边界、非有限数值或异常超大坐标范围会安全清除网格。

2026-07-23 第 81 阶段完整回归：针对性测试 16/16，前端 29 个测试文件 309/309；TypeScript/Vite 生产构建和 `git diff --check` 通过。真实浏览器显示 10 个 P1S 主刻度，原点与正负毫米文案正确，标签命中位置实际落到 Canvas；从“Z 轴 +50 毫米”标签位置真实拖动可旋转视角，Canvas 变化而版本保持 4/4。对象来源变化后网格刻度、床面标识和俯视入口同步清除，验收页 Console 为 0 个错误、0 个警告。

### 82. 打印平台多对象联合占地预览第一版（已实现）

- 当前单对象打印平台分析仍有效时，后台会读取当前装配中全部可打印精确 STL，按各对象真实位置、旋转、均匀缩放和装配基础位置派生 X/Z 毫米占地；CAD 零件、任意上传 STL 和制造拆件均走通用来源协议，不绑定示例名称。
- 每个可打印对象以不同颜色虚线显示只读占地，整体联合边界使用黄色粗虚线；中文摘要列出对象数量、整体宽深、每个对象尺寸与安全区域状态，并统计被排除的参考对象、隐藏对象和无有效几何对象。
- 制造拆件使用保留原始坐标的 `preserved` 归一化模式，避免把每个拆件分别居中而破坏联合占地；普通 CAD 使用对象局部归一化，上传 STL 复用整体显示基础位置。
- 联合预览独立于模型版本和几何导出，不自动移动、旋转、缩放或排列对象；几何修订、对象变换、可见性、来源或单对象平台分析失效时同步清除，异步旧请求不能覆盖新状态。

2026-07-23 第 82 阶段完整回归：针对性测试 55/55，前端 30 个测试文件 318/318；TypeScript/Vite 生产构建和 `git diff --check` 通过。真实浏览器识别主体与上盖 2 个可打印对象、参考对象 1 个，显示各对象与整体联合轮廓并判定均位于安全有效区域；从摘要区域拖动可旋转 Canvas，版本保持 1/1，修改主体 X 位置后旧联合预览立即清除。验收页 Console 为 0 个错误、0 个警告。

**下一阶段：**实现打印平台多对象间距与重叠诊断第一版；基于现有对象占地计算两两重叠、最小水平间距和可配置安全间距，只读标出需要分离的对象，不自动移动零件。
